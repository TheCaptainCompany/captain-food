# Spec log — what the DSL now says differently

**One row per landed `specs/**` change, newest first.** This page exists to satisfy one standing
product-owner obligation, verbatim: *"Just keep me informed"*
([ADR-20260810-221840](adr/ADR-20260810-221840-specs-are-the-teams-work-the-freeze-is-lifted.md)).

It is a **pull surface, not a report**. There is no cadence, no weekly digest and nothing to send.
Read it when you want to know what the product now promises that it did not before — the same way you
read [`docs/proposals/DECISIONS.md`](proposals/DECISIONS.md). It is kept current by a gate, not by a
habit.

> **Not a changelog of files.** `git log -- specs/` already says which files changed and that is
> exactly the thing nobody can read. The **"What the product now promises differently"** column is
> the whole point of this page: one sentence, in product language, that someone who has never opened
> the repo can evaluate. A row that says *"added field `x` to `Y`"* has failed and should be rewritten.

---

## How to write a row

Write it **in the same commit as the spec change**. Cost: one sentence. If the sentence is hard to
write, that is signal — it usually means the change is a Tier 2 that has not noticed yet.

### The tier is the boundary, and writing it is where the boundary is enforced

| Tier | Test | What it means |
|---|---|---|
| **0 — free** | Nothing emitted, nothing stored, no client shipped, no recorded decision touched | Land it. One row here, and that is the whole obligation. |
| **1 — migration** | The shape already exists in `domain_events`, in a shipped client, in an alert route, in a partner contract (HubRise/Stripe/Uber), or in a legal artifact (allergens, VAT, receipts, retention) | **Not an edit — a migration.** The versioning story is recorded *before* it lands. Stored events are immutable contracts: upcasting, never mutation (Greg Young, *Versioning in an Event Sourced System*). The GDPR tombstone path ([ADR-20260731-160000](adr/ADR-20260731-160000-order-erasure-tombstone-then-stream-deletion.md)) is the one recorded exception and is not a precedent. |
| **2 — decision** | It contradicts, or creates, a recorded decision in [`DECISIONS.md`](proposals/DECISIONS.md) or `docs/adr/` | **Stop.** This is a decision reversal wearing a spec edit's clothes, and it is not the team's to make regardless of how small the diff is. File a register row instead. **A Tier 2 row should never appear on this page** — if you are writing one, the change should not be landing. |

Structure is not a tier. A structural change with a real option space is routed by CLAUDE.md's
**proportionality** rule to a proposal + tracking issue + a register row — which *is* the discussion
the product owner offered. A structural change with no option space (a file move that rewrites no
`$ref`s, because refs are kind-logical) is a Tier 0 like any other.

> **Known blind spot: the tier test is applied to the `specs/**` diff, so a stored-shape change
> arriving through CODE is invisible to it** (found in review of
> [#469](https://github.com/TheCaptainCompany/captain-food/issues/469), 2026-08-11). That change
> widened what is written to `domain_events` envelopes — every open-path command now stamps a
> `user_id`/`user_type` it did not carry before — while its `specs/**` diff is two descriptions and
> a metric, i.e. a correct **Tier 0** on both its rows. Tier 1 asks "is the shape already emitted or
> stored?", but it only ever gets asked of the YAML. Until a gate closes this, ask the tier question
> of the **change**, not of the diff: if the code half alters what lands in `domain_events`, in a
> shipped client, in an alert route or in a legal artifact, it is a Tier 1 conversation whatever the
> YAML says.

---

## Rows

| Date | What the product now promises differently | Tier | Change | `make validate` |
|---|---|---|---|---|
| 2026-09-05 | **A restaurant manager can now actually see their team and invite list, invite a colleague, and revoke someone from a real screen — and an invited person now has somewhere to land the invitation link.** Round 2 of #639 part C step 6-iv finishes the slice: `/team` renders the live `restaurantRoster`/`restaurantInvitations` queries (per-row revoke, manager-gated), an invite sheet dispatches `inviteRestaurantMember`, and a new hand-written `/invitation` landing page sequences the two-lane accept client-side (`acceptRestaurantInvitation` then `grantRestaurantAccessByInvitation`, with bounded retry on the second leg so someone who already accepted never sees "link no longer valid"). All of it still ships DARK behind `RUN_RESTAURANT_INVITATION`. Declared gap, not silently skipped: the revoke action has no confirmation step (the SDUI `open_bottom_sheet` primitive carries no per-row context to open one against; documented on the screen rather than built ad hoc), and `invitationPreview` (showing the invited restaurant's name before accept) is not built — it needs a resolver shape combining live token verification with a read-model lookup that the mechanical query-emission pipeline has no precedent for. | 0 | `specs/screens/restaurant_backoffice.yaml`: new `/team` screen (roster + invitation list, invite sheet, per-row revoke), new `invitation_accept` screen (`sdui: false`). `specs/screens/restaurant_backoffice.translations.yaml`: `back.team.*`/`back.invitation.*` keys. `specs/translations.code_refs.yaml`: `back.invitation.*` registered as hand-written-Rust-consumed. `crates/web/src/invitation_accept.rs` (new): the SSR shell + browser-side two-leg dispatch. `crates/web/src/handwritten.rs`: new `HandWrittenScreen::InvitationAccept` arm. `crates/web/src/router.rs`: `invitation_accept` added to the PUBLIC-screen exhaustive test list. `docs/decisions/RESTAURANT-INVITATION-PRECONDITIONS.yaml`: Art. 13 -> Art. 14 correction for the invited address, [#902](https://github.com/TheCaptainCompany/captain-food/issues/902) expiry-delivery precondition added, retention ceiling declared `UNVERIFIED input`, Art. 30 entry, the MANAGER guard noted landed. `docs/adr/ADR-20260905-101349-...md` §2 amendment (the round-2 command split, with a Consulted block). `specs/common/scalars.yaml`: `MemberId` description gains the invitation-minted-before-bridge paragraph. | 0 errors |
| 2026-09-05 | **The invitation accept's second step is now its own public door instead of a hidden switch on the admin one, a manager's team page can now list who is actually on it, and only a manager — never a colleague with lesser standing — may invite or remove someone.** Round 2 of #639 part C step 6-iv, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md) §2 amendment (a presentation pass found round 1's `basis` argument on the admin-only grant let an anonymous caller mint a membership for any subject on any restaurant): `grantRestaurantAccessByInvitation` is a new, separate, public-only door that proves the caller is the same person who accepted before deriving anything, and `grantRestaurantAccess` reverts to admin-only with every field required again. A restaurant's own team roster and its invitation list (pending/accepted/revoked/expired) are now real, queryable read models — `restaurantRoster`/`restaurantInvitations` — and a manager-only guard now genuinely refuses a lesser-privileged colleague at the server, closing a gap round 1 could only describe. | 0 | `specs/network/commands.yaml`: `GrantRestaurantAccess` reverted to `roles: [ADMIN]`/all-required; new `GrantRestaurantAccessByInvitation` (`roles: [PUBLIC]`, `{invitationId, token}`). `specs/network/api.yaml`: `grantRestaurantAccessByInvitation` mutation; new `restaurantRoster`/`restaurantInvitations` queries + their connection/entry types. `specs/network/scalars.yaml`: `RestaurantInvitationStatus` gains `ACCEPTED_PENDING_ACCESS`. `specs/database/tables/projection_tables.yaml`: new `RestaurantRoster`/`RestaurantInvitationList` tables, own projector groups. `specs/stories.yaml`: `AcceptRestaurantMembershipInvitation`→`AcceptRestaurantInvitation`; `ManageTeam` gains the two roster/invitation-list read steps. `crates/server/src/graphql/acl.rs`: new `AuthorityGuard`. `migrations/20260905130000_restaurant_roster_and_invitation_list.sql`. | 0 errors |
| 2026-09-05 | **A restaurant manager can now invite a colleague by email instead of waiting on Captain to provision them by hand, and the platform can tell the difference between "an admin granted this" and "a real person accepted an invitation" without a second reservation table.** #639 part C step 6-iv, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md) §2/§3: the new `RestaurantInvitation` aggregate (own stream, own lane) records an invite, its accept, its revocation and its TTL expiry; `GrantRestaurantAccess` widens to accept `basis: MEMBER_INVITATION` as the two-lane accept's SECOND command (client-sequenced, never a saga) — the handler DERIVES the grant's `scopeId`/`memberId`/`authority`/`authSubject` from the invitation's own recorded facts, never from whatever the client also sent, so an invitation that was never accepted, or a wrong invitationId, refuses identically to every other terminal state (no enumeration oracle). Ships DARK behind `RUN_RESTAURANT_INVITATION` (default false). **NOT landed in this change, declared rather than silently skipped**: the `RestaurantRoster`/`RestaurantInvitationList` read models and the `/equipe`/`/invitation` screens — see [PR #901](https://github.com/TheCaptainCompany/captain-food/pull/901) and the PROP's §11 row 6 for the reasoning and the follow-up. | 0 | `specs/network/{scalars,commands,events,errors,rules,actors,configuration,api}.yaml`: the `RestaurantInvitation` aggregate + its four commands/four events/six errors/five rules; `GrantRestaurantAccess.{scopeType,scopeId,memberId,authSubject,authority}` now `nullable`, plus `invitationId`; `roles: [ADMIN, PUBLIC]`. `specs/stories.yaml`: `ManageTeam` (restaurant_manager) + `AcceptRestaurantMembershipInvitation` (public_user) activities. `specs/architecture/c4-l2.yaml`: `actor-restaurant-invitation` container. `specs/observability.yaml`: the `restaurant-invitation` contract. `specs/tests.yaml`: 20 new behaviour tests. `docs/decisions/RESTAURANT-INVITATION-PRECONDITIONS.yaml` (open). `docs/proposals/PROP-20260831-180622-...md` §8.2 `ADMINISTRATOR`→`MANAGER` (a stale draft word, the ADR already settled the vocabulary) + §11 row 6 updated. | 0 errors |
| 2026-09-05 | **The member sign-in confirmation no longer promises to echo an address it cannot paint, and the sign-in door's abuse-wall secret now actually stops a real deploy that forgot to set it.** Round 3 (ceiling) of 6-ii, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md), two fixes: (1) round 2's confirmation panel ended "...envoyé à :" followed by a `text` node meant to echo the typed address — but a `text` node paints from RESOLVER data at PAINT time, never from a form field's DISPATCH-time `.value` (the same class as the round-2 `#870` bug this file already named), so it always painted an empty line; the sentence is restored to the self-contained round-1 wording that never needed the address, and the real fix (a form-state-to-text binding) is left as the screen's `gaps:` line rather than shipped half-working. (2) `EMAIL_QUOTA_KEY_HMAC_SECRET`'s prose already claimed it was "required" in staging/production; it now actually is — an unset key in those profiles now STOPS the boot (exit 78) instead of silently signing real email addresses with the dev-only key. | 0 | `specs/screens/restaurant_backoffice.yaml`: `member_sign_in_confirmation_sheet`'s address-echo `text` node removed, one new `gaps:` line on `sign_in`. `specs/screens/restaurant_backoffice.translations.yaml`: `back.sign_in.confirmation_body` restored to the round-1 self-contained sentence. `specs/common/configuration.yaml`: `EMAIL_QUOTA_KEY_HMAC_SECRET` gains `required: [staging, production]`. `crates/web/src/renderer.rs`: new render test `member_sign_in_confirmation_panel_has_no_empty_echoed_address` (red against round 2's shape, green against this fix). | 0 errors |
| 2026-09-05 | **Every GraphQL role's schema now bounds how deep and how complex a request may be, and the member sign-in door's landing link now actually goes somewhere.** Round 2 of 6-ii discharges the PROP's `public-graph-limits` Concern it deferred: a `parse_query` extension on the ONE shared schema reads the request's acting role and refuses (before any resolver runs) a document past that role's depth or complexity ceiling — ceilings CODEGEN-DERIVED per role from the GENERATED client documents real screens bind, times a declared headroom (`GRAPHQL_LIMIT_HEADROOM_PERCENT`), never guessed; a role no resolver names gets the declared floor. Introspection is exempt (tooling depth is not attacker-controlled cost). The magic-link RETURN leg (`/sign-in/return`) is a new hand-written screen — the mail client's link previously opened nowhere; it now confirms, claims the parked session, and routes to the orders queue or the not-linked refusal. The confirmation panel is dismissable and echoes the typed address; the not-yet-linked screen's exit is a live control; the per-address email send-abuse wall now HMAC-hashes the address before it becomes a shared counter key (never the raw address) and is wired into the actor-runtime worker too (previously unguarded there); a misconfiguration can no longer burn a sign-in link before refusing. #639 part C step 6-ii round 2, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md) §9/§10/§13. | 0 | New generated module `crates/server/src/graphql/generated/limits.rs` (`MAX_DEPTH_<ROLE>`/`MAX_COMPLEXITY_<ROLE>`, `tools/codegen-rs` emitter); `crates/server/src/graphql/query_limits.rs` (the extension, registered unconditionally at schema build); new config key `EMAIL_QUOTA_KEY_HMAC_SECRET` (`specs/common/configuration.yaml`, secret, unset falls back to a dev-only key); `specs/screens/restaurant_backoffice.yaml` new `sign_in_return` screen + `confirm_member_sign_in` action, confirmation-sheet close control + address echo, `not_linked`'s exit control now `navigate`, new Art. 13 `gaps:` line on `sign_in`; `MEMBER-SIGN-IN-DOOR-PRECONDITIONS.yaml` names an owner for the legal/privacy page and the return-route/Art. 13/Art. 30 items; `application::email_guard::EmailSendPolicy` HMAC-hashes its quota key; `infrastructure::integrations::SupabaseIdentityService.email_guard` is non-`Option` (compiler-first); `crates/infrastructure/src/mailbox/standalone.rs` now wires the durable email guard; the `graphql-limits` observability contract's spans/metrics (declared dark in round 1) now have call sites; new tests: `crates/server/tests/graphql_role_limits.rs` (per-role depth/complexity boundary + a per-role differentiation proof), two `SupabaseIdentityService` guard-refusal tests, an HMAC key-hashing test, plus the round's mutant verifications. | 0 errors |
| 2026-09-05 | **A restaurant manager can now request a magic sign-in link and use it, identify-only, with no way to learn whether an email address is a member from the request alone.** `requestMemberSignInLink`/`confirmMemberSignIn` join `/public/graphql`, answering byte-identically for a real member and a stranger (the `Member` bridge is never consulted on the request leg); the token, once verified, is stamped with a bare `{role: MEMBER}` claim (no id — the same one-stamper-per-role pattern as the customer/rider claims) and the session is parked even when the address turns out unlinked, so the confirming page always gets a real cookie to retry against. The identity seam gains a MEMBER arm: `auth_subject -> Member -> ScopeMembership`, failing closed unless the member holds exactly one restaurant scope (V0 does not support more). Ships DARK: `RUN_MEMBER_SIGN_IN_DOOR` defaults `false` everywhere (behind the open `MEMBER-SIGN-IN-DOOR-PRECONDITIONS` decision row), and the PROP's `public-graph-limits` Concern is explicitly NOT discharged by this change — reasoned and recorded on the PR rather than silently skipped, since the two new mutations are the first unauthenticated PUBLIC writes and the per-role GraphQL depth/complexity limiter they were meant to ship beside needs a custom runtime extension (one master schema serves every role) that was not built this slice. #639 part C step 6-ii, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md). | 0 | New commands `RequestMemberSignInLink`/`ConfirmMemberSignIn` (`specs/network/commands.yaml`, `api.yaml`, `actors.yaml`); `identity.stamp_member_claim` (`specs/services.yaml`); errors `MemberNotLinked`, `MemberSignInRequiresSession`, `MemberSignInDoorClosed` (`specs/network/errors.yaml`); `InvalidVerificationToken` and `AuthSubjectHoldsAnotherRole` promoted to `specs/common/errors.yaml` (now thrown from more than one scope); `RUN_MEMBER_SIGN_IN_DOOR` (`specs/network/configuration.yaml`, default false) + `EMAIL_MAX_SENDS_PER_ADDRESS_PER_HOUR/DAY`, `EMAIL_MAX_SENDS_PER_DAY_GLOBAL`, `GRAPHQL_LIMIT_HEADROOM_PERCENT` (`specs/common/configuration.yaml`, the last one unused until the limiter itself lands); rules `MemberSignInIdentifiesOnly`/`MemberSignInGatedBeforeStore`; `sign_in`/`not_linked` screens on `specs/screens/restaurant_backoffice.yaml`; the `member-sign-in` and `graphql-limits` observability contracts (the latter's spans/metrics landed ungated, ready for the runtime extension that will call them); nine new behaviour tests. | 0 errors |
| 2026-09-05 | **A restaurant can now have a natural-person member with a login, and Captain can grant or revoke that access by hand — dark, behind a gate.** A new `RestaurantMembership` aggregate (`grantRestaurantAccess`/`revokeRestaurantAccess`, ADMIN-only, human-acted) mints one `MembershipId` per (person, restaurant scope) relationship; the handler accepts only `basis: CAPTAIN_ONBOARDING` today (the other three declared bases are refused, not yet implemented) and reserves `(MEMBER, authSubject)` in 2a's existing `auth_subject_reservations` table — no new reservation table, no release, ever. The `Member` bridge table (`auth_subject -> memberId`) is born with its own checkpoint from zero; `ScopeMembership` gains a targeted MEMBER grant/revoke pair (never the broad role-wide delete) and a documented, not-yet-fixed dual meaning on its `member_id` column. Nothing is reachable: `RUN_MEMBER_ACCESS_GRANT` defaults `false` in every environment (production pinned `"false"` behind the open `MEMBER-ACCESS-GRANT-PRECONDITIONS` decision row), the sign-in door that would let anyone use the grant is a separate, not-yet-built slice (6-ii), and production itself is suspended. #639 part C step 6-i, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md). | 0 | New scope: `specs/network/{scalars,actors,commands,events,errors,rules,api,configuration}.yaml` (`RestaurantMembership`, `MembershipId`, `MemberAuthority`, `AccessBasis`, `AccessRevocationGround`, `RUN_MEMBER_ACCESS_GRANT`); `specs/database/tables/projection_tables.yaml` new `Member` table + `ScopeMembership` `fedBy`/column notes; `specs/business_metrics.yaml` new `RestaurantMemberAccess` projection + `restaurants_with_a_bound_member` metric (spec-only, #484); `specs/stories.yaml` new `ManageRestaurantMembership` activity; `specs/tests.yaml` eight new behaviour cases (plus two new fixtures); `application::queries::BoundPrincipal::Member` (a stored-shape-adjacent write-side reservation variant — no event/table shape changed, an additive enum arm); ten DB-gated integration tests plus the six named mutants (reservation-kind collapse, simulated release-on-revoke, the broad-delete swap, TRUNCATE-for-reset, a grant-shaped column, a rebound auth_subject) each seen red and reverted. | 0 errors |
| 2026-09-05 | **A restricted rider's live socket is now closed within the push path's latency, behind a gate — the fact reaches the socket the moment it lands, not on the next request.** A RIDER connection over `/rider/graphql` now carries a connection-local standing cell the write-side guard reads FIRST (per-yield freshness, zero I/O); when the platform restricts that rider, a per-connection watcher matching the rider's OWN `RiderRestricted` fact on the in-process event bus flips the cell and pushes a readable Close (graphql-transport-ws's own `4403 RIDER_RESTRICTED`, never a bare disconnect) — closing the gap where an idle rider with a tab open kept ACTIVE standing until the socket happened to reconnect. A missed fact (the bus lagging) is re-derived ONCE from the read model; a lookup error during that re-derivation never asserts a restriction, so an infrastructure blip can never falsely disconnect a rider at peak. Ships DARK: `RUN_RIDER_RESTRICTION_SOCKET_CLOSE` defaults off in every environment (the restrict door itself is still pinned off and the rider population is zero), so nothing changes in production today; the client leg (reading the close code and routing to `/restricted` instead of reconnecting) is a separate, not-yet-built slice (#894). #639 part C step 5, [ADR-20260905-065415](adr/ADR-20260905-065415-the-restriction-fact-terminates-the-rider-s-socket-a-connection-local-standing-read-inside-the-guard-and-one-writer-to-the-transport.md). | 0 | New key `RUN_RIDER_RESTRICTION_SOCKET_CLOSE` (`specs/common/configuration.yaml`, default `false`); additive `rider-restriction` observability: `rider_restriction_socket_close_total{outcome}`, `rider_restriction_socket_close_latency_ms`, `rider_restriction_socket_close_missed_total{reason}`, `rider_restriction_socket_watch_live` (an inverted dead-man's-switch gauge, the `otp_send_guard_enforcing` precedent), and the nested INFO event `rider.restricted.socket_terminated`. `crates/server/src/graphql/rider_socket.rs` (new): the connection-local `RiderStandingCell` (monotone-tightening by TYPE — only `restrict()` exists, "send ACTIVE" is unspellable) and the watcher task. `crates/server/src/graphql/routes.rs`: the WS sink refactored to ONE forwarder task over an `mpsc::Sender<Message>` (structural, ungated, every role); the watcher/cell/close themselves gated. `crates/server/src/graphql/acl.rs`: `StandingGuard` reads the cell when present, `ReadScope` otherwise. `crates/infrastructure/src/persistence/event_bus.rs`: additive `EventBus::subscribe_with_publish_instant` (a second broadcast channel carrying the publish `Instant` — never widening `AppendedEvent`/`subscribe`'s existing return type, both consumed as plain struct literals by the fenced mailbox path). `crates/server/src/auth.rs`: `current_rider_standing(auth_subject, &RiderIdentitySource)` re-derives through the SAME identity seam `connection_init` resolved through (ADR-20260904-124600 §3's "one function, three callers"), keyed on the auth subject, never a separate roster read; a `NoMapping` result counts the new `missed{not_found}` reason, distinct from `lookup_failed`. New DB-gated suite `crates/server/tests/rider_restriction_closes_the_socket.rs` (a real `graphql-transport-ws` client over a bound `127.0.0.1:0` listener) plus a deterministic in-process unit test in `acl.rs` (the WS-level race between the watcher's close and an in-flight operation's own resolution is real and empirically un-winnable from outside — the in-process watcher reliably beats a real network round trip — so the mutant that ignores the cell is caught by the race-free unit test, not the end-to-end one). | 0 errors |
| 2026-09-05 | **A restricted rider still holding food is now observable within one sweep, with a named liveness proof.** New gauge `rider_restricted_holding_job_age_seconds` on the `rider-restriction` observability contract: the age of the OLDEST job in ASSIGNED/PICKED_UP/OUT_FOR_DELIVERY whose rider is currently RESTRICTED (through the `RiderRestriction` read model, never the internal `Rider` row), anchored on `now - effective_at`, 0 when nothing is stranded — emitted every sweep by the SAME non-fenced timer worker that already watches handed-back jobs, and certified by the SAME heartbeat (now covering two gauges, not one). No `rider_id` label; the rider and job ids ride a paired INFO event instead. A new debounce key `RIDER_RESTRICTED_CUSTODY_MAX_AGE_SECONDS` (default 1800, marked `UNVERIFIED input` — no antecedent justifies the number) gates only the info event's `threshold_exceeded` field, never a door. The bam measure `heldJobAtDecision` (the job held at the moment of restriction) did NOT land: it is a cross-stream fact the fold grammar cannot express (`RiderRestricted` carries no `deliveryJobId`, and the grammar has no join primitive) — reported to the architect rather than invented. #639 part C step 4-iii-B, [ADR-20260904-152807](adr/ADR-20260904-152807-the-admin-s-hands-one-custody-truth-read-at-query-time-a-door-that-refuses-until-the-notice-exists-and-two-slices.md) §8. | 0 | `specs/observability.yaml`: `rider-restriction` gains the gauge + threshold; one additive comment line on `custody-handback`'s heartbeat naming the second gauge it now certifies. `specs/delivery/configuration.yaml`: new key `RIDER_RESTRICTED_CUSTODY_MAX_AGE_SECONDS`. `crates/telemetry/src/contract.rs` + `meters.rs`: the constant and instrument, same commit as the spec line. `crates/infrastructure/src/integrations/delivery_handback_watch.rs`: the second gauge's set-based SQL query (`rider_restriction` as the small, unindexed build side, joined to `view_deliveryjob`'s held-status set), the per-row INFO event, and a query-duration log line (#883's cost). `crates/server/src/lib.rs`: the resolved config value wired into the worker's spawn call, mirroring `DELIVERY_OFFER_MAX_TTL_SECONDS`. `crates/infrastructure/tests/rider_custody_deadman_metric.rs`: new DB-gated suite (presence, the #870-shaped mis-ordered control, reinstatement, re-restriction, replay-identical, schema-drop). `tools/codegen-rs/src/tests.rs`: new generic codegen test asserting every single-key `thresholds[].derived_from` equals that key's declared default. | 0 errors |
| 2026-09-05 | **A control that cannot possibly work is now removed rather than shown "fixed."** Round 2 rendered two form-field-gated controls unconditionally to close the `screen-condition-on-form-field` regression, but two of them (the rider's free-text issue note, the restaurant claim screen's partial-refund amount picker) bind component kinds (`text_area`, `tip_amount_selector`) the renderer has no arm for — an "unconditionally rendered" node that paints an inert empty div is worse than no control at all. Both are now DELETED instead: a rider reporting an issue closes it by kind only (no note field, until [#888](https://github.com/TheCaptainCompany/captain-food/issues/888) gives `text_area` a renderer arm); a claim's PARTIAL_REFUND resolution has no UI amount path until the same issue gives `tip_amount_selector` one (the resolve button still dispatches for FULL_REFUND/REPLACEMENT/GOODWILL_CREDIT unaffected). The `screen-condition-on-form-field` gate now covers `specs/screens/rider.yaml` too — round 2 had scoped it off that one file to respect a concurrent-session merge fence; round 3 removed the carve-out after fixing that file's own instance (CLAUDE.md: a rule excluded by filename is a weakened gate), and also now catches a form field on the RIGHT side of a comparison, not only the leading one. Separately: the `rider_roster` migration's two indexes are now named + `IF NOT EXISTS` (matching exactly the default name Postgres already auto-assigned them), closing a defect where a migration re-run silently created a second, redundant index instead of erroring or no-op'ing as the migration's own comment claimed. #639 part C step 4-iii-A round 3 (the review ceiling round, ADR-20260826-084500), [ADR-20260904-152807](adr/ADR-20260904-152807-the-admin-s-hands-one-custody-truth-read-at-query-time-a-door-that-refuses-until-the-notice-exists-and-two-slices.md). | 0 | `specs/screens/rider.yaml`: `rider_report_sheet`'s `issue_note` (`text_area`) deleted, `report_issue_btn` no longer sends `issue`; new `gaps:` line on `job_detail` naming #888. `specs/screens/rider.translations.yaml`: `rider.issue.note_prompt` removed (orphaned). `specs/screens/restaurant_backoffice.yaml`: `claim_resolve`'s `refund_amount` (`tip_amount_selector`) deleted; its `gaps:` line rewritten to name #888 honestly instead of claiming PARTIAL_REFUND "fully wired". `specs/screens/restaurant_backoffice.translations.yaml`: three now-orphaned `back.claims.amount.*` keys removed. `specs/screens/restaurant_frontoffice.yaml`: the `recipient: RIDER` gaps line now names [#887](https://github.com/TheCaptainCompany/captain-food/issues/887). `tools/codegen-rs/src/validate/core.rs`: the `screens/rider.yaml` exclusion removed from `screen-condition-on-form-field`'s wiring at both call sites. `tools/codegen-rs/src/validate/screen_bindings.rs`: `condition_subject_root` (singular, leading-identifier-only) replaced by `condition_subject_roots` (every identifier root in the expression, string literals stripped first). `tools/codegen-rs/src/emit/sql.rs`: new `pg_index_name` helper (Postgres's own default-name shape, lowercased, panics past NAMEDATALEN rather than truncating); every emitted `CREATE INDEX` now named + `IF NOT EXISTS`, corpus-wide (61 indexes). `migrations/20260904160000_rider_roster.sql`: both indexes named + `IF NOT EXISTS`, header corrected. | 0 errors |
| 2026-09-05 | **A sentence gated on a form field the client never re-evaluates can no longer ship silently — the class is now caught mechanically, and it found three real, pre-existing instances beyond the one this round fixed.** The `RIDER_REQUESTED` procedure reminder on the restrict sheet is shown to the admin unconditionally now (it never rendered before, for any ground); the SAME defect existed, unnoticed, on a restaurant claim's partial-refund amount picker (a MONEY-PATH control that could never appear regardless of which resolution an admin picked) and a delivery-rating "why was it late" note — both are now spec-honest (no longer gated on a field that can never see anything but its own initial state), but GAP(renderer): `text_area` has no renderer arm and paints an inert empty div until one lands (round 3 correction — the round-2 text here overstated this as "both now render unconditionally", which reads as functioning; see the round-3 row below for what actually got a renderer-arm-aware fix). A reinstated rider's admin detail no longer shows their PAST restriction ground/date in the present tense; it shows "Rétabli le {date}" instead. The restrict sheet's "holds a job" sentence now says "not yet collected" for an ASSIGNED job rather than claiming it was already picked up. `reinstateRider`'s rejection (an unknown or already-active rider) now surfaces inline instead of nowhere. #639 part C step 4-iii-A round 2, [ADR-20260904-152807](adr/ADR-20260904-152807-the-admin-s-hands-one-custody-truth-read-at-query-time-a-door-that-refuses-until-the-notice-exists-and-two-slices.md) §5-7. **Left as a declared, unresolved gap, not fixed:** the post-delivery courier-tip control still hardcodes `recipient: RIDER` (ADR-20260722-181500 wants `RESTAURANT` for a self-dispatch order); reverting to the previous dynamic binding is itself dead (`Order` has no field to bind), and no self-dispatch signal is reachable from that read model today — filed as its own item for the coordinator, not decided here. | 0 | New codegen rule `screen-condition-on-form-field` (ERROR): a `condition:`/`visible_when:` whose subject is a form field declared in the same screen/sheet is refused (scoped OFF `specs/screens/rider.yaml`, fenced this round; that file carries one already-documented instance of the same class from a prior round). `specs/screens/system.yaml`: the sheet's `RIDER_REQUESTED` line moved out of its dead `condition:`; `detail_access` restructured (ground/effective-since nest under a `standing == 'RESTRICTED'` gate, a new `detail_access_reinstated` block renders `reinstatedAt`); `restrict_holds_job` gains an ASSIGNED-specific sentence; `reinstate_rider` gets an `inline_error`. `specs/screens/restaurant_backoffice.yaml`: `claim_resolve`'s amount picker and `issue_resolution_sheet`'s note field render unconditionally. `specs/screens/restaurant_frontoffice.yaml`: `rating_sheet`'s late-delivery reason field renders unconditionally. `specs/screens/system.translations.yaml`: +`roster.detail.reinstated_at_label`, +`roster.restrict.holds_job_assigned`. `crates/infrastructure/src/persistence/delivery.rs` + the emitted `riders` resolver: `held_by_rider`/`held_by_riders` now share a deterministic `requested_at DESC, delivery_job_id DESC` order, and the roster's held-set map is built first-wins instead of last-wins — before this fix, an admin could see a DIFFERENT held job on the triage row than on the rider's own detail page. `migrations/20260904160000_rider_roster.sql`: comment corrected (`CREATE INDEX` carries no `IF NOT EXISTS`, only `CREATE TABLE` does). Fleet-parity: `RUN_RIDER_RESTRICTION_DOOR` is now declared at BOTH composition roots (`declare_flag`) and pinned by `mailbox_liveness_metrics.rs`, closing a gap where a split-fleet deploy of this key was invisible. `docs/legal/rider-requested-restriction-procedure.md`: names Directive (EU) 2024/2831; its own back-reference corrected (a bottom sheet carries no `gaps:` field — the pointer lives on the `rider_detail` screen). | 0 errors |
| 2026-09-04 | **An admin can now see every rider and their access at a glance, and restrict one on a closed ground from a screen — not just through a raw GraphQL client.** The new `riders` triage (`/system/riders`) lists every rider ordered riders-holding-a-job first, then restricted, then active; the detail (`/system/riders/:riderId`) shows the held job, the access grant and its ground/date, the phone (with a call control) and availability, and carries the "Restreindre l'accès" control (a sheet: four closed grounds, no free text, no preselection) and "Lever la restriction" (a direct action, no sheet). Both screens ship DARK today — the System host is not routed and no admin sign-in door exists yet (step 6 owes both) — reachable only through GraphQL as ADMIN until then. #639 part C step 4-iii-A, [ADR-20260904-152807](adr/ADR-20260904-152807-the-admin-s-hands-one-custody-truth-read-at-query-time-a-door-that-refuses-until-the-notice-exists-and-two-slices.md) §5-6. | 0 | New table `RiderRoster` (own checkpoint group, fed by the five rider events, no `auth_ref`); new queries `riders(limit, offset)` / `rider(riderId)`, type `RiderRosterEntry`, `roles: [ADMIN]`; new screens `riders` / `rider_detail` + bottom sheet `restrict_rider_sheet` on `specs/screens/system.yaml`, strings under `roster.*` in `system.translations.yaml`; story steps `ViewRiders`/`ViewRider` under `ManageRiderStanding`. `crates/infrastructure`: `PgRiderRosterRepository`, `DeliveryReadRepository::held_by_riders` (a set-based batch narrow — the held job is read fresh at query time, never folded onto the roster, ADR §2). `crates/web/src/renderer.rs`: a standalone `{ type: badge, text:, variant: }` node now actually renders (it fell into the generic label/value catch-all before, showing nothing — found while wiring these screens, fixed for every screen using the same shape); `phone_call`'s target prop is `number:`, not `phone:` (the roster detail's own control was rendering permanently disabled). | 0 errors |
| 2026-09-04 | **The restrict door refuses in every environment until three preconditions are met, as an executable gate rather than a promise in prose.** A new configuration key, OFF everywhere today, makes `restrictRider` answer a typed, supervisable refusal instead of silently doing nothing or (worse) silently succeeding before the rider is ever told; reinstating a rider is never gated, and the guard that keeps a restricted rider's other doors closed does not consult this key either way. A codegen rule now refuses the key ever reading anything but "off" in production while its named precondition row stays open — the exact drift ("stopped" in prose, `true` in the deploy value) that slipped through undetected on an older key. #639 part C step 4-iii-A, ADR-20260904-152807 §7. | 0 | New key `RUN_RIDER_RESTRICTION_DOOR` (`specs/delivery/configuration.yaml`, default `false`, `decisionRow: RIDER-RESTRICTION-PRECONDITIONS`); new error `RiderRestrictionDoorClosed` on `RestrictRider`; new codegen rules `decision-row-open-key-must-be-off` and `screen-sheet-binding-unknown` (the latter: a screen's opened sheets now share the §25 template-binding walk — found and fixed two real dead bindings in `restaurant_frontoffice.yaml`'s `rating_sheet`, unrelated to this key, discovered by wiring the rule full-strength on the real corpus). `docs/claude/dsl.md` records both rules. | 0 errors |
| 2026-09-04 | **The paginated-list `offset` argument is now a shared kernel concept, not something only restaurant discovery understands.** No behaviour changed for any existing query; a delivery-scope list gained the same paging vocabulary a network-scope one already had, from one shared place. | 0 | `PageOffset` moved from `specs/network/scalars.yaml` to `specs/common/scalars.yaml`, beside `PageLimit` (kernel purity — a delivery→network `$ref` for a paging primitive was the wrong DAG edge); every `$ref` still resolves unchanged (kind-logical refs). | 0 errors |
| 2026-09-04 | **The `/restricted` screen's held-job facts actually render, and a rider whose access was RESTORED never reads the restricted notice.** The held-job card's restaurant name and pickup address used to render blank — the emitter only ever sub-selected a nav edge nested one level below the door query's ROOT, so `standing.heldDelivery.restaurant.displayName` was silently omitted from the fetch, and `pickupAddress`/`dropoffAddress`, being `Address` objects, fell through the currency formatter and rendered "". Both are fixed at the ROOT (the emitter now walks a nav edge nested inside a declared property; a new `format_address` filter formats an address as one line). Separately: a reinstated (ACTIVE) rider who lands on `/restricted` (back-navigation, a stale reload after being reinstated) now reads "Votre accès est rétabli." with one control back to the job list, instead of the stale "Votre accès est restreint." notice. Round 2 of the same slice's independent review. #639 part C step 4-ii round 2, [ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md) §7. | 0 | `specs/screens/rider.yaml`: the `restricted` screen's whole notice body (including the door's own header) is now wrapped in one `conditional_section` keyed on `standing.standing == 'RESTRICTED'`, with an `if_false` branch rendering the reinstated sentence + a `navigate: "/"` control; `pickupAddress`/`dropoffAddress` bindings on `job_detail` and `restricted` gain `| format_address`. `specs/screens/rider.translations.yaml`: +`rider.restricted.reinstated`. `specs/delivery/api.yaml`: a stale line-number citation dropped from a comment (names only). `specs/observability.yaml#/rider-restriction`: a comment declaring the client bounce leg RESERVED (`rider.restricted.bounced`, joinable by `correlation_id`) — no runtime change. `crates/web/src/renderer.rs`: +`format_address` filter beside `format_currency`/`format_datetime`. `tools/codegen-rs/src/emit/web.rs`: `collect_screen_nav_selections` now sub-selects a nav edge nested inside a declared property, not just first-segment edges. `docs/claude/dsl.md`: `format_address` documented. | 0 errors; warning surface unchanged |
| 2026-09-04 | **A restricted rider is now TOLD — never a bare refusal.** The next refused read or button tap bounces to a dedicated `/restricted` screen naming the fact ("Votre accès est restreint."), the observed ground in one of five counsel-reviewable sentences (or "Motif non reconnu" for an unrecognised one), BOTH the decision date and the effective date (even though V0 stamps them equal — the equal values are the honest V0 statement), and a human review route ("Vous pouvez contester cette décision… : {support address}") bound once from configuration, never hard-coded in a translation string. A rider still holding a customer's food sees the pickup address and restaurant on the SAME screen with the one control the restriction leaves them (hand it back) — no second query, no dead control. The bounce is keyed on the server's OWN signal (an additive `reason` beside the unchanged `FORBIDDEN`), never on a bare role refusal, so it fires identically whether the rider is browsing or mid-tap. #639 part C step 4-ii, [ADR-20260904-124600](adr/ADR-20260904-124600-the-restricted-rider-is-told-on-the-client-leg-first-keyed-on-the-server-s-own-reason-and-the-page-get-leg-rides-with-the-socket.md), [ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md) §7, [ADR-20260904-014136](adr/ADR-20260904-014136-rider-restriction-ships-now-with-the-smallest-closed-set-of-grounds-and-counsel-can-only-add.md) §6(ii). | 0 | `specs/delivery/api.yaml`: `RiderStandingInfo` +`contestContact` (nullable `EmailAddress`, additive, resolved from `SUPPORT_CONTACT` configuration). `specs/screens/rider.yaml`: two new screen keys `restricted:`/`while_restricted:` (the `unauthenticated:` twin, keyed on standing instead of session) on `jobs`/`job_detail`; new screen `restricted` (`roles: [RIDER]`, `while_restricted: true`, no `rider_topbar`, resolver `standing.mine -> myStanding`); new bottom sheet `rider_restricted_handback_sheet` (a second sheet bound to `standing.heldDelivery.*`, no screen-level alias grammar existing to reuse `rider_handback_sheet`). `specs/screens/rider.translations.yaml`: +24 keys (title, ground×5 split lead/trail around the bound address, both date labels, the transient sentence, the held-job card, the footer split lead/trail). DSL: two new validator rules `screen-restricted-route-unknown` / `screen-restricted-binds-uncarved-op` (a restricted-bounce target may bind only `whileRestricted:` operations for its role and can never mount `rider_topbar`, since its one action is never carved); `docs/claude/dsl.md` records both keys plus the new `format_datetime` render filter (Europe/Paris, `fr`, beside `format_currency`). `crates/infrastructure`: new `DeliveryReadRepository::held_by_rider` port (a per-paint SQL narrow, replacing `for_rider(..).find(..)`'s whole-history-plus-PENDING-pool scan for this one read, #879). `crates/server`: `StandingGuard` adds the additive `extensions.reason` (the shared `RIDER_RESTRICTED` constant, never a hand-copied string); `crates/web`: the GraphQL transport parses `extensions` into a typed shape once, at the boundary; ONE pure `bounce_after` function (new module `crates/web/src/bounce.rs`) now owns BOTH the standing bounce and the pre-existing 401 "no session" bounce, which used to live inline in `hydrate`'s `spawn_local` and had never been seen red until this change's own tests. | 0 errors; `docs/proposals/DECISIONS.md` unaffected |
| 2026-09-04 | **The restriction attribution (ground, decided-at) now disappears from a rider's own standing answer the moment an admin reinstates them — the identity row's log stays, but the RIDER no longer sees a stale reason once they are ACTIVE again.** A rider whose access is denied gets a real trace span now (not just a counter), joinable to their session; the two lag/denial telemetry notes on this feature were rewritten to say what the gauge actually reads rather than an aspirational "0 when caught up". Nothing here changes who can restrict or reinstate, or the four grounds — this is round 2 of the same slice's independent review, all NON-BLOCKING-to-the-product findings folded in before merge. #639 part C step 4-i round 2, [ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md) §2 (mechanism sentence corrected, guarantee unchanged). | 0 | `specs/delivery/errors.yaml`: +`RiderRestrictionGroundUnrecognised` (a handler-only belt against an `UNRECOGNISED` ground bypassing the door entirely — unspellable at the GraphQL door already, so `noTestFixturePossible: true` exempts it from `test-uncovered-error`, pinned by a Rust unit test instead, `tools/codegen-rs/src/validate/core.rs`'s new per-item exemption grammar). `actors.yaml#/Rider`: `RestrictRider` +`throws: RiderRestrictionGroundUnrecognised`; new validator rule `pm-emits-human-only-event` (the `emits:` complement of `pm-sends-human-only-command` — no saga inbox may declare `emits:` of an event only a human-only command's door produces). `api.yaml`: one-word correction ("restriction" not "revocation" of access, matching the rule six lines above it). `observability.yaml#/rider-restriction`: the `rider.standing.denied` span is now REAL (`crates/telemetry/src/spans.rs::rider_standing_denied`, `crates/server/src/graphql/acl.rs`) rather than a declared-but-never-constructed span backing an unreachable `technical_error` rule (removed — a restriction denial is a business outcome, never a technical error, matching `auth_scope_membership`'s own posture); `rider_standing_lag_positions`'s doc reworded to what `drain_group` actually records (a lower bound capped at `batch_size` while draining, not "0 when caught up"). `database/tables/projection_tables.yaml#/RiderRestriction`: `RiderRestriction` gets its OWN `ProjectorGroup` checkpoint (`crates/infrastructure/src/projection/worker.rs`) starting at 0, migration comment corrected ("no backfill is owed" was wrong — the backfill IS replay); +1 rebuild rule. `tests.yaml`: one stale-comment correction (both stamped dates ARE in the `riderRestricted` fixture). `docs/claude/dsl.md`: +`legacyStates:` section. | 0 errors; warning surface unchanged (the `rider-restriction` contract was silently OUT of the `obs-technical-error-unreachable` warning population before this change too — its span had no constructor at all, the "unemitted contract" carve-out — so the count does not move either way) |
| 2026-09-04 | **An admin can now restrict a rider's access on a closed, additive-only ground — and the restriction bites on the rider's very next request, with no token round-trip.** A restricted rider is refused everywhere except FOUR doors: seeing their own standing and why, tracking the job they still hold, reporting a problem on it, and handing it back — never the unassigned-jobs list (that would hand a compromised account's session the customer address book). Restriction is a platform GRANT, separate from a rider's own online/offline availability — a restricted rider may still legitimately be mid-delivery holding food. The four grounds a human admin can cite (`RIDER_REQUESTED`, `ELIGIBILITY_DOCUMENT_LAPSED`, `IDENTITY_MISMATCH`, `ACCOUNT_COMPROMISE`) are the only ones today; more can be ADDED, never repurposed. The retired `SUSPENDED` status can no longer be set by anyone — a rider's own status door now offers only OFFLINE/AVAILABLE/ON_DELIVERY, and no automated process may ever restrict a rider on its own — only a human, through the admin surface. #639 part C step 4-i, [ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md) §1–§9, [ADR-20260904-014136](adr/ADR-20260904-014136-rider-restriction-ships-now-with-the-smallest-closed-set-of-grounds-and-counsel-can-only-add.md). | 1 | `specs/delivery/scalars.yaml`: +`RiderStanding`, +`RiderRestrictionGround` (with the new `readOnlyCatchAll: UNRECOGNISED` scalar attribute — [docs/claude/dsl.md](../docs/claude/dsl.md) "readOnlyCatchAll"), +`RiderAvailabilityTarget`; `RiderStatus.SUSPENDED` retired to LEGACY/parse-only. `commands.yaml`: +`RestrictRider`, +`ReinstateRider`; `ChangeRiderStatus.status` retyped to `RiderAvailabilityTarget`. `events.yaml`: +`RiderRestricted { riderId, ground, decidedAt, effectiveAt }`, +`RiderReinstated`. `errors.yaml`: +`RiderAlreadyRestricted`, +`RiderNotRestricted`, +`RiderAccessRestricted`. `actors.yaml#/Rider`: `receives: RestrictRider`/`ReinstateRider` with `requires: acting: { ADMIN: any }` (no EXTERNAL key — the human-only door, enforced a third way by the new `pm-sends-human-only-command` validator rule); the four `→ SUSPENDED` lifecycle entry edges removed (the new `legacyStates:` lifecycle key exempts the `SUSPENDED → OFFLINE` legacy exit from the reachability gate). `rules.yaml`: +`RiderStandingIsAGrant`, +`RiderRestrictionLifecycle`, +`RestrictionTakesEffectWhenDecided`, +`RestrictRiderIsAHumanAct`, +`RiderAvailabilityNeverSpellsRestriction`. `api.yaml`: +`restrictRider`/+`reinstateRider` [ADMIN], +`myStanding` [RIDER] (a hand-written resolver — the compound `standing`/`restriction`/`heldDelivery` shape has no mechanical `reads:` fold); the new `whileRestricted:` grammar ([docs/claude/dsl.md](../docs/claude/dsl.md) "whileRestricted") carves `{ myStanding, delivery, reportDeliveryIssue, handBackDelivery }` — never `myDeliveries` (the unassigned-pool exposure). `database/tables/projection_tables.yaml#/Rider`: +`standing` column (a Complex/hand-written fold — the creating arm never writes it, preserving whatever a replay-in-place row already carries); +new table `RiderRestriction` (the Art. 11 attribution, the source of `myStanding`). `business_metrics.yaml`: +`RiderRestriction` projection (keyed on `riderId` alone — a documented keying limit, since a rider can be restricted twice and the grammar has no repeatable-fact key yet), +`restrictions_by_ground`, +`restricted_to_reinstated_duration_ms`. `observability.yaml`: `rider-identity` +`business.standing` span attribute; +`rider-restriction` contract (`rider_restricted_denied_total`, `rider_standing_lag_positions`). `stories.yaml`: rider `Deliver` +`ViewMyStanding`, admin +`ManageRiderStanding`. `tests.yaml`: +6 behaviour cases. DSL: `whileRestricted:` (3 new validator rules), `readOnlyCatchAll:` (Rust `#[serde(other)]` + tolerant `EnumText` + the SDL/mirror-enum exclusion), `legacyStates:` (lifecycle reachability exemption), `pm-sends-human-only-command`. `ReadScope::Rider` becomes a struct variant `{ id, standing }` (compiler-first: a guard that ignores standing cannot compile). Migration `20260904110000_rider_standing.sql` (metadata-only ALTER + a new table, no checkpoint rewind — a NEW event type with zero stored occurrences). | 0 errors; `obs-metric-no-emitter` UNCHANGED (both new metrics have real emitters in the same commit) |
| 2026-09-04 | **The handback's customer banner and rider sheet, corrected before first release (review round 2 on PR #870): the banner now actually renders, refreshes live, and states only facts.** The banner had never rendered at all — its predicate compared `order.status` to a token no code path ever produces, and it read a second query the live-update path never refreshed — so a customer mid-delivery whose food a rider handed back saw nothing change and, had it rendered, would have read a promise the platform does not keep ("we are reassigning the delivery" — nothing does yet). The signal now lives directly on the order the customer is already watching and updates the instant a handback lands, and the words say only what is true: the delivery will be late, the restaurant has been told, updates will appear here. Separately, the rider's own "a problem with this delivery" sheet had the same defect on the CONTROL side — a first choice (report / hand back) that the app could never act on, so the door built for exactly this looked present and did nothing. | 1 | `specs/ordering/api.yaml`: `Order` +`deliveryHandedBack` (boolean, additive). `specs/database/tables/projection_tables.yaml#/OrderTracking`: +`delivery_handed_back` column, folded by `OrderTrackingCompute` (hand-written hook) from `DeliveryHandedBackByRider`/`DeliveryAcceptedByRider`/`DeliveryAcceptedByPartner` (already in `fedBy`). `specs/screens/restaurant_frontoffice.yaml`: `order_tracking`'s `eta_bar`/`delivery_reassigning_banner` predicates re-keyed to the new flag, `data_requirements` drops the now-unused `delivery.byOrder`; translation copy replaced in both languages (facts only, no promised remedy). `specs/screens/rider.yaml`: `rider_issue_sheet` becomes a two-button router into new `rider_report_sheet`/`rider_handback_sheet` sheets (form-field `visible_when` cannot re-evaluate after a chip pick — a renderer/driver limit, not a spec defect — so the two paths are now real SDUI navigation edges instead); the handback confirm reuses the existing "tell the restaurant" label rather than a "hand back" verb no longer accurate on the WITH_RIDER card; the now-unused `rider.issue.handback_confirm` translation key removed. `specs/observability.yaml#/custody-handback`: a required span attribute nothing ever records removed; the dead-man gauge's threshold corrected to its own stated antecedent (900s, not 300s). `specs/delivery/rules.yaml#/HandBackIsNeverALever` and `specs/business_metrics.yaml#/DeliveryHandback`: one added sentence each — `riderId` is job attribution, never a `groupBy` dimension (a per-rider handback rate is a refused performance ground, ADR-20260904-014136 §3). Migration `20260904090000_ordertracking_delivery_handed_back.sql`. | 0 errors; warning surface unchanged |
| 2026-09-04 | **A rider holding a customer's paid, cooked food can now hand the job back, stating where the food is — and the read models fold it in the same slice, so the restaurant and the customer are told.** A rider who never picked up hands back to PENDING (the food never left the restaurant); a rider who collected and returns it to the kitchen hands back to PENDING too (re-offerable); a rider who still HOLDS the food hands the job back FAILED, never PENDING, so a second courier can never be offered food that is still in the first rider's bag. No free-text reason travels with it — the closed `foodLocation` is the only fact the platform records. The restaurant's delivery board pins a card headlined by where the food is; the customer's tracking page, mid-delivery, replaces the countdown with "we are reassigning the delivery" rather than either going blank or counting down to an arrival that will not happen at that pace. A dead-man's-switch gauge now watches for a handed-back job nobody has re-offered — the re-offer itself is a follow-on ([#860](https://github.com/TheCaptainCompany/captain-food/issues/860), fenced), so until it lands this gauge is the thing that notices. #639 part C step 3-ii, [ADR-20260904-015903](adr/ADR-20260904-015903-the-custody-doors-are-a-new-fact-a-rider-hands-a-job-back-with-the-food-s-whereabouts-and-the-read-models-fold-it.md) §1-3/§7-9. | 1 | `specs/delivery/scalars.yaml`: +`FoodCustody`; `commands.yaml`: +`HandBackDelivery { deliveryJobId, riderId, foodLocation }`; `events.yaml`: +`DeliveryHandedBackByRider`; `actors.yaml#/DeliveryJob`: `receives: HandBackDelivery`, three custody-keyed lifecycle rows (the `via:`+`when:` grammar extension — an event field that is NOT the aggregate's own status scalar can now drive several transitions collectively, `crates/domain/src/generated/lifecycles.rs`); `rules.yaml`: +`DeliveryHandBackKeepsCustodyHonest`, +`HandBackIsNeverALever`; `api.yaml`: +`handBackDelivery` [RIDER] `derived: { riderId: rider }`, `DeliveryJob` +`foodLocation`/+`handedBackAt`; `projection_views.yaml#/View_DeliveryJob`: `status`/`provider`/`rider_id` fold the handback (custody-keyed via the new `derive: { from, map }` grammar, `DeriveVal::Mapped`), +`food_location`/+`handed_back_at`; `tables/projection_tables.yaml#/OrderTracking`: `delivery_status`/`courier` gain the arm (hand-written `Compute` hook, unaffected by the new `derive:` form); `stories.yaml`: +1 step; `business_metrics.yaml`: +`DeliveryHandback` fold, +`handback_rate`, +`time_to_reassign_ms`; `observability.yaml`: +`custody-handback` contract (mirrors #608's dead-man shape); `screens/rider.yaml`: `rider_issue_sheet` +second exit, `job_detail` +after-state; `screens/restaurant_backoffice.yaml`: +pinned handback card; `screens/restaurant_frontoffice.yaml`: `order_tracking` +`delivery.byOrder` read, the ETA-replacing banner; +12 translation keys. DSL: lifecycle `via:` gains an optional `when:` sibling; `derive:` gains `{ from, map }` (four new validator checks, incl. exhaustiveness); a false-positive fix in `screen-status-token-unknown` (a compound `visible_when` mixing `order.status` with an unrelated field's tokens). Migration `20260904060000_delivery_job_handback.sql` (view body verbatim). | 0 errors; `obs-technical-error-unreachable` 12→13 (the new contract's spans don't yet set `otel.status_code` — the same pre-existing gap every other contract in this baseline already carries) — baseline refreshed in the same commit; `obs-metric-no-emitter` UNCHANGED (the ratchet that may not move) |
| 2026-09-04 | **A rider never states who they are — the door knows.** `acceptDelivery`, `confirmPickup`, `completeDelivery`, `declineDelivery`, `reportDeliveryIssue` and `changeRiderStatus` no longer accept a `riderId` on the wire at all: the caller's own identity is read from the seam that authenticated them (`ReadScope::Rider`) and injected server-side, so a rider physically cannot name any rider but themselves — closing the shape that let a bare RIDER token author an acceptance in anyone's name (the finding `rider_without_a_row_is_forbidden_on_the_write_half.rs` records). `changeRiderStatus` is now the RIDER's own online/offline toggle and nothing else — a rider's LIFECYCLE (restriction/reinstatement) is ADMIN's through `RestrictRider` instead (ADR-20260904-014136 §Decision 6(i)), a different door with a different actor. An ADMIN-filed delivery issue report carries no rider at all (`riderId: null`, "ops reported") — the envelope's `user_id` says who acted, never the payload. This is the recorded final vision (PROP-171500 D2); the input-field deletions are breaking-but-free only because no client has shipped against the old shape yet (production is suspended, ADR-20260817-105844) — they must land before first deploy, which is why they land now rather than as a later migration. | 0 | `specs/delivery/api.yaml`: six mutations gain `derived: { riderId: rider }`; `changeRiderStatus.roles` narrows `[RIDER, ADMIN]` → `[RIDER]`. DSL: api.yaml mutations gain the `derived:` key (`api-operation-key`'s closed `MUTATION_KEYS` set); three new ERROR rules `api-derived-field-unknown` / `api-derived-type-mismatch` / `api-derived-role-mismatch`; `action-missing-required-input` subtracts derived keys from what a screen action must supply. Generated: the six `<Command>Input` types lose their `riderId` field (SDL + the server InputObject), each gaining a description stating the id is derived; the resolver template gains ONE injection block per `derived:` property, between `command_payload` and the typed deserialize, reading `ReadScope` and failing closed (`errors.yaml#/Forbidden`) when a REQUIRED derived property's source scope did not resolve. `specs/screens/rider.yaml`: `accept_delivery`/`confirm_pickup`/`complete_delivery` rebound from the unknown `orderId` to `deliveryJobId` (the previously-missing required input); the stale "no rider-identity root to bind riderId from" comment is closed, not just removed. `specs/tests.yaml`: +`TestConfirmPickupByAnotherRiderIsRejected`, +`TestCompleteDeliveryByAnotherRiderIsRejected` (D1: the aggregate-side compare at `commands.rs` stays the authority where a rider is already assigned; the seam is the only guard where none is, e.g. PENDING jobs). #865, [ADR-20260904-015903](adr/ADR-20260904-015903-the-custody-doors-are-a-new-fact-a-rider-hands-a-job-back-with-the-food-s-whereabouts-and-the-read-models-fold-it.md) §6 (the closed operation-key seam), PROP-171500 D2 (ADR-20260808-171056). The `ReadScope::Rider(RiderId)` fact this derives from is [#849 "#639 part C step 2b: the auth_ref -> rider_id resolver at the request seam"](https://github.com/TheCaptainCompany/captain-food/pull/849) / [ADR-20260830-191457](adr/ADR-20260830-191457-a-role-guard-takes-a-witness-and-an-unbound-caller-is-recorded-as-public.md) parts A+B, realized in `crates/server/src/auth.rs::resolve_rider_scope` — the dispatch card's citation of "ADR-20260904-014135" for that fact was a card defect (that ADR is the one-subject-several-roles record, a different founder decision the same day), corrected in review round 2. | 0 errors; `action-missing-required-input` 11→7, `action-unknown-input` 7→4 (both narrowed by the screen rebinding + the derived-key exclusion) — baseline refreshed in the same commit |
| 2026-09-04 | **A rider who hits a problem on a delivery can now say so from the job, by kind, and the restaurant is told through its board — a report is a closed reason (address not found, customer unreachable, order not ready, food damaged, vehicle or injury, other) plus an optional 300-character note prompted "the facts, without describing anyone"; the job's status never moves; whoever was told (the restaurant, never the reporter) closes it with a closed outcome; a rider can refuse a pending offer; both notes are the customer's data inside the order's stream and die with the order's tombstone.** Under it: the stored `DeliveryIssueReported` gains a nullable `kind` and `DeliveryIssueResolved` a nullable `resolution` kind + `note` (additive, readers first — rows appended before carry none and read NULL); the delivery board row gains `openIssue`; and the `derive:` grammar gains an explicit `null` (an event that CLEARS a folded column — a YAML null used to fall out of the fold silently). #639 part C step 3-i, [ADR-20260904-015903](adr/ADR-20260904-015903-the-custody-doors-are-a-new-fact-a-rider-hands-a-job-back-with-the-food-s-whereabouts-and-the-read-models-fold-it.md) §4–§6. | 1 | `specs/delivery/scalars.yaml`: +`DeliveryIssueKind`, +`DeliveryIssueResolution`; `commands.yaml`: `ReportDeliveryIssue` +`kind` (required), `issue` optional ≤300; `ResolveDeliveryIssue.resolution` → the kind, +`note` ≤300; `events.yaml`: `DeliveryIssueReported` +`kind` nullable, `issue` nullable; `DeliveryIssueResolved.resolution` → nullable kind, +`note`; `api.yaml`: +`reportDeliveryIssue` [RIDER, ADMIN], +`resolveDeliveryIssue` [RESTAURANT, RESTAURANT_ACCOUNT, ADMIN], +`declineDelivery` [RIDER], `DeliveryJob` +`openIssue`; `projection_views.yaml#/View_DeliveryJob`: +`open_issue_kind` (`derive: { Reported: {from: kind}, Resolved: null }`), both facts in `fedBy`; `rules.yaml#/DeliveryIssueLifecycle` sharpened; `tests.yaml`: 2 cases extended, +3 negatives; `stories.yaml`: +3 steps; `business_metrics.yaml`: +`DeliveryIssue` fold, +`delivery_issue_rate`; `screens/rider.yaml`: +`rider_issue_sheet`, `job_detail` +[Un problème], `jobs` +[Refuser]; `screens/restaurant_backoffice.yaml`: +pinned issue card, +`issue_resolution_sheet`; +33 translation keys. DSL: `derive:` accepts `null`; validator +`api-operation-key`, +`view-derive-value-unknown`, +`view-derive-null-not-nullable`. Migration `20260904021500_delivery_job_open_issue.sql` (view body verbatim from the generated SQL). | 0 errors; `action-missing-required-input` 10→11 (`declineDelivery` needs `riderId`, which the rider surface has no identity root to bind — the class accept/confirm/complete already sit in), `command-no-mutation` 11→8, `event-not-projected` 6→4 — baseline refreshed in the same commit |
| 2026-09-03 | **A rider can now sign in from the rider app itself — the door step 2c-i built is reachable from a screen, and a refusal is read in French, on the screen, naming the support address.** The rider app gains a `/sign-in` screen: prefilled `+33`, the phone number, one button, then the six-digit code; a rider with no session who opens the job list is sent to that screen (by the server before any render, by the app on a refused read), instead of a blank list. Under it, ONE new screen capability (R1, the founder's FORK 3 choice): a screen may declare `graphql_role` and speak to another role's graph — the door speaks to the anonymous one — and the validator refuses the declaration unless every control on that screen would actually work under that role (a control that renders and does nothing is now an ERROR for a declared transport role, and a WARNING for the general audience case, which exposed two pre-existing dead controls: the back office's escalate button for RESTAURANT_ACCOUNT viewers, the storefront's favourite button for anonymous visitors). The refusal sentences are the catalogue's own French text, interpolated by the server — the support address comes from configuration, never from a screen. | 0 | `specs/screens/rider.yaml`: +`sign_in` screen (`graphql_role: PUBLIC`), +`rider_code_sheet`, +actions `request_rider_sign_in_code` / `confirm_rider_sign_in`, `jobs`/`job_detail` +`unauthenticated`; `specs/screens/rider.translations.yaml`: +10 `rider.sign_in.*` keys; `specs/stories.yaml`: `SignInAsRider` names its screen in prose (no story→screen grammar exists). DSL: +`graphql_role`, +`unauthenticated`, +`inline_error.for_action` (validator §26). No GraphQL shape change (`Operation.message` is the same nullable string, now localized); no SQL, no stored shape. | 0 errors; +2 `screen-role-refused-operation` (new WARNING kind, the two pre-existing dead controls — baseline refreshed in the same commit) |
| 2026-09-03 | **A rider sign-in confirmation that arrives with no browser session is now refused before the code is spent — the credential the door hands out always has an owner.** The credential is released only to the session that asked for it; a confirmation carrying no session used to park a credential with no owner, which anyone holding the acceptance id could have collected. Now it is refused outright, the one-time code is not consumed (a correct retry still works), and nothing is stamped or parked. No rider using the app can see this: the app always sends its session; the refusal exists for callers that do not. | 0 | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 2c-i / PR [#852](https://github.com/TheCaptainCompany/captain-food/pull/852) review round 1, finding B1 (`HOLD: human`). `specs/delivery/errors.yaml`: +`RiderSignInRequiresSession` (context-free); `specs/delivery/actors.yaml`: on `ConfirmRiderSignIn`'s `throws`; `specs/delivery/rules.yaml`: `RiderSignInIdentifiesOnly` gains the owner clause; `specs/tests.yaml`: +`TestRiderConfirmSignInWithoutSessionIsRejected`, and `TestRiderConfirmSignInIdentifies` renamed to what `then: []` proves (identify-only, nothing appended — the stamp and the parked session are port effects the transport test carries). Purely additive GraphQL (one error code); no SQL, no stored shape. | 0 errors; warning surface unmoved |
| 2026-09-03 | **A rider can now be issued the credential that walks through the door step 2b built — and ONLY a rider: a phone nobody registered as a rider is refused and creates nothing.** Two anonymous operations on the rider's phone: ask for a code, confirm it. The confirmation identifies the login in OUR rider table and never registers — the whole reason the customer's register-or-identify door was not reused with a wider role list — then stamps a credential that says RIDER and **nothing else** (no id: the rider's identity keeps resolving from our database on every request, so a revocation is immediate and there is no cache to invalidate) and hands the session over the existing cookie path, never in the API response. Asking for a code answers identically whether or not the phone is a rider's, and never looks in the rider table, so the door leaks nothing about who is a rider. A refused sign-in names the support route, which is now a **declared, required configuration key with no default** (`SUPPORT_CONTACT`, baked `support@captain.food` — the founder's 2026-08-31 answer) rather than a recorded design. **One collision is registered, not decided**: a login that already carries a customer claim is refused rather than overwritten (the provider replaces the whole claim object) — a rider who also orders dinner stays unrepresentable until the founder decides (`one-subject-one-role` Concern). **Structural, no behaviour change**: the OTP vocabulary (three scalars, five refusals) moves to the kernel because two doors now throw it. **Still not usable from the rider app**: the sign-in screen and the per-screen PUBLIC capability are step 2c-ii. | 0 | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C **step 2c-i** / PR [#852](https://github.com/TheCaptainCompany/captain-food/pull/852) (`HOLD: human`), [PROP-20260831-180622](proposals/PROP-20260831-180622-staff-authentication-the-roster-the-invitation-and-the-door.md) build-order row **2c** (inserted; rows 3 and 4 now depend on it) + Concern `one-subject-one-role`. `specs/services.yaml`: +`identity.stamp_rider_claim` (input `authRef: AuthSubject`, throws `AuthSubjectHoldsAnotherRole`). `specs/delivery/commands.yaml`: +`RequestRiderSignInCode`, +`ConfirmRiderSignIn`; `specs/delivery/actors.yaml`: both on the `Rider` inbox, `emits: []`; `specs/delivery/api.yaml`: +`requestRiderSignInCode`, +`confirmRiderSignIn` (`roles: [PUBLIC]`, with the ADR-20260818-101500 justification); `specs/delivery/errors.yaml`: +`RiderNotRegistered` (context `supportContact`), +`AuthSubjectHoldsAnotherRole` (context `authRef`); `specs/delivery/rules.yaml`: +`RiderSignInIdentifiesOnly`; `specs/tests.yaml`: +6 cases + fixture `riderRegisteredOnCustomerLogin`; `specs/stories.yaml`: `public_user.SignInAsRider` (the RIDER persona cannot hold a `[PUBLIC]` step — validator `story-role-not-authorized`); `specs/common/configuration.yaml`: +`SUPPORT_CONTACT`; `specs/observability.yaml`: `rider-identity` +`rider_claim_stamp_failed_total{reason}`; **kernel promotion** (validator `scope-placement-error` + kernel purity): `DialingCode`/`NationalPhoneNumber`/`OtpCode` → `specs/common/scalars.yaml`, `PhoneCountryNotServed`/`VerificationSendLimitReached`/`VerificationSendCapacityExhausted`/`InvalidVerificationCode`/`VerificationCodeExpired` → `specs/common/errors.yaml` (refs are kind-logical: zero ref rewrites, zero SQL/GraphQL/stored-shape change). Purely additive GraphQL; no migration. | 0 errors; `identity-property-not-on-command` 1 → 3 **accepted** (the two sign-in commands cannot carry a `riderId` — the caller does not know it yet, exactly as `RequestPhoneVerification` carries no `customerId`; baseline refreshed in the same commit) |
| 2026-09-03 | **A rider is whoever OUR database says the login belongs to — never whoever the token says, and never anybody when the database has no row.** The RIDER role now has a sign-in door at the request seam: on every GraphQL request and every WebSocket connect, the verified login subject is looked up in the `Rider` read model part A landed (`auth_ref -> rider_id`, one probe on its UNIQUE index) and the request is scoped to THAT rider. A token claiming a rider id is structurally unread — the product parses no such claim any more — so a stale, forged or mis-stamped id cannot widen a rider's view; and a login the projector has not caught up on, or that a registration refusal never bound, is `Public` (nobody), not the claim. The seam is ungated on purpose: unlike the customer seam there was no working claim path for an OFF switch to preserve. Its outcomes are observable on their own `rider-identity` contract (a missing row is OBSERVE, a seam that cannot answer is PAGE) so a paging rule on the customer seam cannot go quiet while riders fail. **End-to-end, a rider still cannot sign in**: nothing mints a `role: RIDER` token yet (the sole claim stamper hardcodes CUSTOMER) — the door is built; the token that walks through it is the next step's. **Corrected in the re-presentation of #849 (same day)**: as first pushed, 2b regressed part B for RIDER — a `role: RIDER` login the database did NOT know could still ACT as a rider on every rider mutation (accept, pick up and complete a delivery, change status, mark an order delivered) and was recorded as one, while reading as nobody; the confirming re-presentation restores the promise on both halves — a login the database does not know is nobody to read AND nobody to act, and is recorded as nobody. | 0 | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C **step 2b** / PR [#849](https://github.com/TheCaptainCompany/captain-food/pull/849) (`HOLD: human`), [PROP-20260831-180622](proposals/PROP-20260831-180622-staff-authentication-the-roster-the-invitation-and-the-door.md) build-order row 2 now LANDED. `specs/observability.yaml`: +`rider-identity` (span `rider.identity.resolve`; `rider_identity_resolve_ms{result}`, `rider_identity_not_found_total`, `rider_identity_lookup_failed_total{reason}`, `rider_identity_lookup_source_total{source}` — the customer seam's shape under its own names). `specs/database/tables/projection_tables.yaml`: the `Rider` table's `internal:` note no longer says "written and not yet read" and names its reader. `specs/customer/configuration.yaml`: `RESOLVE_CUSTOMER_IDENTITY_FROM_POSTGRES`'s prose no longer claims RIDER has no seam. No stored event shape moves, no table, no migration, no GraphQL change. Code: `Identity::Rider { sub }` (no id field, and minted ONLY by the seam's `Resolved` outcome — the verifier yields `Identity::Unbound` for every rider token, so the write-half witness cannot be RIDER before Postgres answered), `ProductClaims` without `rider_id`, `RiderIdentityRepository` port + `PgRiderRepository`, `IdentitySources` at the seam; the §10 pair and its WS mirror; `role_injection_gate` walks `tests/` recursively; the duplicate-`authRef` classification test under the default `DbFaultPolicy::Skip`. | 0 errors; warning ratchet exact match (surface unchanged) |
| 2026-09-03 | **A rider's login can now belong to exactly ONE rider account, and the platform decides that BEFORE it records the registration — not after, in a projector that can only get stuck.** Until now nothing on the write side stopped two `RiderRegistered` facts carrying the same login credential under two different rider ids: the `Rider` read model's `auth_ref UNIQUE` would refuse the second row, but only after the fact was already in the immutable log — a stuck projection, not a rejection, and a rider whose grants (`ScopeMembership` keys on `member_id = rider_id`) could land on the wrong rider. A registration whose credential is already bound to another rider is now refused up front with a typed, translated `RiderAuthSubjectAlreadyBound` (*"This login is already linked to a rider account. Sign in to that account instead."*). Two properties are deliberate and stronger than the storefront-slug sibling: **the binding is keyed by (kind of principal, credential), never the credential alone** — a rider who is also a customer keeps both, and nothing bars a rider from later becoming a restaurant member — and **there is no release path at all**: suspending or revoking a rider never frees the login, so the same human always resolves to the same rider id and never orphans their delivery history. **No rider can sign in yet** — this is the invariant the sign-in door (step 2b) is built on, landing before it. | 0 | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C **step 2a** / [#794](https://github.com/TheCaptainCompany/captain-food/issues/794) / PR [#846](https://github.com/TheCaptainCompany/captain-food/pull/846), the `slug_reservations` copy job of [ADR-20260728-011344](adr/ADR-20260728-011344-slug-lifecycle-and-sirene-inbound-events.md). `specs/database/tables/reservations.yaml`: +`auth_subject_reservations` (composite PK `(principal_kind, auth_subject)` typed by `PrincipalKind` + `AuthSubject`, `principal_id` typed by the kind as `ScopeMembership.member_id` is, `reserved_at`, NO `released_at`, retention NONE). `specs/delivery/errors.yaml`: +`RiderAuthSubjectAlreadyBound` (context `authRef`, en/fr); wired into `RegisterRider`'s `throws` (`specs/delivery/actors.yaml`). `specs/delivery/rules.yaml`: +`RiderAuthSubjectBoundOnce`; `specs/tests.yaml`: +`TestRiderAuthSubjectAlreadyBoundIsRejected` (the fake port is seeded with a foreign holder under `"already-bound"`, the `SpecSlugReservations` sentinel convention, so the case is not a tautology). `specs/database/databases.yaml`: the `captain_write` prose names both reservations. Emitter: `emit/sql.rs` learns a COMPOSITE primary key (two `pk: true` columns → one table-level `PRIMARY KEY (a, b)`; single-column keys keep the inline form; the ONE other table that moves is `mailbox_partitions`, whose old artifact carried two inline `PRIMARY KEY`s — DDL Postgres refuses — while `migrations/20260731063000_actor_mailbox_tables.sql:93` always had `PRIMARY KEY (actor_type, partition)`, so the emitter change repairs a latent generator bug and owes no migration). Two records this change falsified are corrected in the same commit: `PrincipalKind`'s description now names the reservation key by the column that exists, `(principal_kind, auth_subject)`, and the `Rider` table's rule no longer calls the write-side reservation "designed but unbuilt" but points at `auth_subject_reservations` and `RiderAuthSubjectBoundOnce`. New table, new migration `20260903060000`; no stored event shape moves, no GraphQL change (the error is a mailbox verdict, not a schema type). Build-order row 2 of [PROP-20260831-180622](proposals/PROP-20260831-180622-staff-authentication-the-roster-the-invitation-and-the-door.md) is NOT yet landed — 2b (the resolver and the door) follows. | 0 errors, 96 warnings (unchanged) |
| 2026-08-31 | **The platform can now say, in its own vocabulary, that a PERSON is holding the tablet — and it no longer describes a human being's login with the same word it uses for a pizza's import code.** Two gaps closed at once. First, `RESTAURANT` was a *place* standing in for whoever happened to be logged in, so a restaurant could not have staff: no colleague could be invited, none could be given or lose access, and nothing recorded which human accepted an order. There is now a name for that person (`MemberId`) and a name for what kind of party is acting (`PrincipalKind`, which is where `MEMBER` lives) — the vocabulary the invitation and the roster in [PROP-20260831-180622](proposals/PROP-20260831-180622-staff-authentication-the-roster-the-invitation-and-the-door.md) are built from. Second, the identifier proving *who someone is to the login provider* was typed as `ExternalReference` — the scalar the kernel defines as the **HubRise catalog reference**, whose own examples are `'MARGHERITA'` and `'CAT-PIZZAS'`. A customer's credential and a menu item's import key were literally interchangeable, in the shared kernel, so nothing could stop one being passed where the other was meant. They are now different types and the compiler refuses the swap. **Nothing a customer or restaurant can see changes today, and nothing stored moves**: this is the vocabulary staff sign-in will be built on, landing before it rather than during it. | 0 | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C **step 1 of seven** / PR [#835](https://github.com/TheCaptainCompany/captain-food/pull/835), [ADR-20260831-220559](adr/ADR-20260831-220559-the-person-is-a-principalkind-not-an-eighth-usertype.md) — which also **closes the [PRINCIPALS-MEMBER](decisions/PRINCIPALS-MEMBER.yaml) register row** that gated this step. `specs/common/scalars.yaml`: +`AuthSubject`, +`MemberId`, +`PrincipalKind` (`CUSTOMER|RESTAURANT|RESTAURANT_ACCOUNT|RIDER|MEMBER`). **`UserType` is NOT widened** — verified mechanical reason, not preference: `tools/codegen-rs/src/emit/bins.rs`'s `user_type_roles` reads that enum and emits one gateway bin and one `/{path}/graphql` route per member, so an eighth value would conjure an eighth URL surface out of naming a person; and it is stored on every `domain_events` row (ADR-0041), so `RESTAURANT` stays a legal value forever and is never renamed. **`ScopeType` untouched.** Seven `authRef` sites retyped `ExternalReference` -> `AuthSubject` (`specs/customer/events.yaml` x2, `specs/delivery/events.yaml`, `specs/delivery/commands.yaml`, and `specs/services.yaml` x3 — the last three feed the first four, so the retype does not type-check without them). **Zero-byte diff** in `schema.generated.sql`, `views.generated.sql`, `security*.generated.sql` and `databases.generated.json`: `type: string` emits `TEXT` regardless of the scalar's name, so no DDL, no migration, no backfill, no replay. The GraphQL diff is **purely additive** (two scalars + one enum declared; **no field retyped**, because no API field binds `authRef`). No event payload shape, no command input, no stored JSON. `requires.acting`'s semantics are stated, not built — zero `requires:` blocks exist in any `specs/*/api.yaml`, and #636 owns the emitter. | 0 errors, 96 warnings (unchanged) |
| 2026-08-31 | **The button a customer presses to place an order now states that they are undertaking to PAY, not merely how much the food costs — and the recap of that total is no longer something they might never open.** The order button read *"Commander — 23,50 €"*: an amount. French distance-selling law (C. conso. **L221-14**, transposing **CRD 2011/83 Art. 8(2)**) requires the button immediately before ordering to carry an unambiguous mention of the **obligation to pay**, alongside a clear and legible recap of the total — and its sanction is not a fine but that **the consumer is not bound by the contract**, i.e. an order we shipped and paid a rider for is voidable. The French button now carries the statutory safe-harbour formula *"Commander avec obligation de paiement"* and the English one the directive's own *"Order with obligation to pay"*, both keeping the total; and the checkout order-summary section is no longer declared collapsible, so the total is visible without the customer having to expand anything. **This is over-compliance pending an answer, not a legal conclusion.** That the requirement exists is not in doubt; whether any particular wording or presentation *satisfies* it is **QT-4** on the counsel list ([docs/legal/BRIEF-20260831-repricing-and-price-quote-counsel-packet.md](legal/BRIEF-20260831-repricing-and-price-quote-counsel-packet.md)) and no counsel is engaged. Adopting the safe-harbour wording costs nothing, so it is the right thing to ship while the question is open — but it does **not** close it, and one detail counsel now has to weigh exists only because of this change: the shipped label is the formula **followed by the total** (`Commander avec obligation de paiement — 23,50 EUR`), and Art. 8(2)'s second subparagraph speaks of labelling the button *"only with the words … or a corresponding unambiguous formulation"*. Keeping the total on the control is a design choice, not a reading of the article. This repo issues no clearance ([ADR-20260812-143619](adr/ADR-20260812-143619-the-founder-is-the-founder-and-every-founder-message-goes-to-the-whole-team.md)). | 0 | [#817](https://github.com/TheCaptainCompany/captain-food/issues/817). `specs/screens/restaurant_frontoffice.translations.yaml` `checkout.place_order`: both `en` and `fr` messages rewritten (the key, its `params` and every call site are unchanged). `specs/screens/restaurant_frontoffice.yaml` checkout `order_summary` section: `collapsible: true` removed — nothing in the codegen or the renderer ever read that flag and the hand-written runtime always rendered the section expanded, so this ends a declaration that disagreed with its runtime. Explicitly NOT touched, per [PROP-20260831-134539](proposals/PROP-20260831-134539-priced-quote-token.md) §4 UC4 (#817 is *referenced, not absorbed*): the reprice disclosure UI, `CheckoutRepriced`, the `PriceMismatch` copy, the quote token and `PlaceOrder`'s input — all slice 5. No key added or removed, no event payload, no command, no GraphQL schema, no stored shape and no migration; the whole runtime footprint is the regenerated translation catalog and the checkout button that now resolves its label from it. | 0 errors, 96 warnings (unchanged) |
| 2026-08-31 | **A dish the kitchen has run out of can no longer be bought.** Until now the spec promised the availability-and-stock check only where a customer EDITS their cart; checkout re-priced the cart but never re-asked whether the food could still be made. The gap is the ordinary Friday-night one: a customer adds a dish at 19:50, the kitchen 86's it at 20:20, the customer pays at 20:40 — and the order was **accepted**, because re-pricing only fails on a dish that has left the menu entirely, not on one that is merely marked unavailable or has one portion left against two in the cart. The money moved and the ticket landed on a pass that could not fill it, which loses the customer and the restaurant in the same order. The spec now says orderability is **re-derived at the moment of payment** — availability, tracked stock covering the quantity, and a still-valid option selection — and says where in the sequence that happens: strictly **before** the card is touched, so a refusal can never leave a real Stripe hold or a spent goodwill credit behind. The promise has an explicit floor written into it too: **a restaurant that does not count portions is never blocked** — untracked stock is not zero stock — because a guard that failed that way would refuse every order for most of the Tours estate. | 0 | [#823](https://github.com/TheCaptainCompany/captain-food/issues/823) / PR [#824](https://github.com/TheCaptainCompany/captain-food/pull/824). **Slice 1 of [PROP-20260831-134539](proposals/PROP-20260831-134539-priced-quote-token.md) §11** (approved by the founder 2026-08-31, recorded in [ADR-20260831-165146](adr/ADR-20260831-165146-the-quote-backstop-is-thirty-minutes-and-the-priced-quote-token-is-approved.md) — the recorded approval covering this `specs/**` edit; the only slice outside `HOLD: human`), discharging its §3 F1 finding. `specs/ordering/rules.yaml#/CheckoutPricesCartCreatesPaymentIntent`: states the re-check, its position before the payment call, and the untracked-is-not-zero floor. `specs/ordering/processmanager.yaml`: `PlaceOrderProcess` gains three declared guards (`OfferUnavailable`, `InsufficientStock`, `InvalidOptionSelection`) between the existing `PriceUnresolvable` and `PriceMismatch` guards. `specs/tests.yaml`: +2 fixtures, +3 behaviour tests — the two rejections **and** the untracked acceptance floor. **No new error declared** (both codes already existed with en/fr messages and an existing checkout toast), no event payload, no command input, no GraphQL schema and no stored shape change; `specs/ordering/rules.yaml#/ServerPriceAuthority`'s `expectedTotal` sentence is deliberately untouched — §11 puts it in slice 4. Warning surface unmoved, no baseline refresh. | 0 errors, 96 warnings (unchanged) |
| 2026-08-31 | **Every saga step that makes one aggregate ask another to do something now names, in the spec, the switch that turns THAT hand-over off and the key its door treats as "the same request".** Four such steps existed with neither: the customer bind, the goodwill-credit grant, and the two ways a delivery is reported complete. Nothing changes in behaviour yet — all three switches ship OFF and the existing in-process code is preserved byte-for-byte — but the spec now promises two things it could not before. **A customer's second goodwill credit can never be silently swallowed**: the credit door is keyed per RECLAMATION, not per customer, where inheriting the target's own identity would have keyed it on the ledger a customer holds for life and dropped every credit after the first — money owed, never paid, no error raised anywhere. And **the delivery-completion switch now states its retention consequence out loud**: today an order completed by a partner's DELIVERED report or by an independent rider's completion starts NO order-retention clock, while the same order closed through the `markOrderDelivered` mutation does; turning that route on puts both on the same footing as the mutation. That is written into the switch's own text because the decision to flip it is made from that text. | 0 | [#807](https://github.com/TheCaptainCompany/captain-food/issues/807) / PR [#808](https://github.com/TheCaptainCompany/captain-food/pull/808), [ADR-20260831-093000](adr/ADR-20260831-093000-the-enumeration-is-deliver-and-send-not-deliver-alone.md). `specs/{ordering,delivery}/processmanager.yaml`: the four `send:` steps gain `to:` / `route_gate:` / `dedup_by:` (all `$ref`s). `specs/common/configuration.yaml`: +3 bool keys, all `default: false` — route gates are KERNEL config because every PM bin's composition root constructs the whole `RouteGates` struct — and `ROUTE_ORDER_DELIVERY_COMPLETION_THROUGH_LANE.gates` enumerates the `OrderExpired` retention clock as consequence (e). `specs/common/processmanager.yaml` documents both keys in the kernel. New validator rules `pm-send-dedup` (no default: the safe axis does not follow from the target) and `pm-route-gate` widened to `send:`. No event payload, stream or stored shape change; legacy arms byte-for-byte (`git diff -w` on the regenerated `process_managers.rs`: zero deletions). | 0 errors, warnings unchanged |
| 2026-08-30 | **An operator reading why the SIRENE ingestion is switched off is no longer told a reason that stopped being true a month ago.** `RUN_SIRENE_WORKER`'s own text said the drain was *"PAUSED since 2026-07-28 with the CI sweep (issue #220)"*. [#220](https://github.com/TheCaptainCompany/captain-food/issues/220) closed the same day it was written, and so did the other named bottleneck [#218](https://github.com/TheCaptainCompany/captain-food/issues/218) — so the chain has been off for over a month behind a blocker that no longer exists, and nobody re-took the decision to restart it. Found while checking the premise of the 2026-08-30 STAFF-AUTH answer, whose claim-your-listing journey needs listings to claim. The text now says what is actually true, and names the two things that must land BEFORE a restart rather than after: an owner-declared restaurant carries no SIRET — the crawl's idempotency key — so un-pausing first manufactures duplicate restaurants, and a self-declared listing currently enters the prospect funnel and is eligible to be cold-emailed. **The flag itself is untouched**: restarting is a decision with real costs, recorded on [#800](https://github.com/TheCaptainCompany/captain-food/issues/800), not a doc fix. | 0 | `specs/network/configuration.yaml` `RUN_SIRENE_WORKER.gates` rewritten; `docs/STATUS.md` rows 66–67 corrected. Prose only — no key, default, payload or schema change; the 8 regenerated `config.rs` doc comments are the whole runtime footprint. | 0 errors |
| 2026-08-30 | **A saga step that hands a message to another aggregate's lane now names, in the spec, the single switch that turns THAT hand-over off — and only that one.** Two such hand-overs exist today (the Order birth after payment, the replacement order after a claim is resolved) and they already had a switch each, but the code behind them read "is there a lane at all?", so both were really driven by whichever switch the process that ran them happened to consult. Adding a third would have silently bound it to one of the existing two: turning off a misbehaving hand-over would have turned off an unrelated one at the same time, which is a second, unintended change made under incident pressure. The switch is now declared on the step itself, so each hand-over is turned off on its own, and a new one that names no switch does not build. Neither existing hand-over changes position. | 0 | [#797](https://github.com/TheCaptainCompany/captain-food/issues/797) | 0 errors, 99 warnings |
| 2026-08-30 | **Seven inbound facts that an aggregate declares it receives now say OUT LOUD, in the spec, that the aggregate does not model them yet — and a fact that reaches the door with nowhere to go is no longer destroyed.** Until now the mailbox's fact route ended in a catch-all: twelve declared facts fell into it and would have been recorded as terminally FAILED, invisible to the operator's poison queue and refused by the requeue command — for `RefundOpened`, which is the SOLE source of the restaurant's pending-refund queue, that means the restaurant is never asked to decide and **captured money stays captured**. The route is now exhaustive by construction (a declared fact with no arm is a compile error), five more Payment facts are genuinely recorded — including `RefundOpened` itself, which review found did not record at all until the money lane's record leg carried its TYPED fact instead of a widened one — and the remaining seven carry a `deferred:` block naming what the MODEL is missing — in every case the aggregate's own "have I already seen this?" rule, without which a re-delivered fact would be recorded twice. Those seven PARK instead of failing: the row stays deliverable and reaches the operator's existing poison queue and recovery, because a fact already happened somewhere else and this system has no authority to refuse it ([ADR-20260830-224500](adr/ADR-20260830-224500-an-unrecorded-declared-fact-parks-it-is-never-terminally-failed.md)). **The mailbox delivery door also becomes observable for the first time** — it had no span at all, and the status bus ignores non-COMMAND rows, so a lost fact was a zero-signal event. | 0 | [#780](https://github.com/TheCaptainCompany/captain-food/issues/780) / PR [#783](https://github.com/TheCaptainCompany/captain-food/pull/783). `specs/{ordering,catalog,customer,delivery}/actors.yaml`: **7 `deferred:` blocks** — the first users of the grammar #771 landed empty ([#784](https://github.com/TheCaptainCompany/captain-food/issues/784) Cart, [#785](https://github.com/TheCaptainCompany/captain-food/issues/785) Catalog, [#786](https://github.com/TheCaptainCompany/captain-food/issues/786) Customer, [#787](https://github.com/TheCaptainCompany/captain-food/issues/787) DeliveryJob). `specs/observability.yaml`: NEW `mailbox-delivery` contract (span `message.deliver`, metric `mailbox_fact_unrecorded_total{actor_type, message_type, reason}` with `reason` a declared closed set) bound to a NEW dispatch surface `mailbox` — `graphql` would have been a false binding for a webhook-sourced fact. No event payload, no command, no schema and no stored shape change; `inbound_messages` untouched. | 0 errors, warnings unchanged (99) |
| 2026-08-30 | **A Captain rider now has an identity the platform can resolve — the last missing half of "who is this person" for the RIDER role.** `RiderRegistered` has always carried the auth-provider subject (`authRef`), and nothing ever stored it, so a rider could be registered and still be unrecognisable at sign-in: `auth_ref` appeared exactly ONCE in the entire projection set, on Customer. There is now a `Rider` read model holding the `auth_ref -> riderId` mapping [ADR-20260818-004646](adr/ADR-20260818-004646-no-business-identifier-lives-in-the-identity-provider.md) requires to live in our own Postgres rather than in the identity provider, alongside the rider's name, phone and availability. **This does not give riders a way to sign in** — no login operation exists for them yet (that is #639 part C) — it removes the reason one could not be built. Two promises are made explicitly and are enforced by the database rather than by review: one auth subject belongs to at most ONE rider (a duplicate would silently hand one rider another rider's order scope), and the rider's PHONE is never a lookup key, because French mobile numbers are recycled and the person who inherits a number must not inherit the account. | 0 | `specs/database/tables/projection_tables.yaml`: new `Rider` table (`read_common`, `internal: true`, `recovery: replay`) fed by `RiderRegistered`/`RiderInfoUpdated`/`RiderStatusChanged`; `auth_ref` NOT NULL UNIQUE, `display_name`/`phone` NOT NULL (a nullable column would make the emitter blind-overwrite on a partial update and erase the name), `status` from the payload and never a `derive:` map. New migration + projector group with its own checkpoint; empty `RiderCompute`. No event, command or API change; no new stored shape. Warning surface NARROWED (`event-not-projected` 9 -> 6), baseline tightened in the same commit. | 0 errors |
| 2026-08-30 | **`PUBLIC` in the event log stops meaning "nobody" and starts meaning "no role was proved".** An authenticated caller whose credential asserts a role but carries no domain binding used to have that asserted role written into `domain_events.user_type` — so the log could record *"RESTAURANT approved this"* for a credential bound to no restaurant, permanently, in a record that is never rewritten. Such a caller is now recorded as PUBLIC while keeping its auth subject, because it did authenticate. The consequence for anyone reading the log is stated in the scalar itself: the pair `(user_type = PUBLIC, user_id = <subject>)` is now inhabited and did not exist before, so a fold that inferred "anonymous" from PUBLIC must read `user_id` instead. | 0 | `specs/common/scalars.yaml` `UserType`: the `PUBLIC` gloss and the reading note. Prose only — no value added, removed or renamed, and no stored row rewritten (historical events keep what they recorded, which is the evidence the defect existed). The behaviour change is in `crates/server` (#639 part B). | 0 errors |
| 2026-08-30 | **Why a restaurateur can claim their listing without an account is now written down where the permission is granted, instead of being re-derived by whoever next reads the roles list.** `claimRestaurantListing` and `optOutRestaurantListing` are open to anonymous callers; [ADR-20260818-101500](adr/ADR-20260818-101500-the-restaurant-signs-in-by-email-link-and-638-freezes-at-chunk-1.md) required that to be resolved explicitly rather than discovered later. Kept, with the reason: the Google ownership proof IS the authentication, and the claimant by construction has no account yet — that is the whole pre-registration listing journey. Opting OUT keeps it for a stronger reason: an owner who wants off our listing must not have to create an account with us first. | 0 | `specs/network/api.yaml`: comments on the two operations' `roles:` lists. No role added or removed — the surface is byte-identical. | 0 errors |
| 2026-08-30 | **The operator's map of the Order-birth switch now says what the switch actually does.** `ROUTE_ORDER_BIRTH_THROUGH_LANE` was flipped ON on 2026-08-30 ([ADR-20260830-012200](adr/ADR-20260830-012200-the-order-birth-routes-through-the-lane.md)) but its operator-facing text went on saying *"OFF — the DEFAULT — is today's behaviour byte for byte"*, and that text is the doc comment of every generated config: **14 files** carried it, with `Config::describe()` printing the live `true` beside a doc asserting OFF. Under incident pressure the sentence told the operator that setting it OFF returns to current behaviour, when OFF is now a REVERT to the legacy unrouted birth. The text now describes the current state, and the duplication that produced it is gone: **a configuration key's default is no longer restatable in prose at all** — the emitter prepends `DEFAULT \`<value>\`` to every generated doc comment from `default:` itself, and a validator rule refuses any bool `gates:` block that names which position is the default. `ENFORCE_ACCEPTANCE_TIMEOUT`'s precondition (5a) is corrected in the same change: it presented the routing flip as pending work, and the flip has landed. | 0 | `specs/ordering/configuration.yaml` `ROUTE_ORDER_BIRTH_THROUGH_LANE.gates` rewritten (ON is the current state; OFF is the legacy revert) and `ENFORCE_ACCEPTANCE_TIMEOUT.gates` (5)/(5a) un-staled; default-polarity restatements removed from 8 further bool keys' prose in `specs/{common,customer,network,ordering}/configuration.yaml` (true, but a second source). `tools/codegen-rs`: emitter prepends the declared default into the generated doc comment; new validator rule `config-gates-restates-default` + 2 codegen tests. No key, default value, payload or schema change — the 58 regenerated `config.rs` doc comments (57 bins + the server's) are the whole runtime footprint. | 0 errors, warnings unchanged |
| 2026-08-30 | **A message an actor is declared to receive but nobody handles can now be said OUT LOUD in the spec, with a reason and an owner, instead of being an invisible dead end.** `actors.yaml` gains `receives[].deferred: { reason, issue }`: the actor still receives the message, the door is still real, and what is deferred is the handler. **The grammar lands with ZERO instances, on purpose.** Its intended first user — the delivery job's rider/admin status correction, one of ten declared-but-unrouted commands [#771](https://github.com/TheCaptainCompany/captain-food/issues/771) found — turned out to have its handler already written and is simply WIRED, so declaring it deferred would have been a false statement in the spec. The grammar is kept because the remaining `deliver:` routes of the isolation work will each be an opportunity to declare a real one, and it is cheaper to have the reviewable form ready than to invent it under pressure; deferrals used to live in a Rust list inside the code generator, with no reason and no owner. | 0 | `specs/*/actors.yaml`: new OPTIONAL `deferred:` key on a `receives:` entry, validator-enforced (`receives-deferred-shape` — closed key set, both fields required, the issue a full URL). **No instance declared**, and a codegen test (`the_corpus_declares_no_deferral`) makes the first one a deliberate, argued change. `specs/delivery/actors.yaml`: comment only, recording why `UpdateDeliveryStatus` was invisible. Additive; no payload, schema or stored shape change. | 0 errors |
| 2026-08-30 | **The delete-my-account mutation no longer tells clients that its refusal depends on a deployment switch.** Its description — which is the text every CUSTOMER-role client sees in GraphQL introspection — said the request is rejected *"when the deletion engine is gated OFF in this deployment"*. Nothing in the running system reads that switch on this path: the mutation refuses UNCONDITIONALLY while the journey that executes an erasure is being built, and the description now says so, matching the four other places that already did. Raised by the independent reviewer pass on [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1. | 0 | `specs/customer/commands.yaml` `RequestCustomerErasure.description` rewritten; regenerated into `crates/server/src/graphql/generated/inputs.rs`, `crates/domains/customer/src/commands.rs` and the documentation artifact. Description only — no field, schema or stored shape change. | 0 errors |
| 2026-08-30 | **Captured payments and refunds are now covered by the same retention gate as the orders they settle.** `PaymentCaptured` and `PaymentRefunded` carry the 10-year commercial-books horizon, so any future deletion path is mechanically forbidden from destroying the record of money that actually moved. There is no deletion block on Payment today — this closes the window BEFORE the first one is written, which is the only moment the fix is free. Raised by the legal lens reviewing [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1. | 0 | `specs/payments/events.yaml` +2 `legalRetention:` markers, both `FRENCH_COMMERCIAL_BOOKS_10Y`. No payload, schema or stored shape change. | 0 errors |
| 2026-08-30 | **The message shown when account deletion is unavailable no longer promises a service we do not run.** It previously said *"contact us and we will handle your request"* — an intake commitment for a channel with no runbook, no request register and no owner. It now states plainly that the request was NOT recorded (so nobody stops chasing a right they believe is in progress) and that their right to erasure is unaffected, without naming a channel or a delay we would then owe. Raised by the legal lens reviewing [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1. | 0 | `specs/customer/errors.yaml` `ErasureEngineUnavailable.messages` en/fr rewritten. Copy only. | 0 errors |
| 2026-08-30 | **A Tours customer can now ASK to be forgotten, and the product answers honestly instead of pretending.** The DSL declares the whole Art. 17 vocabulary — request, confirm, cancel, the recorded grace window, the provider-side identity unlink, and a pseudonymous `CustomerErased` receipt that outlives the deleted stream — plus a status surface that resolves the subject from the session, so nobody can ask what is happening to somebody else's account. The `deletion:` block that would ARM this journey is deliberately NOT declared yet: the deletion engine validates every policy when it starts, so declaring one it cannot serve would stop the engine constructing at all and break the ORDER erasure that already works. While the journey that executes an erasure is still being built, all three write paths REFUSE with a typed, translated error and record nothing: accepting a request nothing can execute would start the statutory 30-day clock on a promise we could not keep, and leave the person watching a "scheduled" screen forever. | 0 | [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1, [PROP-20260829-150752](proposals/PROP-20260829-150752-customer-erasure.md) | 0 errors |
| 2026-08-30 | **An event can now declare the law that forces us to keep it, and the deletion engine is forbidden from contradicting it.** `legalRetention:` names an approved retention window on the event itself, so the 10-year commercial-books horizon on order financial records and the never-delete Art. 21 objection register are enforced by the validator rather than remembered by a reviewer. Concretely: a deletion window shorter than a retention horizon is now refused, an indefinite horizon bars stream deletion outright, and a window nobody can compute is refused rather than assumed long enough. This closes the trap [BRIEF-20260811 §3](legal/BRIEF-20260811-erasure-zone-and-retention.md) recorded as BLOCKER-on-arrival — giving Restaurant an Order-shaped deletion block would have destroyed the register that proves an objection was honoured. | 0 | [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1, DECISIONS MET-W | 0 errors |
| 2026-08-30 | **A restaurant that goes silent on a REPLACEMENT order can now be noticed.** When a reclamation is resolved as REPLACEMENT, the no-charge remake is placed by asking the Order aggregate through its own mailbox lane instead of the saga writing the order into existence from outside — so the replacement gets the same acceptance clock every other order gets (before this, it got none: the deadline is declared on a DELIVERY, and there was no delivery), and a refusal the Order makes (the original order is gone) lands as a supervisable rejected message instead of a log line nobody routes. Behind a per-route gate, default OFF, so the behaviour that ships is unchanged until a separate recorded decision flips it. Chunk C2 of [ADR-20260829-230418](adr/ADR-20260829-230418-aggregates-own-the-facts-isolation-first.md), [#595](https://github.com/TheCaptainCompany/captain-food/issues/595). | 0 | `specs/ordering/configuration.yaml` +`ROUTE_REPLACEMENT_BIRTH_THROUGH_LANE` (bool, `default: false`), and its sibling `ENFORCE_ACCEPTANCE_TIMEOUT` prose repointed (the gap it named is now a gated route, not an unfixed one). `specs/ordering/processmanager.yaml` ReclamationProcess `receives[ReclamationResolved]` +`sends:` — a NEW declarative key on a PM leg for a command its hand-written wrapper really sends but no `send:` step can express (the payload is a derived id, not a `with:` mapping); it makes that seam a `$ref` the refs walker can see, held by new validator rules `pm-sends-kind` / `pm-sends-no-inbox` and a new `REF_CONTRACT` row; `specs/common/processmanager.yaml` documents the key in the kernel's `receives` field list, so the grammar is discoverable without reading the validator. No event payload, stream or stored shape changed. | 0 errors, warnings unchanged |
| 2026-08-30 | **A placed order's birth now travels through the Order's own lane by default** — the checkout saga stops appending `OrderPlaced` to the Order's stream itself and instead hands the fact to the Order's lane worker, which appends it and thereby arms the acceptance clock (the precondition for ever auto-cancelling an unacknowledged order); the customer-visible checkout is unchanged, the birth appears one lane-hop later, and rollback is a config flip back with no redeploy. Decided by the founder, verbatim *"A — Flip it ON"* ([ADR-20260830-012200](adr/ADR-20260830-012200-the-order-birth-routes-through-the-lane.md), evidence PR [#761 "C1a: pre-flip evidence for ROUTE_ORDER_BIRTH_THROUGH_LANE (#758)"](https://github.com/TheCaptainCompany/captain-food/pull/761)). | 0 | `specs/ordering/configuration.yaml` `ROUTE_ORDER_BIRTH_THROUGH_LANE` `default: false → true` (one line; the key's text untouched). Replay-safe, NOT a migration (young, ADR-20260829-230418: payload/type/stream unchanged; the envelope change is recorded, stored rows never backfilled). | 0 errors, warnings unchanged |
| 2026-08-29 | **The acceptance-timeout's birth-routing precondition is now honestly TWO flips, not one**: routing the Order birth through its own lane needs walk-stack evidence (flag ON + births observed on the birth-lag histogram — what C1a produces; "walk stack, not production" is a reading of the deliberately suspended production, ADR-20260817-105844, and the flip decision is the founder's per dispatch card 598 §7/§9), while the acceptance-timeout default itself still waits on production time-to-accept distributions that no walk can produce. The earlier fused wording priced both as one condition. Part of C1a ([#758 "C1a: pre-flip evidence for ROUTE_ORDER_BIRTH_THROUGH_LANE — birth-lag seen recorded, the paid-then-null tracking guard, split flip preconditions"](https://github.com/TheCaptainCompany/captain-food/issues/758), ADR-20260829-230418). | 0 | `specs/ordering/configuration.yaml` `ENFORCE_ACCEPTANCE_TIMEOUT.gates` precondition (5) → (5a)/(5b). Prose only; no key, default or schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **A customer who just paid is never told their order was not found while its birth is still being confirmed.** The confirmation page's paid handoff window now carries the founder-approved acceptance-first reassurance — fr verbatim *"Reçu ✓ — confirmation en cours…"* — instead of silence (unresolved) or *"Commande introuvable"* (an answered-null read while the routed birth rides the Order lane, the window `ROUTE_ORDER_BIRTH_THROUGH_LANE` makes real). A stranger's URL still honestly says not found. Closes the GAP(copy) that rode [#420 "Frontend: Leptos/WASM SDUI renderer"](https://github.com/TheCaptainCompany/captain-food/issues/420)'s spec half; part of C1a ([#758 "C1a: pre-flip evidence for ROUTE_ORDER_BIRTH_THROUGH_LANE — birth-lag seen recorded, the paid-then-null tracking guard, split flip preconditions"](https://github.com/TheCaptainCompany/captain-food/issues/758), ADR-20260829-230418). | 0 | `specs/screens/restaurant_frontoffice.translations.yaml` +`order.confirming` (fr founder-approved verbatim, en translation); `specs/translations.code_refs.yaml` +code_ref (`crates/web/src/tracking.rs`). No schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **The prose product spec is retired: the DSL is the ONE authority for what the customer web client promises.** Founder decision, verbatim: *"retire the product spec file"* (in the same message as the option-1 choice on [#755 "Local dev: accept `{slug}.localhost` as tenant space so storefronts are viewable without /etc/hosts"](https://github.com/TheCaptainCompany/captain-food/issues/755), after a 13-lens consult). `specs/PRODUCT_SPEC_WEB_CLIENT.md` — a pre-DSL narrative that asserted Next.js against ADR-0034/0035 and a checkout shape/capture posture the commands DSL contradicts — is deleted after migrating its three unique sections: checkout gating + the passkey/SMS-cost stance now live in `specs/integrations/supabase.md` §5-§6, the founding ~2s-on-4G budget in `docs/frontend/renderer-architecture.md` (beside the screens' already-executable `performance:` contracts), and the not-yet-built `restos.captain.food/onboarding` destination is a DECLARED gap on the partner landing screen instead of a live-looking promise. Register row `docs/decisions/PRODUCT-SPEC-WEB-CLIENT-RETIRED.yaml`, ADR-20260829-092233. | 0 | `specs/PRODUCT_SPEC_WEB_CLIENT.md` deleted; `specs/integrations/supabase.md` +§5 checkout gating, §6 passkey stance sharpened; `specs/ordering/events.yaml` `OrderPlaced.customerId` comment repointed; `specs/screens/captain_frontoffice.yaml` `partner_landing` +gap. No scalar, event, command or schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **A restaurant's storefront has exactly ONE address — `https://{slug}.captain.food` — and the old path form (`/r/{slug}`) permanently redirects to it.** Founder decision, verbatim: *"I don't want to have /r/<slug> possible we already have it in the <slug>.captain.food"*. Printed menus, QR codes and old search results that used the path form keep working via a 301 to the canonical host (the same treatment a renamed storefront's old address gets); the web client no longer knows the path form at all. This reverses a recorded "stays for path-addressed access" stance and a product-spec promise — register row `docs/decisions/PATH-ADDRESSED-STOREFRONT.yaml`, ADR-20260829-082615. | 0 | [#749](https://github.com/TheCaptainCompany/captain-food/issues/749) — `specs/screens/restaurant_frontoffice.yaml` restaurant screen `route: "/r/:slug"` → `"/"`; prose sweep in `specs/network/api.yaml` (`restaurant` description) + `specs/PRODUCT_SPEC_WEB_CLIENT.md`; server fallback 301 (`hosts::path_addressed_redirect`). No schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **The storefront MENU now actually renders — for the first time from a real paint.** A customer opening `{slug}.captain.food` finally sees the restaurant's items (names, descriptions, prices) in the initial HTML: the catalog read can now be asked BY STOREFRONT SLUG (`restaurantSlug`, exactly-one-of with `restaurantId` — a DSL-declared constraint the codegen turns into a generated check and an SDL description, since GraphQL cannot spell "exactly one of these two arguments"), a renamed storefront's old label resolves exactly like its old host does, and a request on one restaurant's storefront that names ANOTHER restaurant's catalog is rejected with a typed error instead of silently picking a side. The #745 skip declaration for this read was evicted by its own dead-man rule the moment the fix landed — the mechanism working as designed. | 0 | [#749](https://github.com/TheCaptainCompany/captain-food/issues/749) — `specs/catalog/api.yaml` `catalog` query: `restaurantId` required→optional (ADDITIVE), new optional `restaurantSlug`, new `argsExactlyOneOf` DSL surface (validator rule `api-args-exactly-one-of`, REF_CONTRACT rows); `specs/catalog/errors.yaml` + `CatalogSelectorInvalid`, `TenantSelectorMismatch`; `specs/screens/restaurant_frontoffice.yaml` restaurant screen: `skipped_reads` evicted, `catalog_sections` binds `catalog.products`. Generated SDL diff is the relaxation + the new optional arg + the description, nothing removed. | 0 errors, warnings unchanged |
| 2026-08-29 | **A screen read that structurally cannot run no longer fires the degraded-render alarm on every visit — and every such read is now DECLARED, provable, and impossible to forget.** Nine screen reads across three surfaces asked queries whose required argument the screen has no source for (checkout's payment status before an order exists, the marketplace's per-restaurant category rail, staff screens' own-identity args, the storefront's catalog read — that last one a LIVE MENU defect, filed URGENT as [#749](https://github.com/TheCaptainCompany/captain-food/issues/749)); each failed on EVERY paint and most drowned the `sdui_degraded_render_total` alert the observability contract promises to act on. The screens DSL now declares each (`skipped_reads:` with the missing arg, who supplies it, and why), a validator proves the declaration in BOTH directions (an undeclared unfulfillable read is an error; a declaration that a new source falsifies is an error — the skip can never outlive its justification), the runtime skips them before any network, and the `:orderId`→`id` rename bridge plus tenant-host slug injection moved from hand-written code to the generated data layer — which also makes checkout's service-window read actually work. | 0 | [#745](https://github.com/TheCaptainCompany/captain-food/issues/745) — `specs/screens/*.yaml` `skipped_reads:` on 9 screens; `specs/observability.yaml` read-authorization `resolver_failed` doc extended (structural skips are not counted; reasons go to the trace). No scalar, event, command or schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **The system mailbox supervision screen's two lists are now actually wired to their data.** Both lists bound template names (`{{ mailbox_lanes }}`/`{{ mailbox_poisoned }}`) that matched no name the runtime ever derives from their resolvers, so neither could ever show rows NOR carry the error state — a supervision surface that would have rendered permanently empty over live lanes and poisoned messages (journal W35's "dormant" finding, forced by [#729](https://github.com/TheCaptainCompany/captain-food/issues/729)'s screen-level red test). They now bind the derived reversed aliases (`{{ lanes_mailbox }}`/`{{ poisoned_mailbox }}`), the corpus convention (`featured_restaurants`, `all_restaurants`, `search_restaurants`) — chosen over widening the alias derivation because it is the smaller diff and adds no third alias form every future scanner must know. | 0 | [#729](https://github.com/TheCaptainCompany/captain-food/issues/729)/[#730](https://github.com/TheCaptainCompany/captain-food/issues/730) — `specs/screens/system.yaml` `lane_list.items` + `poisoned_list.items` rebound; screen content only, no scalar, event, command or schema change. | 0 errors, warnings unchanged |
| 2026-08-29 | **The marketplace search screen's branch content is now honest about what exists.** The "Popular categories" section now BINDS the real categories catalog instead of a data source that never existed — though the tiles themselves still await a `category_grid` renderer arm ([#741 "`category_grid` has no renderer arm — the search screen's now-visible "Popular categories" section renders a tile-less container over real data"](https://github.com/TheCaptainCompany/captain-food/issues/741)), so today it renders as a titled section over a generic container; the "Dishes" results section is declared for what it is — a gap with no dish-search behind it — so the client renders it as gapped instead of a permanently-empty list that looks live; and "Recent searches" is a DECLARED gap (client-side state with no producer and unregistered tap actions, the client-state producer tracked in [#723 "The OTP sheet's resend affordance is unreachable: nothing produces `resend_available` client state for the condition evaluator"](https://github.com/TheCaptainCompany/captain-food/issues/723)) that stays hidden rather than rendering a dead control. No behaviour was added — the screen now tells the truth about which of its parts work. | 0 | [#725](https://github.com/TheCaptainCompany/captain-food/issues/725) — `specs/screens/captain_frontoffice.yaml` search screen: `popular_categories` → `categories` (the categories.all alias), `results.restaurants`/`results.dishes` → the real `search_restaurants`/`search_dishes` aliases, new `searches.recent` gap resolver + screen gap note. Paired emitter change un-flattens `conditional_section` branches into renderable named child groups. | 0 errors, warnings unchanged |
| 2026-08-29 | **Nothing customer-visible — the customer search screen's "empty query" condition is respelled from `== ""` to `== ''` so every string literal in the screens DSL now uses the ONE quoting the validator's status-completeness tokeniser can see; the renderer's grammar rejects double quotes loudly instead of accepting a spelling the validator is blind to.** | 0 | [#728](https://github.com/TheCaptainCompany/captain-food/issues/728) — `specs/screens/captain_frontoffice.yaml` search screen `conditional_section` condition: `search_input.value == ""` → `== ''` (eval-identical); `crates/web/src/condition.rs` `parse_quoted` narrowed to single quotes only, `== ""` pinned as a permanent ParseError. | 0 errors, warnings unchanged |
| 2026-08-29 | **Every screen binding that names a field its data does not carry is now a build failure — and the twenty-one that already did are gone.** The #468 cart fix built the checker but left it unwired because the rest of the corpus was dirty; re-measured today the debt was 21 dead bindings across 5 screens (the recorded estimate said 13), each rendering empty while the spec read as though it were bound. All 21 are paid down: the staff conversation screen's status chip and public timeline work again (its two resolvers silently shadowed each other under one data namespace — now split, and the staff-only notes namespace still never reaches the customer schema), the storefront titles a restaurant by its real display name and tags, the cart and rider job screens name the restaurant through the schema's real navigation edge, and the order-tracking summary binds the order's real line items and buyer total. Five widgets that bound concepts the product does not model at all — storefront cover/logo imagery, a pre-order delivery ETA, a delivery-fee row that could render "Livraison gratuite" as a false price claim, a minimum-order row whose amount nothing supplies, and the rider's and customer's call-the-restaurant buttons (no restaurant phone exists on the API) — are REMOVED and declared as named concept gaps rather than left as dead controls; the empty ETA slot is the priority gap (the ETA is the product). The checker now also walks FK-derived navigation edges, so a binding through `cart.restaurant` is checked instead of falsely flagged. | 0 | [#717](https://github.com/TheCaptainCompany/captain-food/issues/717) — `specs/screens/restaurant_backoffice.yaml`: resolver `conversation.internalNotes` renamed `internal_notes.byOrder` (namespace split; read bindings rebound, `post_message`/actions untouched — this also renames the `resolver` attribute value seen by `sdui_degraded_render_total`, no contract change). `specs/screens/restaurant_frontoffice.yaml`: `restaurant.name`→`displayName`, `categories`→`tags` (discovery line only), `restaurant.catalog.categories`→`catalog.categories`, hero imagery/ETA/fee/min-order rows removed + concept gaps, `cart.restaurantName`→`cart.restaurant.displayName`, order_tracking `lines/total`→`items/totalAmount`, timeline + contact rows removed + concept gaps. `specs/screens/rider.yaml`: contact row → plain name row via `delivery.restaurant.displayName` + STO-8-citing gap; new key `rider.job.restaurant`. Orphaned keys `common.delivery.free`, `restaurant.min_order` deleted. Validator §25 `screen-binding-unknown-field` wired full-strength (no skip list, no warning-only) + nav-edge walk; the generated client data layer now also FETCHES the screen-bound nav edges (`restaurant { displayName }` under `cart.current`/`delivery.byOrder`, and nothing else — round-1 review fix, so an approved nav binding cannot render empty); checkout degraded-state widget and `schema.generated.graphql`/`operation_scopes.rs`/gateway bins byte-identical. | 0 errors, warnings unchanged |
| 2026-08-29 | **A control that declares a condition now obeys it, and a page whose data failed to load now says so instead of pretending the thing does not exist.** The screens DSL declares 35 show/hide/disable conditions (auth-sheet controls, the cart FAB, the checkout button over an empty cart, claim badges…) and the renderer evaluated none of them — every conditional control rendered live, unconditionally. The DSL's condition grammar is now pinned to exactly the shapes the specs use (one outlier spelling, an unquoted `selected_delivery_mode == delivery`, is quoted to match the grammar the validator already tokenises — no behaviour intent changed), a failed data read renders a translated "couldn't load" state with a user-initiated retry (never "your cart is empty"/"order not found" over a transient failure, and never the raw transport error), and every degraded server render is counted with the page, the failing read and the reason, joined to its own request trace. | 0 | [#472](https://github.com/TheCaptainCompany/captain-food/issues/472) — `specs/screens/restaurant_frontoffice.yaml`: 3 × `visible_when: selected_delivery_mode == delivery` quoted to `== 'delivery'` (expression spelling only). `specs/translations.yaml` + `specs/screens/restaurant_frontoffice.translations.yaml`: new keys `common.error.data_unavailable`, `common.error.retry`, `cart.error.load`, `order.error.tracking_stale` (additive, fr+en; declared in `translations.code_refs.yaml` — consumed by hand-written Rust). `specs/observability.yaml` `read-authorization`: new counter `sdui_degraded_render_total{screen, resolver, reason, correlation_id}` (bounded reasons `resolver_failed` \| `condition_unparseable`; `client_*` legs reserved, no OTel in WASM). Emitter: `ResolverKey::roles()` (api.yaml `roles:`, verbatim) so a role-refused read is distinguished from a real failure structurally. No scalar, event, command or GraphQL schema change. | 0 errors, warnings unchanged |
| 2026-08-28 | **The cart screen can now show a customer their price.** Six bindings on the cart summary named fields the `Cart` API type does not have (`cart.subtotal`, `cart.deliveryFee`, `cart.serviceFee`, `cart.total`, `cart.discount`, `cart.minimumOrderMet`), so every row rendered `undefined` — the price a customer decides on before ordering was invisible. Subtotal, delivery fee, service fee and total now bind the real, graphql-verified checkout breakdown (already TTC, so no relabeling was needed) and the service-fee row carries the decided "Captain service fee" wording. Two of the six named a concept the product does not model at all yet — a promo-code discount and a minimum order amount — so those rows, and the minimum-order warning control, are removed rather than left silently wrong, and a decision is now open on whether V0 ships either at all. | 0 | [#468](https://github.com/TheCaptainCompany/captain-food/issues/468) — `specs/screens/restaurant_frontoffice.yaml`: `cart` screen's `order_summary_block` + `checkout_btn.disabled_when` rebind to `cart.breakdown.*`; `cart.discount`/`cart.minimumOrderMet` removed from live widgets and declared as `gaps:`; checkout screen's `cart_summary_mini` total rebinds the same way. `specs/screens/restaurant_frontoffice.translations.yaml`: `cart.service_fee` reworded, `cart.discount`/`cart.min_warning` keys removed. `docs/decisions/V0-PROMO-AND-MINIMUM.yaml` (open, owner founder). No scalar, event, command or GraphQL schema change — screen content + translations only. New validator mechanism `tools/codegen-rs/src/validate/screen_bindings.rs` (§25, resolves `{{ }}` template bindings against the bound api type) is proven by 7 unit tests but left UN-WIRED: run corpus-wide it reds 13 times on OTHER screens, none a trivial rename. | 0 errors, warnings unchanged |
| 2026-08-28 | **A customer who signs in on the customer or marketplace storefront now actually gets signed in: the OTP code sheet opens, and completing it lands the session cookie.** Two screens declared the whole phone-verification flow but never wired the two moments that make it work — the code-entry sheet had nothing telling it to OPEN once the SMS is sent, and the endpoint that turns a verified code into a browser session had no caller at all, so a customer who entered a valid code stayed a stranger to every following request. Both are now declared, using a new closed, typed vocabulary for "what happens next" (`on_success`) that a screen author can no longer typo past unnoticed: opening a sheet and claiming a session join `navigate`/`close_sheet`/`show_toast` as the only five values the loader accepts anywhere in the DSL. Nothing about who is allowed to sign in, or when, changes — anonymous checkout is untouched. | 0 | [#529](https://github.com/TheCaptainCompany/captain-food/issues/529) — `specs/screens/captain_frontoffice.yaml` + `specs/screens/restaurant_frontoffice.yaml`: `auth_sheet.send_otp_btn.action` gains `on_success: { type: open_bottom_sheet, sheet_id: otp_sheet }`; `otp_sheet.otp_field.on_complete` gains `on_success: [{ type: claim_session }, { type: navigate, route: "$reload" }]`. New validator rule `screen-on-success-unknown-type` closes the `on_success.type` vocabulary loader-wide. No scalar, event, command, error or GraphQL schema change — SDUI screen content + a validator rule only. | 0 errors, warnings unchanged |
| 2026-08-28 | **A customer's identity can now be resolved from our own database instead of trusted off the sign-in token — behind a switch that is OFF today.** IDENT-1 Phase A ([ADR-20260818-004646](adr/ADR-20260818-004646-no-business-identifier-lives-in-the-identity-provider.md)) adds the declared toggle that will let the server stop reading the `customer_id` the identity provider stamped onto a token and look the caller up in Postgres instead, plus the telemetry that will tell operators apart a customer with no mapping row yet (ordinary) from the lookup itself failing (an outage). The switch stays OFF until a separate, later decision flips it — nothing about how any customer is identified today has changed. | 0 | [#641](https://github.com/TheCaptainCompany/captain-food/issues/641) — `specs/customer/configuration.yaml`: new key `RESOLVE_CUSTOMER_IDENTITY_FROM_POSTGRES` (bool, default `false`, gate-then-stabilize). `specs/observability.yaml`: new `customer-identity` contract (span `customer.identity.resolve`, histogram `customer_identity_resolve_ms{result}`, counters `customer_identity_not_found_total` / `customer_identity_lookup_failed_total{reason}` / `customer_identity_lookup_source_total{source}`). No scalar, event, command or schema change. | 0 errors, warnings unchanged |
| 2026-08-28 | **The refusal metric for OTP sign-in can now tell a rejected North American customer apart from a rejected customer anywhere else in the world, and the record now says out loud that nobody is watching the SMS pack's balance yet.** Dropping bare `+1` from the served phone allowlist protected the sign-up budget, but it also made every refused `+1` collapse into the same catch-all bucket as every other refused country — so the very cohort the ADR was written about became invisible on the dashboard meant to justify it. A refused send is now bucketed into one of three declared regions (North America, non-EU Europe, everywhere else) alongside the existing refusal reason, without widening the label an anonymous caller can mint: whatever dialing code arrives, only one of three new values can appear. Separately, the product now records — rather than silently owes — that nobody is yet told when the prepaid SMS credit pack is running low, which matters because that pack running out is the same thing as phone sign-in stopping for everyone. | 0 | [#696](https://github.com/TheCaptainCompany/captain-food/issues/696) — `specs/observability.yaml` `customer-identification`: `otp_send_refused_total` gains attribute `region` (`attribute_values: [north_america, non_eu_europe, rest_of_world]`, mapping table declared in `crates/telemetry/src/meters.rs`); new gauge `ovh_sms_credit_balance` declared NOT YET IMPLEMENTED (tracked by [#699](https://github.com/TheCaptainCompany/captain-food/issues/699), ADR-20260813-021500's second `OWED` item). No `reason` value, no scalar, no event and no operation changed. | 0 errors, +1 `obs-metric-no-emitter` (the declared-but-unbuilt `ovh_sms_credit_balance`, accepted — #699 closes it by building the poll and the instrument together); baseline refreshed in the same commit |
| 2026-08-17 | **When a checkout fails, the record now says WHICH part failed — and it is built so it can never say anything a payment provider wrote.** Until today a failed order placement left one line: *an internal error occurred*, with an empty explanation and nothing in the log. "Stripe is refusing our credentials" and "the stored request no longer fits the running software" produced the identical record, so at Friday peak nobody could tell which was happening. The record now names the boundary that failed and why, plus the provider's HTTP status where there is one — 401 (our credentials), 400 (our request), 402 (the customer's card) — from a fixed, declared list of possibilities. **The reason it is a fixed list and not free text is a real hazard, not tidiness**: the record is kept for the retention window and is shown back to callers, and a payment provider's own error text for a bad key literally contains the key. The precedent everywhere else in the code is to copy that text in, and one branch of this very path already did. It cannot any more: the shape has nowhere to put a sentence. The full diagnostic text still exists — it goes to the log, where it is not retained for ninety days and not served to anyone. **Nothing about what fails, what the customer is told, or how anything retries has changed.** Alongside: the riskiest step of checkout — the outbound payment call — can finally be marked as failed in tracing at all, which it never could before, so the error budget for checkout stops being permanently empty for a reason that had nothing to do with reliability. | 0 | [#623](https://github.com/TheCaptainCompany/captain-food/issues/623) (with the floor of [#625](https://github.com/TheCaptainCompany/captain-food/issues/625) and part 1 of [#624](https://github.com/TheCaptainCompany/captain-food/issues/624)) — `specs/common/scalars.yaml`: new `CommandFailureSeam` (`PAYMENT_GATEWAY` / `COMMAND_PAYLOAD` / `DOMAIN_INVARIANT` / `INFRASTRUCTURE`, every member with a producer proved by an exhaustive test — a fifth, `EVENT_APPEND`, was drafted and WITHDRAWN in review because its producers are #628's, and a declared-but-unemitted member is the defect class this scalar exists to fix), `CommandFailureReason` (`GATEWAY_REFUSED` / `CARD_DECLINED` / `PAYLOAD_UNDECODABLE` / `UNCATALOGUED_INVARIANT` / `TRANSIENT_INFRASTRUCTURE`), `GatewayStatusCode` (integer). `specs/common/entities.yaml`: new `CommandFailureAttribution` — the ONLY shape `inbound_messages.error.context` may carry on a non-catalogued failure, every field a closed set or a number. **No `errors.yaml` change**: catalogue membership is the REJECTED-vs-FAILED discriminator, so a new code would flip a verdict. Additive-only in `schema.generated.graphql` (three unreferenced declarations; entity/scalar emission is unconditional). **A declined card's record improves too**: it used to say `PaymentDeclined` and nothing else once its provider prose was dropped — safe, but it stopped answering *declined how?* on the money path — and now carries the same bounded shape with the gateway's `402`. It is claimed only on the EVIDENCE of a real gateway answer, so the fail-closed stand-in, which mints the same catalogued code with no gateway behind it, keeps saying nothing. Nothing stored, nothing emitted, no client, no recorded decision touched. New validator §21 `obs-technical-error-unreachable`. | 0 errors, +11 `obs-technical-error-unreachable` — a NEW rule (§21) measuring an existing gap, `place-order` fixed in the same commit (12 findings before the instrumentation, 11 after); baseline refreshed in the same commit |
| 2026-08-17 | **The specification stops claiming a security control the product does not have.** Six API descriptions said, in so many words, that the server checks you own the thing you are asking for — *"ownership enforced server-side"*. **That check does not exist on any of the six.** What actually protects them is a role guard, which asks *"are you a restaurant?"* and never *"are you THIS restaurant?"* — so any account of the right kind can read another business's data. The worst is the refund queue: the restaurant filter is **optional**, so a caller who simply leaves it out is handed **every restaurant's** refunds, on the money path. Also affected: another restaurant's claims queue (likewise unfiltered when called with no arguments), its delivery-satisfaction data, its list of locations, and — the sharpest, because it is reachable by a **self-registered customer** and holds unbounded customer free text — any order's message thread. Nothing about the product's behaviour changed today; **what changed is that the source of truth now describes it truthfully**, and points at the issues fixing it. This is the correction of a claim, not the closing of a gap: a false control claim is how a reviewer stops looking, which is the real damage it was doing. Nine other descriptions using the same wording were checked one by one against their code and are **true** — they were left exactly as they were. | 0 | [#618](https://github.com/TheCaptainCompany/captain-food/issues/618) read side / [#178](https://github.com/TheCaptainCompany/captain-food/issues/178) write side, [DECISIONS §39](proposals/DECISIONS.md) — description prose only, on `specs/ordering/api.yaml` (`restaurantReclamations`, `restaurantDeliverySatisfaction`), `specs/comms/api.yaml` (`orderConversation`, `orderConversationInternalNotes`), `specs/payments/api.yaml` (`pendingRefunds`), `specs/network/api.yaml` (`restaurantLocationsByAccount`). No scalar, field, arg, role, event, view or operation changed; `schema.generated.graphql` is byte-identical, so no shipped client and no stored shape is touched. Regenerates `query.rs` doc comments + `documentation.generated.{md,html}`. Records: obligation-map HYGIENE row discharged, publication-split control withdrawn and replaced, `SECURITY.md` live-surface exclusion. | 0 errors, warnings unchanged |
| 2026-08-16 | **The system can now tell someone that a customer's money is held with no order behind it — and it says so even when the answer is "none".** That is the worst thing this product can do, and until now nothing anywhere could notice it: the one declared alarm was computed over the order-tracking table, so an order that was NEVER CREATED had no row and was invisible to it — and that alarm had never been emitted by any running code at all. The product now promises a signal, on every sweep, that says how long the oldest stranded authorization has been stranded, split by WHY (the hand-over is still being retried / the hand-over is dead and will never be retried / there is no checkout run for this payment at all), with a zero reported for each cause when there is nothing to report — so silence means the detector died, never "all clear". The alarm now also carries the response a human is meant to take, and states plainly that nobody is wired up to receive it yet. | 0 | [#608](https://github.com/TheCaptainCompany/captain-food/issues/608) — `specs/observability.yaml`: `place-order` gains `payment_authorized_no_order_birth_age_seconds{reason}` (declared `attribute_values`, lane-derived `thresholds` whose `derived_from` `$ref`s `configuration.yaml#/keys/MAILBOX_MAX_DELIVERY_ATTEMPTS` + `MAILBOX_HEARTBEAT_SECONDS`) and `payment_birth_gap_sweep_heartbeat_total`; the `payment-settlement` header loses its false coverage claim for `payment_authorized_unsettled_age_seconds`. New `REF_CONTRACT` site `*.metrics[*].thresholds[*].derived_from[*]` → `ConfigKey`. Runbook `docs/runbooks/authorized-payment-no-order-birth.md`. [ADR-20260816-213000](adr/ADR-20260816-213000-the-birth-gap-detector-reads-the-saga-run-not-an-anti-join.md). | 0 errors, +41 `obs-metric-no-emitter` warnings — a NEW rule (§20) measuring an existing gap, baseline refreshed in the same commit |
| 2026-08-16 | **The registry of mailbox lanes is no longer what decides which lane an order goes to, and the operations page stops teaching that it is.** The registry records who currently owns a lane and how far they have got; it is written only when a worker starts up, so it can legitimately be empty, partial or out of date. Anything deciding addressing from it will put the same order in two lanes at once — and both will look correctly locked. The spec now says the width comes from the declaration, that changing a declared width is a data migration rather than a settings change, and that such a cutover requires the affected queue drained first. **The supervision page changed with it**: it now lists every DECLARED lane instead of only the registered ones, so a queue holding a paid order whose worker was never started is visible instead of absent — it used to render nothing at all — and each row carries a new badge saying whether that lane is registered, never registered, or stranded outside the declared set, because the first two were previously indistinguishable on screen. The page's own guidance said lanes appear once a worker registers them; that sentence was the belief this change exists to destroy and is gone. | 0 | [#596](https://github.com/TheCaptainCompany/captain-food/issues/596) — `specs/database/tables/journals.yaml`: `mailbox_partitions.description` gains the NOT-THE-ROUTING-SOURCE paragraph, the stored-shape-migration ruling on width changes and the drain-first cutover condition. `specs/common/scalars.yaml`: new `MailboxLaneRegistration` (`SEEDED` / `DECLARED_UNSEEDED` / `UNDECLARED_ORPHAN`). `specs/common/api.yaml`: `MailboxLane` gains `registration` (ADDITIVE field on an ADMIN-only query) and the `mailboxLanes` description now states the declared-grid source. `specs/screens/system.yaml` + `system.translations.yaml`: registration badge, corrected empty state, rewritten reading guide. [ADR-20260816-165714](adr/ADR-20260816-165714-lane-addressing-is-declared-not-observed-and-an-unseeded-lane-must-wait.md). | 0 errors, warnings unchanged |
| 2026-08-16 | **Checkout can no longer be scored "successful" when the order was never written down anywhere.** Yesterday's change removed the order-write step from the checkout success rule outright, because after the birth-queue flip that write happens elsewhere. But the flip has not happened: today the saga still writes it inline, so a checkout that silently lost the order would have been reported as a success — the money moved, nobody was told, and the dashboard said fine. The rule now requires ONE of the two legal ways the order is made durable: the inline write, or the hand-over to the order's own queue. Same verdict before and after the flip, and it never reads the switch — a rule that reads the switch is a second place the flip has to be got right. A checkout that did NEITHER is now, correctly, a failure. | 0 | [#598](https://github.com/TheCaptainCompany/captain-food/issues/598) — `specs/observability.yaml` `place-order`: `status_rules.success.required_spans` gains the ALTERNATION term `{ any_of: [event.store.append, order.lane.enqueue] }` and the new `order.lane.enqueue` PRODUCER span; loader grammar extended (`obs-required-span-shape` / `obs-required-span-alternation`, four spec-data mutants red) and the emitted-contract guard widened so a branch nothing emits fails the build. | 0 errors, warnings unchanged |
| 2026-08-16 | **The 800 ms checkout target STAYS, on the record and with a date attached — and the time the flip moves out of it is now measured on its own.** After the birth-queue flip the checkout workflow is strictly shorter, so the target scores less work than it used to. Re-baselining it today would mean inventing a percentile from a distribution nobody has: the number is a promise about what the CUSTOMER feels, and what the customer feels has not changed. Two things make keeping it honest rather than lazy: the hand-over now carries its OWN target (1 s typical, 12 s worst case — neither measured, and not the same kind of number: 12 s is the queue's declared fallback pass plus an allowance for draining it, while 1 s is a threshold CHOSEN to tell "the fast path is carrying this" from "the fallback is", since the fast path has no declared latency to derive from), so "paid order → restaurant told" stays covered end to end; and the re-baseline has a trigger written into the spec — the first Friday/Saturday 19:00–21:30 service after the flip, because peak is the only traffic worth baselining from. | 0 | [#598](https://github.com/TheCaptainCompany/captain-food/issues/598) — `specs/observability.yaml` `place-order`: the contract `latency_budget` keeps 800/1500 with the decision, both conditions and the trigger date at the line; new per-metric `latency_budget` on `order_birth_lag_ms`, validated (`obs-metric-budget`). | 0 errors, warnings unchanged |
| 2026-08-16 | **The order queue now says "I am alive and there is nothing waiting" instead of saying nothing — starting today, months before the thing it guards is switched on.** The hand-over timing figure only exists when an order is actually handed over, so while the switch is off it is silent, and after the switch is on a silent figure means either "nobody ordered" or "the order queue is dead". Two new signals remove the ambiguity: a heartbeat that must tick for every declared queue on every pass, and the age of the oldest thing still waiting on it. Both run whatever the switch says — a liveness signal first proved on the day of the flip is a signal proved BY the flip — and the alarm is the heartbeat STOPPING, never a threshold. Deliberately not done by feeding zeros into the timing figure: injected zeros would corrupt the very number the flip is judged on. Alongside, each running process now publishes which side of each switch it is on, so a half-deployed fleet — where one order gets its acceptance deadline and the next does not, invisibly — is a fact someone can query rather than a promise someone made in review. | 0 | [#598](https://github.com/TheCaptainCompany/captain-food/issues/598) — `specs/observability.yaml` `place-order`: new `order_lane_watch_heartbeat_total{lane}` (counter), `order_lane_oldest_pending_age_ms{lane}` (gauge) and `runtime_flag_state{flag,value,bin}` (observable gauge), with the declared routed-lane population now emitted at runtime (`ROUTED_LANES`). | 0 errors, warnings unchanged |
| 2026-08-16 | **A paid order's birth now waits for the Order's own queue — and that is the only reason its acceptance clock can start at all.** Until now the checkout saga wrote the new order straight into the order's history itself, so nothing ever arrived in the order's own inbox: the five-minute "the restaurant has not accepted this" deadline had nothing to attach to and could never begin, for any real order. From now on the saga DECIDES the order exists and hands that decision to the order, which records it and starts its own clock. What a customer notices: the order appears a beat later after payment is authorised (it was already asynchronous — it happens on the payment webhook, not on the checkout click), and in exchange an order nobody accepts can finally be noticed. **It ships OFF.** Turning it on is a flip, and so is turning it back off; while it is off nothing changes at all. Two honest limits recorded rather than hidden: a REPLACEMENT order created by a reclamation still takes the old path and still has no clock ([#595](https://github.com/TheCaptainCompany/captain-food/issues/595)), and orders born after the flip carry a different record of who wrote them — legal, permanent and never rewritten, because a stored fact says what actually happened. | 0 | [#588](https://github.com/TheCaptainCompany/captain-food/issues/588) · [ADR-20260816-040239](adr/ADR-20260816-040239-deliver-is-a-lane-enqueue-not-a-foreign-stream-append.md) — new `specs/ordering/configuration.yaml#/ROUTE_ORDER_BIRTH_THROUGH_LANE` (bool, default false) stating what flipping changes and that rollback is a flip; `ENFORCE_ACCEPTANCE_TIMEOUT`'s precondition (5) restated against it and given the #595 second gap. Not a migration: payload, event type and stream name are unchanged, so no upcaster. | 0 errors, warnings unchanged |
| 2026-08-16 | **The checkout dashboard stops requiring the saga to do something it no longer does, and starts measuring the gap it introduced instead.** Checkout "success" required the order-write step to appear inside the checkout trace. With the birth handed to the order's own queue that write happens in a different piece of work, so the rule would either fail a checkout that did exactly the right thing, or quietly redefine what the 800 ms target measures while still showing green. The write is no longer required there. In its place, a new measurement: how long the hand-over takes, from the saga passing the order over to the order recording it. Nothing measured that before, because before this change there was no hand-over — and a growing figure here is the acceptance clock starting late, which is a restaurant hearing about a paid order later than it should. | 0 | [#588](https://github.com/TheCaptainCompany/captain-food/issues/588) · [ADR-20260816-040239](adr/ADR-20260816-040239-deliver-is-a-lane-enqueue-not-a-foreign-stream-append.md) — `specs/observability.yaml` `place-order`: `event.store.append` `required: false` and out of `status_rules.success.required_spans`; new technical metric `order_birth_lag_ms{routed}` (operational, not BAM — it must keep working when Postgres is degraded). | 0 errors, warnings unchanged |
| 2026-08-16 | **The acceptance-timeout gate now says out loud that its clock is never armed — flipping it today would change nothing while reading as shipped.** The `ENFORCE_ACCEPTANCE_TIMEOUT` gate text listed FOUR flip preconditions, one of which is "shadow evidence from the reminder.promote span". That evidence cannot accumulate: no production path enqueues an `Order`-lane birth message (the PlaceOrderProcess `deliver:` step appends `OrderPlaced` straight to the event store, the reclamation PM calls `place_replacement_order` in-process), so the deadline is scheduled for no real order and the shadow guard never runs outside tests. A FIFTH precondition is now named — a production producer for the Order-lane birth message ([#588 "The normal checkout path never enqueues OrderPlaced onto the Order lane — the acceptance clock cannot start for saga-appended births"](https://github.com/TheCaptainCompany/captain-food/issues/588)) — and recorded as FIRST in dependency order despite being listed last. Prose only: no key, default, event, view or operation changed, so nothing the system does today is different; what changed is that the record no longer overstates what ships. | **0** | [#167](https://github.com/TheCaptainCompany/captain-food/issues/167) · [PR #586](https://github.com/TheCaptainCompany/captain-food/pull/586) third-look review fixes — `specs/ordering/configuration.yaml` `ENFORCE_ACCEPTANCE_TIMEOUT` gates text (fifth precondition), matching qualifications in `docs/STATUS.md` and the Phases 0-1 row below, [PROP-20260810-234225](proposals/PROP-20260810-234225-business-metrics-for-every-persona.md) swept to the landed map-form `measures:` grammar, and a comment at each of the two `reschedule: keep` SQL sites naming the test that guards it | 0 errors, 39 warnings (unchanged) |
| 2026-08-16 | **A timed-out order now reaches the customer's screen, its shadow evidence is a declared contract, and the platform's first BUSINESS METRIC exists — the number the timeout's own deadline must be chosen from.** Three promises move: (1) OrderTracking folds `OrderAcceptanceTimedOut`, so the tracking page leaves PLACED the moment the cancellation records (and the back-office expired filter can find it) — this banks the +1 `event-not-projected` ratchet debt back down (40→39). (2) The `acceptance-timeout` observability contract makes the two present-tense "exported as OTLP evidence" claims true: the `reminder.promote` span (shadow/would-cancel split + fire delay) is the flip ADR's declared data set, and the promotion machinery's dead-man's switch + SCHEDULED-depth gauge are contract rows, not intentions. (3) `specs/business_metrics.yaml` is born — the FIRST row of the ADR-20260811-014129 catalog: `time_to_accept_ms`, a declared fold over OrderPlaced→OrderAcceptedByRestaurant envelope timestamps, p50/p90/p99 grouped by daypart AND restaurantId under the ManageOrders activity, refs-checked and fold-key-total by validator (`bam-*` rules; the projector/query emitters remain [#484](https://github.com/TheCaptainCompany/captain-food/issues/484)'s machinery) — and it is now the FOURTH named precondition in the `ENFORCE_ACCEPTANCE_TIMEOUT` gates text: the TTL is chosen from the distribution, never guessed. Alongside: `screen-status-token-unknown` refuses any hand-written status token outside the OrderStatus enum (the `order_card_status.status:` prop and the `order.status` visible_when lists), and the stale-bundle unknown-status gap is recorded as a `gaps:` entry on order_tracking. | 0 | [#167](https://github.com/TheCaptainCompany/captain-food/issues/167) · [PR #586](https://github.com/TheCaptainCompany/captain-food/pull/586) Phase 3 — `projection_tables.yaml` OrderTracking fedBy/status/status_changed_at + fold arm + pg test, `specs/observability.yaml#/acceptance-timeout`, `specs/business_metrics.yaml` + loader/ref-kinds (`Activity`/`BamProjection`/`BamMeasure`) + `validate/business_metrics.rs`, `specs/ordering/configuration.yaml` fourth flip precondition, `screen-status-token-unknown` + mutation-red codegen tests, `tools/codegen-rs/warning-baseline.json` −1 | 0 errors, 39 warnings (−1 `event-not-projected`, banked) |
| 2026-08-16 | **The acceptance deadline now actually DELIVERS — and a shadow firing can never start a live order's deletion clock.** Phase 2 of #167 routes the promoted `OrderAcceptanceTimedOut` reminder: gate ON, a still-PLACED order records the cancellation and starts its GDPR retention reminder in the same transaction; gate OFF (today's default), the identical guard runs, the delivery is absorbed as Ignored, and THE FENCE holds — `schedules:` apply on the Recorded/Cancelled arm ONLY, so a shadow would-cancel can never arm the OrderExpired deletion clock on an order that is still live (stated in the receive's effect prose, proved by a pg test, and the mutation that weakens it goes red). The spec's declared birth receive ("PlaceOrderProcess delivers OrderPlaced; the Order records it") gains its actual delivery route — previously a delivered birth would have landed loud-FAILED — and a redelivered, duplicate-absorbed birth re-applies its schedules, where `reschedule: keep` makes the FIRST deadline win (pg-proved: the pending row's time does not move). Every promoted-reminder delivery now exports the `reminder.promote` OTLP evidence span (due time, fire delay, gate position, would-cancel), and the promotion machinery gets a dead-man's switch that emits on every tick from OUTSIDE the worker — a dead promotion pass reads as a growing lag, never as silence (ADR-20260810-231300). | 0 | [#167](https://github.com/TheCaptainCompany/captain-food/issues/167) · [PR #586](https://github.com/TheCaptainCompany/captain-food/pull/586) Phase 2 — `specs/ordering/actors.yaml` timeout-receive effect prose (the fence, binding); `handle_acceptance_timeout` + `record_inbound_order_placed` routes, `reminder.promote` span + `reminder_promotion_due_lag_ms`/`mailbox_scheduled_depth` instruments, `CommandDeps.enforce_acceptance_timeout`, `InboundMessage.scheduled_at`, promotion watch, 4 pg E2E tests (contract rows land with Phase 3's observability.yaml in this same PR) | 0 errors, warnings unchanged |
| 2026-08-16 | **The restaurant's "expired — no response" treatment now renders only on an order that actually timed out, and its copy no longer claims the customer's money already moved.** The mob checkpoint on PR #586 stopped two things: the backoffice timed-out card was an unconditional screen-level node whose fallback rendering showed "Expired — no response" on EVERY board load (a false signifier on a money-adjacent surface), and its body stated the payment-hold release as done ("was released" / "a été libérée") while the release leg is still deferred — a completion the platform cannot prove. The component is renamed `order_card_state` → `order_card_status` (evans: `state:` keeps one meaning per surface), binds the board's orders, and renders per-card exactly for orders holding `CANCELLED_BY_TIMEOUT` — nothing otherwise, pinned by a renderer test both ways; the copy now says "is being released" / "est en cours de libération". | 0 | [#167](https://github.com/TheCaptainCompany/captain-food/issues/167) · [PR #586](https://github.com/TheCaptainCompany/captain-food/pull/586) mob-checkpoint STOP fixes — `specs/screens/restaurant_backoffice.yaml` (`order_card_status` node: `items:` binding + honest comment), `restaurant_frontoffice.yaml` registry rename, `restaurant_backoffice.translations.yaml` release-in-progress wording, dedicated `OrderCardStatus` renderer arm + pin test | 0 errors, warnings unchanged |
| 2026-08-16 | **A paid order that nobody answers no longer sits PLACED forever — the worst failure mode in the product finally has a clock on it.** Five minutes after birth (both births: a normal checkout and a $0 replacement — and the FIRST deadline wins; a redelivered birth fact can never push it out, a new `reschedule: keep` semantics built for exactly this), an order still unaccepted records `OrderAcceptanceTimedOut` and moves to a new terminal state `CANCELLED_BY_TIMEOUT`, which also starts its GDPR retention clock like every other terminal. The customer was never charged on this path — capture is on fulfilment, so downstream the authorization is RELEASED, not refunded, and the customer copy says exactly that ("the hold on your card is being released", en+fr — never the word "refund", never a promised bank date); the restaurant's queue shows an explicit "Expired — no response" treatment instead of a silent vanish. **Enforcement ships OFF** (gate-then-stabilize on a money-adjacent path): the full still-PLACED guard runs in shadow on the identical code path, only the append is inert, and the flip is a separate recorded decision gated on the payment-release leg and the customer notice. Alongside: reminder durations become TYPED (`unit: days\|seconds` on the window key — the emitter's `_DAYS` name-suffix parse is deleted), and the customer status screen's map must now key the FULL OrderStatus enum both ways (new validator error), which also fixed a live drift where a bare `CANCELLED` key matched no enum member and every cancelled order fell through to the fallback rendering. **This row records a promise of RECORD, not a live behaviour — and the qualification is not only the gate**: beyond enforcement shipping OFF, no production path enqueues an `Order`-lane birth message at all (the PlaceOrderProcess `deliver:` step appends `OrderPlaced` directly to the event store; the reclamation PM calls `place_replacement_order` in-process), so **no acceptance clock is armed for any real order today**, the birth route is reachable from tests only, and no shadow evidence accumulates — which also makes the shadow-evidence flip precondition unreachable by this mechanism. The producer is tracked as [#588 "The normal checkout path never enqueues OrderPlaced onto the Order lane — the acceptance clock cannot start for saga-appended births"](https://github.com/TheCaptainCompany/captain-food/issues/588) and is the FIFTH flip precondition in the `ENFORCE_ACCEPTANCE_TIMEOUT` gate text; until it lands, a paid order that nobody answers still sits PLACED forever. | 0 | [#167 "No order-acceptance timeout: a paid, unaccepted order sits forever with no alert, cancel or refund"](https://github.com/TheCaptainCompany/captain-food/issues/167) · [PR #586](https://github.com/TheCaptainCompany/captain-food/pull/586) Phases 0-1 (ADR-20260808-195315 §1.3) — `OrderAcceptanceTimedOut` event (orderId only, mirroring OrderExpired), `OrderStatus.CANCELLED_BY_TIMEOUT`, Order reminder + both-birth `schedules:` + record-semantics receive + lifecycle transition/terminal, `ORDER_ACCEPTANCE_TIMEOUT_SECONDS` (300, honest ±promotion-slop precision) + `ENFORCE_ACCEPTANCE_TIMEOUT` (default OFF), 2 rules + 5 behaviour tests (gate both edges on one fixture), status_config completeness fix + `screen-status-config-incomplete` rule, `reminder-window-unit` rule + typed-Duration reminder table | 0 errors, 40 warnings (+1 `event-not-projected`: OrderTracking folds the new event in Phase 3 of the same PR — baseline refreshed in the same commit) |
| 2026-08-15 | **An actor answer can no longer carry a knob nobody reads, or exist without a door to ask through.** The `answers:` grammar's closed key set loses `params:` — refused with a validator error, because the local ask serves a fold of the actor's own stream and a parameter could not change the answer; a key the runtime ignores would be a dead control a future caller trusts. It returns only when a slice gives it semantics, as that slice's recorded decision. And an answering actor must now declare its `mailbox:`, since the typed ask rides the per-actor client — previously a mailbox-less declaration would have silently generated no ask surface at all. Both closed before any spec used either. | 0 | [#582 "Actor `answers:` block + PM `ask:` step — typed request/reply for actor queries, transport stays parked"](https://github.com/TheCaptainCompany/captain-food/issues/582) · [PR #584](https://github.com/TheCaptainCompany/captain-food/pull/584) hardening — `specs/common/actors.yaml` doctrine header (params refusal + mailbox requirement), `ans-params-refused` + widened `ans-shape` + strict `ans-ref-isolation`, [PROP-20260815-142349](proposals/PROP-20260815-142349-actor-answers-block-and-the-ask-step.md) §§0/2/3/8.4 amended to the refused posture | 0 errors, warnings unchanged |
| 2026-08-15 | **An order and a payment can now be ASKED for the facts a settlement decision needs — as a declared, typed contract instead of an ad-hoc fold.** `Order` answers `paymentReference` (the payment intent it was placed with — declared nullable, so a $0 replacement or partner-paid order forces every caller to handle "there is nothing to capture" instead of phantom-capturing) and `Payment` answers `settlementView` (its lifecycle status, the order it settles, whether a capture already failed). Both actors also now DECLARE the write-side state their folds carry — including that a payment's order identity truly comes from the frozen checkout snapshot (a nested, ref-checked lineage), not from a sibling event's copy. A reply is a snapshot whose authority expires at send: the validator refuses any attempt to persist, project or api-expose one, any transport/deadline key on the actor, and any reply property not composed from the actor's own declared state. | 0 | [#582 "Actor `answers:` block + PM `ask:` step — typed request/reply for actor queries, transport stays parked"](https://github.com/TheCaptainCompany/captain-food/issues/582) · [PR #583](https://github.com/TheCaptainCompany/captain-food/pull/583) (actors half; grammar per [PROP-20260815-142349](proposals/PROP-20260815-142349-actor-answers-block-and-the-ask-step.md) §§2-5) — `specs/ordering/actors.yaml` + `specs/payments/actors.yaml` (`state:` + `answers:`), `specs/common/actors.yaml` (answers kind-doctrine header), `ans-*` validator family + implicit lifecycle-status ref + nested payload lineage | 0 errors, warnings unchanged |
| 2026-08-15 | **An out-of-hours checkout can now actually be refused — and BOTH edges of that switch are promises the suite enforces.** The behaviour-test DSL gains `when.gates` (a boolean configuration gate switched ON for one dispatch, as a `$ref` so it can never be a silently-invisible string), and with it the product's guard contract is stated twice over: gate ON, the 04:00 checkout is refused with the next opening slot and the evaluated window as evidence; gate OFF (today's default), the SAME checkout is accepted and the frozen order snapshot records "outside hours" — so when the refusal flips on by its own recorded decision, the log already proves what it would have refused. A live tracking screen also now promises its "open right now" verdict is re-evaluated at every pushed update, never frozen at subscribe time (`ServiceWindow.evaluatedAt` description). | 0 | [#180 "Opening hours are stored, displayed, and never enforced — a customer can order at 04:00"](https://github.com/TheCaptainCompany/captain-food/issues/180) · [PR #576](https://github.com/TheCaptainCompany/captain-food/pull/576) Phase 4 — `specs/tests.yaml` (`when.gates` header grammar, gate ON on `TestPlaceOrderRejectsOutsideServiceHours`, new shadow-mode test + fixture, `TestPaymentIntentCreatedRecorded` when-payload gains the evidence keys its own `then` fixture asserts) + one sentence on `specs/network/api.yaml` `ServiceWindow.evaluatedAt` | 0 errors, warnings unchanged |
| 2026-08-15 | **"Is this restaurant open?" finally exists — and a paid order can be provably judged against it.** Until now opening hours were stored and displayed but enforced nowhere: a customer could pay at 04:00 and nothing in the system even knew the kitchen was shut. The spec now derives a three-valued verdict at the moment you look — open / outside hours / hours never declared — shows it on every restaurant with the exact instant after which to ask again and the real last-order deadline (door-close or the kitchen cutoff, whichever is earlier), and freezes that verdict as evidence onto every accepted checkout, so a disputed 22:40 order can be proved from the log. Refusing an out-of-hours checkout is declared but ships OFF (shadow mode — gate-then-stabilize on the money path): the verdict is computed and recorded on every order, refusing on it is a separate recorded flip. A restaurant that never declared hours is deliberately NOT treated as closed — every activated restaurant today has no way to declare hours, so "closed forever" would silently take the whole marketplace offline; that acceptance is flagged in the register as a consumer-information exposure (pre-contractual time-limit of performance), not cleared. **Honest tier: the stored `CheckoutSnapshot` gains five evidence fields — all optional FOREVER**, because rows frozen before this change must stay deserializable (a required field would orphan already-paid checkouts); the old-shape tolerance has its own behaviour test, which is the whole versioning story. | 0 | [#180 "Opening hours are stored, displayed, and never enforced — a customer can order at 04:00"](https://github.com/TheCaptainCompany/captain-food/issues/180) · [PR #576](https://github.com/TheCaptainCompany/captain-food/pull/576) · DECISIONS §43 RSO-1 (fourth amendment) — `ServiceWindowVerdict` kernel scalar, `Restaurant.serviceWindow` api type, `OutsideServiceHours` error + guard step, `CheckoutSnapshot` evidence fields, `ENFORCE_SERVICE_HOURS_GUARD` (default OFF) + validity-horizon keys, `command.validate` span attribute, 3 rules + 4 behaviour tests + tests-DSL `when.at`, storefront badge re-bound with an explicit render-nothing branch for undeclared hours | 0 errors, 39 warnings (unchanged) |
| 2026-08-15 | **The spec no longer says the product is one deployable, because it is not — and it now says where each capability goes and what it costs to move it.** The service catalog's header carried one sentence, *"V0: every service is `binding: local`, `expose: false` — one deployable, zero internal HTTP"*, written before the bin topology existed. Read today it says something untrue about the shape of the system, and it says nothing at all about what happens next — which is exactly how an intermediate step gets mistaken for a destination. It now says three things instead: a service's binding describes **how the domain reaches one capability**, not how many processes serve the product (the bins already exist); a capability moves out of process by **flipping its own key**, with the address supplied by an environment variable that is an address book and never a decision; and that flip is a topology change rather than a rewrite **because the generated call types already carry their wire encoding while the binding is local** — a local call and a remote one are the same contract, deliberately, so nothing in the catalog has to change shape when one moves. No service moved; none is due to. **Prose only** — no service, operation, binding or type changed, so nothing about what the product does today is different | **0** | `specs/services.yaml` header description. Records: [ADR-20260815-032807](adr/ADR-20260815-032807-opening-hours-and-stock-are-checked-server-side-and-a-big-catalog-snapshots-every-100-events.md) (same commit, different subject) and the Correction §3 of [ADR-20260815-030206](adr/ADR-20260815-030206-a-process-manager-is-a-write-side-component-and-never-reads-the-read-side.md), which names this file's spec-owned `binding:` + unconditional serde as the final-vision shape an actor `answers:` block would copy | green (`make rust`) |
| 2026-08-15 | **The decision that captures a customer's money will no longer be taken on a screen's data.** Today the settlement process asks the customer-facing order-tracking projection — a rebuildable copy, maintained for display — whether there is a payment to capture and what state it is in; if that copy is even a fraction of a second behind, the answer is "nothing to do", and *nothing to do* is final: the order is delivered, the hold silently expires around day seven, and the restaurant is never paid. From now on a process manager is a **write-side** component and takes its facts from the event log itself, the same source the write side used to make them — with two named exceptions that stay honest: operator-authored reference tables (dispatch config, city rankings) are configuration, not a projection, and questions that name no single thing ("which open carts belong to this session?", "what does this whole catalog cost?") have nothing to ask and remain open questions rather than being quietly blessed. Three things follow that a person can check: a refund guard stops comparing the *word* "CAPTURED" and starts comparing the payment's actual state; the two rival definitions of "what is this payment's status" — one owned by the payment itself, one hand-maintained in a display column — collapse to the one that owns it; and the order boundary's read database stops being readable by the write side at all. **Two honest limits stated with it**: this does *not* make capture race-free (the payment's own stream is fed by Stripe on no fixed relation to the order's, so the window shrinks from seconds to microseconds and Stripe idempotency remains the real protection), and it closes **one** of the three storage questions blocking the split, not all three — the other two are held open by ordinary command handlers, which this rule does not touch. **Record-only row (no `specs/**` diff): the spec is now KNOWN to be wrong at the head of the checkout path** — it declares the place-order process reading the cart and restaurant *projections* while the code folds their streams — and that correction is deliberately left to the in-flight work that already owns those lines. | 0 | [ADR-20260815-030206](adr/ADR-20260815-030206-a-process-manager-is-a-write-side-component-and-never-reads-the-read-side.md) · [DECISIONS §42](proposals/DECISIONS.md) rows **PMW-1/PMW-2/PMW-3** + the **STO-9** option **(e)** annotation in §32. **No `specs/**` file changed** — the rule's spec surface (a `read:`-step source grammar and its validator rule) is PMW-1, open, and sequenced behind [#564](https://github.com/TheCaptainCompany/captain-food/issues/564)'s PR1 | not run — docs-only, nothing regenerates |
| 2026-08-15 | **A saga now declares WHERE each of its reads comes from, not only what shape it reads — and those were never the same question.** Some process-manager reads SELECT a projection; others fold the entity's own event stream and never touch the projection they name, because a lagging projector must not be able to check out a stale cart, accept an order for a restaurant that has just paused, or send a courier to an address that has moved. Nothing behaves differently today; what changes is that the difference is now written down per STEP in a form a machine can read, so the reader sets withdrawn as unknowable in the row below can be DERIVED rather than hand-swept a fifth time. | 0 | [#564 "Derive reader sets mechanically: a declared, walkable `reads:` grammar that distinguishes source from shape"](https://github.com/TheCaptainCompany/captain-food/issues/564) phases 1-2 + hardening — new REQUIRED `source: PROJECTION \| EVENT_STREAM` on every PM `read:` step (`specs/{ordering,delivery,payments}/processmanager.yaml`); validator §2b gains `pm-read-source` (required, closed set), `pm-read-key` (the `read:` body's key set is closed, so the next key cannot be invisible the way #413's `tombstone:` was) and `pm-read-projection-database` (a `PROJECTION` step must name a table resolving to exactly ONE database — a `View_*` resolves to none and a `replicated:` table to the read-database SET; both fail closed rather than having a third token invented under time pressure). Counts are deliberately NOT stated here: `the_committed_process_managers_all_declare_a_source` is the live count and cannot go stale. `make generate` moves generated doc comments only — no `source:` consumer exists yet (that is PR2). | 0 errors, warning baseline unchanged |
| 2026-08-15 | **The checkout leg now declares every read that decides what a customer is charged.** It priced each order from the live CATALOG and spent the customer's goodwill CREDIT against the total, and declared neither — so a reader set derived from it would have concluded that checkout needs no catalog access, which at Friday peak is the difference between a working checkout and one that fails closed on every order. Both are declared now, each saying WHY its source is what it is: the catalog is read from the same projection the cart screen prices from, so what is DISPLAYED and what is CHARGED cannot disagree; the credit is folded from its ledger stream, because the projected balance is a running total with no per-order key and so cannot tell a retried checkout from a second one — which would apply the credit twice, undercharging the buyer, silently. | 0 | [#564 "Derive reader sets mechanically"](https://github.com/TheCaptainCompany/captain-food/issues/564) — `specs/ordering/processmanager.yaml` `PlaceOrderProcess`/`PlaceOrder` gains `read: Catalog` (`source: PROJECTION`, at the pricing guards) and `read: CustomerCreditBalance` (`source: EVENT_STREAM`, before the payment call). The leg is in `PM_HAND_WRITTEN_LEGS`, so nothing is generated from it — the declaration IS the change, and `the_checkout_leg_declares_every_read_it_prices_an_order_from` is what stops it being deleted again in one line. Two further reads this leg's CODE performs stay undeclared on purpose (the frozen `Payment` snapshot on the authorization leg; `ReclamationProcess`'s order read): those legs ARE generated, so declaring them emits hooks — a code change, carried to PR2. | 0 errors, warning baseline unchanged |
| 2026-08-14 | **Every table now has a declared home, and forgetting to give the next one a home is a build failure, not a default.** The storage split's last 17 unplaced tables — the order/catalog/customer read models and the seeded pricing/dispatch/posture referentials — are placed by port evidence, closing register row STO-2: the order boundary's read models isolate the customer's order-tracking projector from catalog-import bursts at Friday peak; the pricing policy tables are replicated into every read database, because three different read paths (the Captain-vs-Uber receipt, the catalog's uberPrice, the cart's estimated breakdown) must read them locally or degrade SILENTLY; the dispatch config lives with the write side that alone reads it, and the runtime postures live there because a replay-restore would otherwise silently revert an operator's flip on a money path — a posture's governed set is restricted to no side, so a future non-write-side tenant would put a fail-closed startup read across the wall, which the declaration now records as a tripwire. The validator's refusal ("business placement is an open register row") flips to a requirement: an unplaced business table is now an ERROR, proven against the same resolution the generated inventory is emitted from, so a table can no longer pass validation while silently missing from the inventory. **A declared home is not yet a physical wall, and the record says so**: four open register rows (STO-7 to STO-10) stand between the map and the split, because the WRITE side reads the read models on paths that must not fail — the add-to-cart oversell guard and the checkout repricing read the catalog, the login path's new-vs-returning decision reads the customer read model, and the settlement process reads the order read model for its payment intent immediately before every Stripe capture, so a wall taken without answering it would deliver food and never collect the money. **And the record now says what it does NOT know**: reader sets are declared non-exhaustive rather than complete — four consecutive hand-sweeps each found what the previous missed, while the crossings were already declared in typed DSL, so completeness is withdrawn until it is DERIVED mechanically. Nothing regresses today (one database, no walls); what is bought is that the split can no longer be taken without answering them. | 0 | [#562 "Close STO-2's placement remainder: place the 17 unplaced tables and make placement a validator requirement"](https://github.com/TheCaptainCompany/captain-food/issues/562) · PR [#563](https://github.com/TheCaptainCompany/captain-food/pull/563) — `database:` `$ref`s / `replicated:` on the 17 tables in `specs/database/tables/{projection_tables,referential}.yaml`; `specs/database/databases.yaml` grammar + read_* descriptions updated (stale `ref_*` clause deleted — that family died with ADR-20260728-170000); validator §18 refusal arm → requirement arm; DECISIONS §32 "STO-2 closure" map; fix round — the closure's reader derivation missed `captain_write`'s `CommandDeps` read ports, so the four *"sole reader"* claims are corrected at source and STO-7 is widened / STO-8 opened (placements unchanged); fix round 2 — `RuntimePosture`'s *"write-side by nature"* is retracted as an inference from today's zero tenants and its cross-wall startup read is recorded as a tripwire at the declaration, and the §18 unknown-database diagnostic stops pinning a database count; fix round 3 — a THIRD wall direction recorded (`captain_write` -> `read_order`: the settlement PM's pre-capture `payment_intent_id` read, plus dispatch / reclamation / cart-binding) as new row **STO-9**, the HubRise adapter's `read_common` read filed as **STO-10** (AMBER — it REOPENS ADP-1) and, the headline, **the completeness claim on reader sets withdrawn as a class**: the crossings were already declared in typed DSL (13 PM `read:` steps `$ref`ing this very file), so `projection_tables.yaml` gains a uniform non-exhaustiveness marker, *"sole reader"* is retired everywhere and `read_order`'s *"no other subgraph holds CONNECT"* is rewritten to the honest form | 0 errors |
| 2026-08-14 | **Capture-on-delivered actually fires now — and the customer is no longer told a lie about being refunded.** The #544 posture below shipped INERT: the settlement process reads the order's Stripe authorization from the OrderTracking read model to know what to capture, but the read model's `payment_intent_id` was only written when the CAPTURE folded — the very fact capture was supposed to produce. So every delivered order read a NULL intent, skipped, and the hold silently expired at ~7 days: the restaurant never paid. The fix wires the id from `OrderPlaced` (which already carries it) into the read model at the order's birth, so the guard has something to read. **Three things a person can now see**: **(1)** a delivered or picked-up order captures the held amount, as promised; **(2)** a rejected order tells the customer the truth — *"your card was not charged; the authorization has been released"* — where it previously promised a refund that never existed (a hold is not a charge), and the checkout screen now discloses *"card authorized now, charged on delivery"* rather than framing the hold as an immediate charge; **(3)** an order that is delivered-but-never-settled (courier app dies, the settlement worker wedges) no longer fails in silence — the payment-settlement contract now declares a dead-man's-switch on the AGE of the oldest still-authorized order, so a capture that never even gets ATTEMPTED pages before the 7-day hold quietly expires. The reconciling-sweep RUNTIME that emits that signal is a tracked follow-up; the contract now demands it rather than allowing it to be omitted. | 0 | [#544](https://github.com/TheCaptainCompany/captain-food/issues/544) fix round — `OrderTracking.payment_intent_id` `from:` gains `OrderPlaced/paymentIntentId` (the projector seeds the id at birth, not `None`); `OrderPlaced.paymentIntentId` description corrected (AUTHORIZED, not "captured"); `restaurant_frontoffice` copy `order.status.rejected.body` / `order.status.cancelled.body` corrected + new `checkout.payment.hold_notice` disclosure (screen + `crates/web/checkout.rs`); `observability.yaml` payment-settlement gains `payment_authorized_unsettled_age_seconds` (gauge, dead-man's-switch) + a by-reason alert-routing note; end-to-end DB test through the real projector + saga runner | 0 errors, warning baseline unchanged |
| 2026-08-13 | **The customer's money now moves when the food does, not when they click pay.** Checkout confirmation only HOLDS the amount on the card (an authorization, valid ~7 days); the charge happens at handover — delivered to the door or picked up at the counter — exactly as the founder decided (*"Authorise on checkout. Capture on delivered / picked up"*, [ADR-20260808-195315](adr/ADR-20260808-195315-customer-brief-answers.md) §1.2, re-confirmed 2026-08-13). Three consequences a customer or restaurant can see: **(1)** a rejected or cancelled order before handover simply RELEASES the hold — *"no need to refund because no capture"* (§1.3) — instead of running refund machinery; post-handover aborts still refund. **(2)** A capture that fails AFTER the food is made is the one new failure this posture creates, and it is never silent: a recorded `PaymentCaptureFailed` fact with a typed reason plus a PAGING counter (`payment_capture_failed_total`) — the inverse of the paid-order-nobody-told-about class. **(3)** The capture keys on the PRESENCE of a Captain authorization, never on the delivery fact alone, so an order that never charged through us ($0 replacements today; post-V0 Uber Eats external orders paid on the partner's rails) can never trip a phantom charge. At-table pay-in-advance is the recorded third arm, not built (no such service type yet). **Tier note**: the event shapes change (`PaymentAuthorized`/`PaymentReleased`/`PaymentCaptureFailed` new; `PaymentCaptured` becomes the settlement fact alone) inside the recorded empty-log window ([ADR-20260813-191111](adr/ADR-20260813-191111-the-acceptance-criterion-six-clauses-walked-with-the-front-door-unlocked-from-inside.md) names this the cheap window for exactly this D2 slice) — additive now, nothing stored to upcast. | 0 | [#544](https://github.com/TheCaptainCompany/captain-food/issues/544) — `PaymentStatus` gains `AUTHORIZED`/`RELEASED`; `specs/payments/events.yaml` +3 events; new `PaymentSettlementProcess` (`specs/payments/processmanager.yaml`); `PlaceOrderProcess` materializes on `PaymentAuthorized`; `payment.capture`/`payment.release` service ops; rule `OrderMaterializedOnPaymentCapture` → `OrderMaterializedOnPaymentAuthorization` + 3 new rules; `observability.yaml` `payment-settlement` contract; smoke L4 asserts `requires_capture`/`AUTHORIZED`; OrderTracking `payment_status` folds `PaymentReleased` and its note states the AUTHORIZED seed (landed once the #543 fence on `specs/database/**` lifted mid-run) | 0 errors, 39 warnings (+2 `event-not-projected`, accepted: `PaymentAuthorized` deliberately seeds via OrderPlaced — it precedes the row; `PaymentCaptureFailed` awaits its operator surface) |
| 2026-08-13 | **Where a table lives is now a declared, validated fact — and a partner's data can no longer sit next to the event log by accident.** The storage split's eleven databases (the write-side transactional unit, three read databases, behaviour tracking, and one completely isolated database per adapter) exist as spec declarations with an owning role, a Kubernetes name binding and a recovery posture (`pitr`/`replay`/`refetch`/`backup-required` — so "can we get this back, and how?" is answered where the database is declared, including that HubRise's non-expiring token is the one adapter state a backup must cover). Every staging mirror and credential table now NAMES its database, the write-side tables derive theirs from what the fenced completion transaction requires, and the validator refuses an absent placement, a placement into an undeclared database, and — the error that nearly shipped once — an adapter's table placed outside its own adapter's database. Business read models deliberately stay unplaced: that is register row STO-2's open remainder, and closing it takes a recorded yes, not a spec edit. Nothing is deployed by this: the databases become real in the #494 program's later slices. | 0 | [#494 "Storage boundaries and least-privilege database users"](https://github.com/TheCaptainCompany/captain-food/issues/494) slice 1 · [PR #543](https://github.com/TheCaptainCompany/captain-food/pull/543) — new `specs/database/databases.yaml` (11 declarations); `database:` `$ref`s on the 6 staging + 4 connection tables; `replicated: read-databases` on `ScopeMembership`; validator §18 (`database-placement-*`); new generated inventory `specs/generated/databases.generated.{md,json}` | 0 errors, warning baseline unchanged |
| 2026-08-13 | **A North American phone number can no longer sign up or sign in — and that is the served-country decision finally being said out loud.** The default SMS allowlist served bare `+1` on the reasoning "the US is not a premium range", but a calling code is not a destination: `+1` reaches every NANP territory — twenty-odd Caribbean and Pacific jurisdictions with their own operators and rates, including exactly the premium-payout ranges the allowlist exists to exclude — all billed to us as if they were a Boston number. V0 Tours now serves `+33,+32,+41,+44,+49,+34,+39`, nothing else, and that is the final shape of *which countries does V0 serve* — serving North America would be a market decision nobody has taken, and its shape then is libphonenumber-class region resolution, never a hand-maintained area-code table. The change touches a recorded decision, so the decision record is amended in the same commit rather than contradicted: [ADR-20260813-021500](adr/ADR-20260813-021500-the-allowlist-is-the-economic-control-and-only-a-global-ceiling-bounds-the-bill.md) also gets four false assertions corrected (the ceiling, not the allowlist, is the economic control at V0 scale; the €/SMS price IS recorded — [PROP-20260724-233605](proposals/PROP-20260724-233605-ovh-sms-hook.md), €0.06 HT/SMS France, making the 200/day ceiling €12/day worst case, derivable rather than a guess; the €67.80/month quote belongs to the hosting budget, not SMS; and the real failure mode is a drained prepaid pack = a founder-gated phone-login outage, not an invoice). Owed and named there, not built: an observed-but-not-served telemetry label set (refused `+1` now collapses into `other`) and a credit-balance gauge. | 0 | [#535](https://github.com/TheCaptainCompany/captain-food/issues/535) — `specs/customer/configuration.yaml` `SMS_ALLOWED_DIALING_CODES` default drops `+1` and its `gates:` reasoning is rewritten (a calling code is not a destination); the same correction sweeps the two formal statements of WHY the guard combination works — `specs/customer/rules.yaml` `OtpSendIsGuardedByCountryAndBudget` and `specs/customer/errors.yaml` `PhoneCountryNotServed.description` (customer-facing `messages.en/fr` untouched) — which had kept the "allowlist is the economic control" claim the ADR correction disproves; `make generate` moves the default into the generated per-bin configs | 0 errors, warning baseline unchanged |
| 2026-08-13 | **We now promise a bounded SMS bill, and we promise to say WHY we refused a code.** Asking for a login code used to be unlimited and unowned: anyone could drive it from anywhere in the world, on our own OVHcloud SMS account, and the first anyone would know is the invoice. Three promises replace that. **(1)** We only send to countries we serve — French first, plus the neighbouring low-payout ranges a Tours visitor plausibly carries — and a number we cannot read is refused rather than sent to, so the ranges that actually pay an attacker are simply shut. **(2)** One phone number gets three codes an hour and five a day, whichever door it asks through and however the number is spelled. **(3)** There is a **total** daily ceiling for the whole platform, which stops sending for everyone once it is spent — and that is deliberate: per-number limits do nothing against an attacker who rotates numbers, so a ceiling on the total is the only thing that bounds what we can be made to spend in one night. It can be lowered to zero, or raised, on a live system with no deploy. **And the refusal is now honest**: a customer told to wait sees the server's own countdown, a customer out of codes for today is offered help rather than a timer, and a visitor with a Belgian number is told we do not deliver there yet instead of being told their number is invalid. **Honest tier note**: the YAML diff is Tier 0 (a new table, five new configuration keys, three new refusals, one shared error gaining a field — nothing emitted, stored or promised before), but the change makes a previously-always-accepted request **refusable**, which is a behaviour change any client already in the field will see. That is stated here rather than hidden behind the diff's tier. **What is still owed**: nobody has recorded what one SMS costs us, so the ceiling is a conservative guess wearing a justification — the real €/SMS is a founder input, and the number must be re-derived from it. | 0 | [#516](https://github.com/TheCaptainCompany/captain-food/issues/516) · [ADR-20260813-021500](adr/ADR-20260813-021500-the-allowlist-is-the-economic-control-and-only-a-global-ceiling-bounds-the-bill.md) — new `specs/customer/configuration.yaml` (5 keys); `PhoneCountryNotServed` / `VerificationSendLimitReached` / `VerificationSendCapacityExhausted` in `specs/customer/errors.yaml`; `RateLimited` gains `retryAfterSeconds` + new `RetryAfterSeconds` scalar (`specs/common/`); `throws` on `RequestPhoneVerification` and `RequestPhoneChange`; `sms_send_quota` table + its `sweep_retention()` leg; `OtpSendIsGuardedByCountryAndBudget` rule + its test; four OTP metrics on the `customer-identification` contract. **No new translation keys**: four `auth.error.*` screen strings were drafted and dropped before landing, because they duplicated the `messages.{en,fr}` the refusals already carry in `specs/customer/errors.yaml` — two sources for one localized string is drift waiting to happen, so the server's catalogue stays the single source and the client render path is [#518](https://github.com/TheCaptainCompany/captain-food/issues/518)/[#521](https://github.com/TheCaptainCompany/captain-food/issues/521). | 0 errors, 37 warnings (unchanged) |
| 2026-08-13 | **A token now has to prove it is OURS, not merely that Supabase issued it.** We are about to put every product of the group behind ONE identity provider account, and at that moment the two things our login check leaned on stop distinguishing anything: the audience is a constant every Supabase user everywhere carries, and the issuer becomes identical across sibling products. So a customer of another product in the group would have arrived at Captain.Food as a signed-in customer of ours. From now on a session is accepted only if it carries Captain Food's own claims — under one product-owned key, which the provider's merge cannot let another product write into — and it carries a role we recognise; anything else is refused outright instead of being quietly treated as a customer. The same closes a second door: with the identity project's address unset, the check used to be SKIPPED rather than failed, which is exactly the configuration in which a staging session already worked in production. Nothing changes for a real customer: their session carries these claims from the moment they verify their phone. **Honest tier: this is a Tier 1 conversation, not the Tier 0 the YAML diff alone would suggest** — the change alters the shape written to an EXTERNAL identity provider and therefore invalidates every token already issued, which is the blind spot this page's own note (above) says to apply to the CHANGE rather than to the diff. It lands as a Tier 1 with its versioning story recorded in the ADR, and the story is that there is nothing to version: **Q-L3 (2026-08-12) records no real phone-verified end user**, the only producer of the old shape was `tools/smoke/prod-smoke.sh` (updated in the same change), and the re-stamp is idempotent and automatic. Deploying this against a provider holding real signed-in users would need the read side to accept both shapes for one token lifetime first — deliberately NOT built, because building it would keep the fail-open shape readable for as long as anyone forgot to remove it. | 1 | [#519](https://github.com/TheCaptainCompany/captain-food/issues/519) · [ADR-20260813-013211](adr/ADR-20260813-013211-a-token-must-prove-the-product-not-only-the-provider.md) — `specs/services.yaml` `identity.stamp_customer_claim` now writes `app_metadata.captain_food = { role, customer_id }`; `specs/observability.yaml` `public_credential_degraded_total{reason=role_not_customer}` widened to the "proves no Captain Food role at all" population, and the claim-stamp span comment renamed; `specs/common/configuration.yaml` `SUPABASE_SECRET_KEY`/`EXTERNAL_API_TOKENS` gates renamed to the nested key. Descriptions only — `make generate` moves generated doc comments and nothing else. | 0 errors, 37 warnings (unchanged) |
| 2026-08-13 | The architecture now describes the thing we actually run, not only the thing we are building towards: the deployed server is a declared container with its own generated deployment, so "how Captain.Food is deployed today" is answerable from the repo — and retiring it becomes a recorded step instead of a silent one. | 0 | [#358](https://github.com/TheCaptainCompany/captain-food/issues/358) · [ADR-20260811-004500](adr/ADR-20260811-004500-role-paths-live-on-audience-hosts-api-host-is-a-webhook-address.md) | 0 errors, 37 warnings (unchanged) |
| 2026-08-12 | **A screen can no longer be pointed at the plumbing by accident, and every query that IS served from the plumbing now says so out loud.** Four API surfaces are genuinely served from write-path tables rather than from a read model — the two ADMIN mailbox-supervision screens, the "did my order go through?" poll, and the checkout payment state — and until now the spec expressed that by saying *nothing*: a type with no read model declared was assumed transient, and a one-word flag could have exposed the mailbox, an OAuth token or a saga row to the API with the validator silent. Each of the four now NAMES the table it reads, and naming any other one is refused. Nothing a customer or restaurant sees changes: no field, no query and no generated schema moves. What changes is that retiring an internal table is small again — the last one cost 110 files because nothing recorded who was reading it. | 0 | [ADR-20260812-214500](adr/ADR-20260812-214500-a-read-target-is-declared-not-inferred-the-reads-ownership-wall.md) — new `api.yaml types.*.readsInfrastructure` key on `MailboxLane`, `PoisonedMailboxMessage`, `Operation` (`specs/common/api.yaml`) and `PaymentIntent` (`specs/payments/api.yaml`); `reads:` doctrine header extended. `make generate` produces a byte-identical tree. | 0 errors, 37 warnings (unchanged) |
| 2026-08-12 | **There is now exactly ONE place a write intent is recorded, and one place to ask what happened to it.** Until tonight two tables answered "did my order go through?" — the mailbox for most commands, and a second journal for the three that move money (`placeOrder`, `approveRefund`, `denyRefund`), picked per request by a runtime posture. Both are gone: every command, from any channel, is one addressed actor message on `inbound_messages`, and `operationStatus` has one row to read. The posture that chose between them is deleted too — its OFF position would now mean "take the money and tell nobody", which is not a setting anyone should be able to select. **Honest tier: this is a Tier 1 conversation, not the Tier 0 the YAML diff alone would suggest** — the change DROPS a stored table, and acceptance history stored in it is lost. It lands as a Tier 1 with its versioning story recorded in the migration and the ADR: the log is empty by decision (start-clean) and production is down, so there is no history to lose today, and the migration REFUSES to run if any un-handled command is still sitting in the table. Deploying the same migration against a database with live command history would need an id-preserving backfill first. | 1 | [#242 "Write path becomes an actor mailbox: inbound_messages replaces command_journal + inbound_events"](https://github.com/TheCaptainCompany/captain-food/issues/242) Runtime D · [ADR-20260812-000000](adr/ADR-20260812-000000-the-pm-mailbox-flip-rides-the-journal-retirement.md) — `command_journal` out of `specs/database/tables/journals.yaml`, `CommandJournalStatus`/`CommandChannel` out of `specs/common/scalars.yaml`, the `command_journal` leg out of `sweep_retention.sql`, `worker-journal-sweep` out of `c4-l2` (57 apps -> 56) | 0 errors, 37 warnings (unchanged) |
| 2026-08-11 | **Nothing new — the spec promises one thing LESS, and that thing was never true.** `c4-l2` described the actor pods as scope-isolated: *"drains ONLY its own mailbox lanes — the scoping is the linker"*. Read literally that says an `actor-cart` pod could not reach another aggregate's code even if something told it to. It can: every actor image links every domain crate behind `bin_runtime`, so a lane list ROUTES work, it does not restrict reach. The comment now says which of the two it is, so nobody builds a blast-radius or least-privilege argument on an isolation the build never provided. Nothing emitted changes — it is a YAML comment, and `make generate` produces a byte-identical tree. | 0 | [#475 "Per-bin scope isolation is nominal: every actor/pm/projector bin transitively links all 8 domain scopes…"](https://github.com/TheCaptainCompany/captain-food/issues/475) · [PR #489 "fix(475): the bin manifest scope header states what the build enforces, not what it wishes"](https://github.com/TheCaptainCompany/captain-food/pull/489) | 0 errors, 37 warnings (unchanged) |
| 2026-08-11 | A customer signed in **before** we started stamping their domain id on their token is served the anonymous view on a storefront rather than a half-identity, and that window is now counted as its own thing. It is expected, it lasts one token lifetime after a release, and it must not be read as customers being denied anything — so it no longer bumps the counter that means "someone's account is not properly provisioned". | 0 | [#469](https://github.com/TheCaptainCompany/captain-food/issues/469) — `specs/observability.yaml` read-authorization: `public_credential_degraded_total` gains `reason=claim_absent`, with the contract text saying why it is not `bridge_unresolved` | 0 errors |
| 2026-08-11 | **`current` is now the cart AT THIS STOREFRONT.** A customer with open carts at two restaurants is served — and priced for — the one belonging to the restaurant whose address they are on; the tenant comes from the web address, never from anything the client can assert, and a page that names no restaurant (the marketplace, an unknown address) shows no cart rather than the most recent one from somewhere else. Carts at other restaurants remain readable through `carts`, which is what that query is for. | 0 | [#469](https://github.com/TheCaptainCompany/captain-food/issues/469) — `specs/ordering/api.yaml` `current`: description now states the Host-derived tenant bound on both legs | 0 errors |
| 2026-08-11 | A customer who is signed in on a restaurant's storefront is now recognised there — the anonymous web path READS the credential the browser already sends, so their cart is theirs rather than a stranger's empty one; when that credential cannot be honoured (expired cookie, identity provider down, or a staff token, which stays anonymous on purpose) they are served the anonymous view rather than an error, and **we now count every one of those silent degrades** — so "identified customers are being served anonymous" can no longer look like "customers stopped having carts". | 0 | [#469](https://github.com/TheCaptainCompany/captain-food/issues/469) — `specs/observability.yaml` read-authorization: `public_credential_degraded_total{reason}` | 0 errors |

---

## Status of the gate

The gate that keeps this page current — *if a commit range touches `specs/**` and this file is
unchanged, fail* — is **not yet built**. Its shape is one open decision with four options and a
recommendation, filed as [`DECISIONS.md`](proposals/DECISIONS.md) §26 row **SPEC-1**.

Until it lands, this page is a prose obligation and is therefore exactly as reliable as CLAUDE.md
says prose obligations are. That is a knowingly accepted, short-lived gap, not an oversight: the page
had to exist the day the freeze lifted, and it must not wait on the gate that protects it.
