# 🚦 Captain.Food — Development & Deployment Status

> Hand-maintained snapshot (NOT generated, outside `specs/` so it never affects the DSL).

## 🌐 Deployment

| Piece | Status | Notes |
|---|---|---|
| Render web service (Docker, Frankfurt) | ⏸️ SUSPENDED | Billing-suspended since ~2026-08-04 (`suspenders: ["billing"]`); `captain-food.onrender.com` returns 404. **This is a decided state, not an open incident** — see "Production is DELIBERATELY SUSPENDED" below (ADR-20260817-105844). Blueprint IaC (`render.yaml`) still describes what was live before suspension |
| Supabase Postgres (Frankfurt, eu-central-1) | ⏸️ idle | No live traffic while the Render app is suspended; the team develops/walks against a **local** Postgres stack instead (ADR-20260813-004634) |
| Hosting target — OVH Managed Kubernetes + in-cluster CloudNativePG, GitOps-reconciled | 📋 decided, not built | [ADR-20260807-002705](adr/ADR-20260807-002705-hosting-ovh-mks-cnpg-gitops.md): MKS (Paris), CNPG ≥3 nodes + WAL archiving + restore drills, manifests GENERATED from specs, GitOps-only ops. Realization backlog tracked under [#271](https://github.com/TheCaptainCompany/captain-food/issues/271); the cluster does not exist yet — production cutover is a separate decision to re-take, not a task in flight |
| CI workflow `ci` (build+test+validate+drift; ex `codegen-consistency`) | ✅ | Gates deploys (`autoDeployTrigger: checksPass`); `changes` also runs the decision-lookup stub suite (#679) |
| CI `Claude Code Review` | ✅ | Fires on `opened`/`ready_for_review`/`reopened` — **one pass per presentation, never per push** (ADR-20260826-084500). Re-request = draft → ready |
| CI `db-migrate` (sqlx-cli, gated on green build) | ✅ | Applies `migrations/*.sql` out-of-band (ADR-0043) |
| `/health` (schema-version readiness), `/ping`, `/projector` | ✅ | `>=` version gate; in-process projector |
| GraphQL `/{role}/graphql` + `/{role}/voyager` | ✅ | Role-as-path; per-role filtered schema |
| Custom domains `*.captain.food` (Dynadot wildcard → Render) + Host router | ✅ | Wildcard TLS issued; apex+`www` 301→`join` (GitHub Pages); `hosts.rs` dispatches audiences (`live`/`restos`/`riders`/`system`) + `{slug}` tenants; onrender URL disabled. Recorded in **ADR-0036 amendment (2026-07-18) + ADR-0042** |

## 📖 Read side (queries)

| Query | Status | Notes |
|---|---|---|
| `restaurants` / `restaurant` | ✅ | Real data once SIRENE runs |
| `prospectionPipeline` | ✅ | Admin; fed by SIRENE registrations |
| `pricingPolicy` / `uberEstimationPolicy` / `uberSplitPolicy` | ✅ | **Real seeded data** |
| `catalog` / `categories` | ✅ | **Real nested data** — catalog `tree` projector (categories→products→offers/option-lists + derived `stockStatus`). [#749](https://github.com/TheCaptainCompany/captain-food/issues/749) fixed the storefront MENU paint: `catalog` takes `restaurantId` OR `restaurantSlug` (DSL-declared exactly-one-of, generated check; slug resolves through the SlugAlias path like the tenant host; host-vs-selector disagreement rejects typed), the host-injected `slug` param feeds it through the generated rename bridge, and the `catalog_sections` renderer arm shows the items — pinned end-to-end by `storefront_menu_paint.rs` (real composed schema) and the smoke's L3c menu-paint probe. `categories` (marketplace rail) still requires `restaurantId!` — its §25b skip stands |
| `carts` / `cart` / `orders` / `order` | ✅ wired | Populated as carts/orders are placed |
| `me` / `favoriteRestaurants` | ✅ | `me` resolves the verified ADR-0047 `Principal` → Customer read model; `favoriteRestaurants` joins the customer's favourites |
| Projection worker → registry (per-aggregate checkpoints) | ✅ | In-process; **no batch cap** (drains all pending per tick, loops 1.5s); hardened to **log-skip a poison event** so one bad record can't wedge projection. ⚠️ Free-tier **spin-down** pauses it when the app is idle >15 min → kept warm via **uptimerobot `/ping` every 5 min** |

## ✍️ Write side (mutations)

| Piece | Status | Notes |
|---|---|---|
| `MutationRoot` (all api.yaml mutations generated) | ✅ | |
| Restaurant aggregate (13 commands) | ✅ | Spec invariants (event-stream rehydration) + 25 behaviour tests |
| Cart (3) · Order (11) · DeliveryJob (4) | ✅ | Round 2a — real invariants + tests; **Cart line-checks now enforced** (OfferUnavailable/InsufficientStock/InvalidOptionSelection) via the catalog offer read port |
| Catalog (12) · Prospect (3) · RestaurantAccount (3) | ✅ | Round 2b — real invariants + behaviour tests |
| Customer (14) | ✅ | Wired end-to-end: `customer` read model + Pg repo, fail-closed `AuthProviderGateway` stand-in (real Supabase ACL deferred), injected at the composition root |
| `placeOrder` + process managers (4 sagas) | ✅ wired | `placeOrder` live (fail-closed `PaymentGateway` stand-in); in-process PM runtime (`/saga`) — PlaceOrder/Refund/CartBinding/DeliveryDispatch react to payment/delivery facts → `OrderPlaced`/`OrderDelivered`/… **Real Stripe create-intent = 🅑**; ✅ **checkout-snapshot DSL closed** (ADR-20260719-014434): `PaymentIntentCreated` now carries `checkout` (`CheckoutSnapshot`), frozen by `place_order`, so `OrderPlaced` rebuilds from the log — priced `items`/`breakdown` + retiring the fail-closed `CheckoutSnapshotSource` ride on server-side pricing |
| **Aggregates own the facts — foreign-stream-append isolation, FIRST** | 🚧 plan decided | Founder directive 2026-08-29 (*"We need to resolve this isolation subject first to avoid any new bad development"*), [ADR-20260829-230418](adr/ADR-20260829-230418-aggregates-own-the-facts-isolation-first.md) / register row [AGGREGATES-OWN-THE-FACTS](decisions/AGGREGATES-OWN-THE-FACTS.yaml). The property is the spec's own sentence (`specs/common/processmanager.yaml:7-9`): only the owning aggregate's lane worker appends to its stream; a PM decides, never appends. Plan: **C1 ✅ DONE** — C1a evidence merged as `2408fc73` (PR [#761](https://github.com/TheCaptainCompany/captain-food/pull/761)) and the DEFAULT is **flipped ON by the founder** (*"A — Flip it ON"*, 2026-08-29; [ADR-20260830-012200](adr/ADR-20260830-012200-the-order-birth-routes-through-the-lane.md) — rollback is a config flip back, no redeploy; `ENFORCE_ACCEPTANCE_TIMEOUT` explicitly NOT flipped, precondition 5b still owed production data) → **C2 ✅ MERGED, gated OFF — the live path is still the LEGACY in-process arm** (`eda50a63`, [#595](https://github.com/TheCaptainCompany/captain-food/issues/595) / PR [#762](https://github.com/TheCaptainCompany/captain-food/pull/762)), [ADR-20260830-101500](adr/ADR-20260830-101500-the-polling-runner-owns-a-fenced-leg-transaction.md): the polling runner now owns a fenced LEG transaction (`commit_leg` — staged enqueues + checkpoint advance in one commit, the second AUDITED `TriggerEnvelope::laned` call site), the replacement birth takes the **COMMAND** door on the Order lane (a rejection becomes a supervisable REJECTED verdict; the delivery arms the acceptance clock the spec already declared for `(Order, PlaceReplacementOrder)` — no new spec needed), and the wrapper seam is declared as `sends:` (`pm-sends-kind`/`pm-sends-no-inbox`). Gated `ROUTE_REPLACEMENT_BIRTH_THROUGH_LANE` (`specs/ordering/configuration.yaml:63`) **default `false`**, so `crates/application/src/process_managers/reclamation.rs:157` still calls `place_replacement_order` in process: on merged code the replacement birth is a **live unlaned write to `Order-{id}`** until the flip. The flip and the legacy-arm deletion are separate recorded changes; golden payload equality already measured → **C3-gate ✅ DONE** — [#797](https://github.com/TheCaptainCompany/captain-food/issues/797), [ADR-20260830-204220](adr/ADR-20260830-204220-a-lane-route-declares-its-own-gate-in-the-dsl.md): C3's per-route gates now EXIST as a mechanism. A routed `deliver:`/`sends:` declares its own gate in the DSL (`route_gate:` `$ref` to its `configuration.yaml` key), the codegen emits a `Route` enum + a `RouteGates` struct with no `Default`, and `TriggerEnvelope::lane_sink()` is deleted in favour of `lane_sink_for(Route)`. Before it, the runner handed ONE sink on ONE boolean and every routed step read bare sink presence, so adding a C3 route would have bound it to `ROUTE_REPLACEMENT_BIRTH_THROUGH_LANE` and made its rollback flip an unrelated route too. Neither existing route moved → **C-send ✅ MERGED, gated OFF — the live path is still the LEGACY in-process arm** ([#807](https://github.com/TheCaptainCompany/captain-food/issues/807), [ADR-20260831-093000](adr/ADR-20260831-093000-the-enumeration-is-deliver-and-send-not-deliver-alone.md)): ADR-20260829-230418's enumeration was `deliver:`-only while the property covers **sends**, so the four `send:` steps that write a foreign stream were outside the count and inside the property. `PmStepDef::Send` now consumes `to`/`route_gate` (it carried neither, while all four steps already WROTE `to:` — validated by `pm-send`, then discarded), `route_decls` has its fourth arm, and the three routes (`BindCartToCustomer`→Cart, `GrantCustomerCredit`→CustomerCredit, `MarkOrderDelivered`→Order; the two delivery legs are two triggers for ONE route) reach `ROUTED_LANES` and #783's dead-man's-switch population. Keys **in `specs/common/`** — route gates are KERNEL config, since every PM bin's composition root constructs the whole `RouteGates` struct — all **`default: false`**, legacy arms byte-for-byte (`git diff -w` on the regenerated `process_managers.rs`: **zero deletions**). `pm-route-gate` now covers `send:` steps and, because `to:` is mandatory on a send, **every send must declare its route**. New rule **`pm-send-dedup`**: a routed send NAMES its door's dedup axis, with no default — inheriting the target's identity would have keyed the credit door on `customerId` while the handler is idempotent per `reclamationId`, swallowing every goodwill credit after a customer's first (money owed, never paid, no error raised). Removes the precondition that would have made #764's credit settlement leg a SECOND unlaned writer to `CustomerCredit-{customerId}`. The delivery-completion key's `gates:` also names, as consequence **(e)**, the `OrderExpired` RETENTION CLOCK: a successful COMMAND-door delivery arms the declared `schedules:`, so today's saga-driven completions (a partner's DELIVERED report, an independent rider's completion) arm **no** retention clock while the `markOrderDelivered` mutation does — a legal surface, named in the text the flip decision is made from. **A precondition now blocks BOTH the C2 and C-send flips**: a routed COMMAND door is minted at ENQUEUE, so a REJECTED first attempt permanently closes it ([#811](https://github.com/TheCaptainCompany/captain-food/issues/811) "A routed COMMAND door is minted at ENQUEUE, so a REJECTED first attempt permanently closes it") — `MarkOrderDelivered` and `PlaceReplacementOrder` both reject, and neither route may be flipped until that is resolved. Flips and legacy-arm deletions are separate records → **C3** the twelve remaining `deliver:` steps in target-actor groups (Payment ×7, DeliveryJob ×4, Cart ×1; each adds its own `route_gate:` key; legacy arm deleted separately under golden payload-equality) → **C4** the compiler owns it (EventStore `save` unspellable from a PM). Erasure BUILD is **unblocked** (C1 proved + [PROP-20260829-150752](proposals/PROP-20260829-150752-customer-erasure.md) approved *"A — Approve Option 1"*) |
| Structured typed errors | ✅ | `DomainError::Rejected{code,context}` → GraphQL `extensions.code` + interpolated en/fr message (ADR-20260719-120000) |
| GraphQL **subscriptions** | ✅ | `SubscriptionRoot` + in-process event bus + WS transport + per-role ACL (`orderStatusChanged`/`operationStatusChanged`); works while the app is warm |

## 🔐 Authorization

| Piece | Status | Notes |
|---|---|---|
| Per-role ACL — execution guard + per-role introspection/Voyager | ✅ | Spec-derived from api.yaml `roles` (ADR-0006); role now **verified** by JWT (ADR-0047), so Voyager filtering is trustworthy |
| Per-field ACL on FK-derived nav edges | 📋 | api.yaml has **op-level** `roles` only; needs a DSL extension → **plan mode** |
| EXTERNAL machine callers | ✅ | Pre-shared `X-External-Api-Key` (`EXTERNAL_API_TOKENS`, constant-time) or Supabase JWT w/ captain_role EXTERNAL (ADR-0047) |
| **OTP send guards — country allowlist + per-number caps + global daily ceiling** | ✅ | **#516**, [ADR-20260813-021500](adr/ADR-20260813-021500-the-allowlist-is-the-economic-control-and-only-a-global-ceiling-bounds-the-bill.md). The OTP request endpoint is anonymous BY DESIGN and every send spends money on our **own OVH account** — it previously had **no limit of any kind**. Now: a fail-closed **exact-membership** allowlist (default `+33,+32,+41,+44,+49,+34,+39` — the served-country decision made executable; bare `+1` was dropped 2026-08-13, #535, because a calling code is not a destination: `+1` reaches every NANP territory, premium Caribbean ranges included, all billed like a Boston number), 3/hour + 5/day per **canonical** number with a 30s→2min→10min cooldown, and a **global daily ceiling with a no-deploy kill switch** (`UPDATE sms_send_quota SET sent_count=999999 WHERE quota_key='global:day'`) — the only guard that bounds the total bill, since an attacker rotates numbers. The counter is **shared** (`sms_send_quota`; one atomic `INSERT…ON CONFLICT…WHERE`, because there is no per-phone actor lane and that statement is the only serialisation). **The wall is `/auth/sms-hook`**, where the euro is actually spent; the identity ACL only *sheds*. Compiler-enforced: `OvhSmsClient::send` takes an unforgeable `AuthorizedSmsRecipient` **by value**, so one claim buys exactly one send (a `&`-borrow would let a loop spend the whole budget on a single claim). Refusals are four **typed** states a client can tell apart, rendered from the server's own `errors.yaml` `messages.{en,fr}` — the single source; there is deliberately no second client-side copy of that string, and the client render path itself is #518/#521. Liveness is an **observable** gauge (`otp_send_guard_enforcing`) re-asserted on every export cycle and re-declared where enforcement is decided, not stamped once at boot. **The 200/day ceiling is derivable, not a guess**: OVH SMS France is €0.06 HT/SMS ([PROP-20260724-233605](proposals/PROP-20260724-233605-ovh-sms-hook.md), founder-approved 2026-07-24, screenshot-confirmed) → €12/day worst case France-rated; still unknown are OVH's per-destination multipliers and which pack was purchased. The account is a **prepaid pack**, so the real failure mode is a drained pack = a founder-gated phone-login outage, not an invoice (#535 corrections in [ADR-20260813-021500](adr/ADR-20260813-021500-the-allowlist-is-the-economic-control-and-only-a-global-ceiling-bounds-the-bill.md)). **The observed-but-not-served telemetry gap is closed (#696)**: `otp_send_refused_total` now carries a closed, hand-declared `region` attribute (`north_america`/`non_eu_europe`/`rest_of_world`), so a refused `+1` is `north_america` instead of collapsing into the same bucket as every other unserved code, with no widening of the attacker-mintable label set. **Still owed: the credit-balance gauge** — `ovh_sms_credit_balance` is DECLARED in `specs/observability.yaml` but not yet emitted, tracked by [#699](https://github.com/TheCaptainCompany/captain-food/issues/699). **OVH SMS facts recorded (2026-08-29, founder console): a 500-SMS pack with 500 remaining** — the pack is untouched, so there is no real OTP traffic yet; the #699 gauge is **PRE-LAUNCH** work (at the 200/day worst case the pack drains in 2.5 days). Alert shape banked (observability lens): warn ≤300, page ≤100, plus a staleness dead-man ([ADR-20260829-145848](adr/ADR-20260829-145848-the-founders-answer-sheet-of-2026-08-29.md)). |
| **STAFF-AUTH parts A+B — the RIDER identity fold, and `Identity::Unbound` cannot satisfy a role guard** | ✅ | [#639](https://github.com/TheCaptainCompany/captain-food/issues/639), [ADR-20260830-191457](adr/ADR-20260830-191457-a-role-guard-takes-a-witness-and-an-unbound-caller-is-recorded-as-public.md), implementing ADR-20260818-094500 / -101500 / -004646. **B closes ADR-20260818-004646's Correction 3 at the edge**: `RoleGuard` used to test membership of a `RequestRole` that `routes.rs` parsed out of the URL PATH and never consulted the verified `Principal`, so a token asserting `captain_food.role = "RESTAURANT"` with no `restaurant_id` (`Identity::Unbound`) passed `approveRefund` — which resolves its actor from the payload's `orderId`, never from the caller — and could approve ANY pending refund. The guard's input is now an `ActingRole`: a private-field newtype in a CHILD MODULE of `auth` (the `FetchIntent` shape — privacy is module-scoped, not type-scoped), whose only producer owns the `Identity` match and whose `Unbound` arm yields PUBLIC. It is the ONLY role value in the GraphQL context now (the bare `RequestRole` injection is gone, and the four ad-hoc `data_opt::<RequestRole>() == Admin` sites in the generated resolvers were swept onto it). **The independent review caught the sweep incomplete**: it matched the literal `.data(RequestRole::X)` and missed three variable-bound `.data(role)` sites inside `for role in ...` loops, which stayed GREEN because an absent role fails closed to PUBLIC and PUBLIC is refused too — three role-refusal loops asserting nothing, one already caught once at 4-of-6 coverage on #536 and now at 0-of-6. Fixed, and the recurrence class is held by `crates/server/tests/role_injection_gate.rs`, a source scan (the compiler cannot reach it: `async_graphql::Data::insert` is `TypeId`-keyed over `Any`, so the wrong type is neither an error nor a warning), seen red against the exact mutant. **Forgetting to inject it is a compile error**, not a silent 403: the witness is a tuple element of `authorize_and_resolve_scope`, which both GraphQL transports destructure; the THIRD transport (`web_ssr.rs`, found at briefing by graphql-architect — the card named two) derives its own from `Principal::anonymous()`. `Principal::role()` became `recorded_role()` and **`Unbound` now records PUBLIC** in `domain_events.user_type` — the asserted role was a false author in an immutable log. Historical rows are UNTOUCHED (they are the evidence the defect existed); the newly-inhabited pair `(PUBLIC, user_id = <sub>)` is recorded in `specs/common/scalars.yaml#/UserType`. **A ships the `Rider` read model** (`read_common`, `internal: true`, `recovery: replay`) carrying the `auth_ref -> rider_id` mapping ADR-20260818-004646 puts in our Postgres: `auth_ref` NOT NULL **UNIQUE** (not indexed — `fetch_optional` returns an arbitrary row on multiplicity, and ScopeMembership keys grants on `member_id = rider_id`, so a duplicate is one rider holding another's order scope), `display_name`/`phone` NOT NULL (the projector emitter branches on the COLUMN's nullability, so a nullable column blind-overwrites and a phone-only `RiderInfoUpdated` would erase the name — caught by `rustc` at the store, one layer below the test), `phone` unconstrained because `authRef` is the key precisely so a recycled French mobile number never inherits an account. **NOT closed, and a green branch must not be read as more**: the money path is still unbound (`other-restaurant ⇒ denied` is false everywhere — `approveRefund` has no ownership comparison and its source is still open between `PaymentState.restaurant_id` and a `refund_process_manager` column), NO RIDER CAN SIGN IN (part A is a fold with no consumer; the write-side `authRef` reservation is designed and unbuilt), and an incompletely-provisioned restaurateur still gets *"role PUBLIC is not authorized"* in English over an empty order queue. **Part C (staff authentication) is NOT claimed** — AMBER, and its **proposal now exists**: [PROP-20260831-180622](proposals/PROP-20260831-180622-staff-authentication-the-roster-the-invitation-and-the-door.md), `Proposed`, `HOLD: human`, **five** unchecked Concerns (four at #799 time; `one-subject-one-role` registered by #852 — which mechanically block `Approved`). It presents three forks unresolved — invitation/membership identity, check-or-lock for an act on another aggregate, and where the sign-in door lives — and corrects the design's headline claim: `UUIDv5(scopeType|scopeId|memberType|authSubject)` is **not** the derivation `ScopeMembership.membership_id` uses (same shape, different value) and contradicts that column's own rule, *"the DOMAIN id … **never** the auth subject"* (`projection_tables.yaml:1181`); the route to the "rename, not a join" property is a person id bridged from the subject. It also records that a `PUBLIC` operation is **not reachable from a staff surface** (`Surface::role()` is one role per surface, `crates/web/src/router.rs:57`; 9/9 and 2/2 screens `requires_auth: true`; a role-excluded control is `SkipReason::RoleRefused` — skipped silently, not refused loudly), so part C needs a renderer capability that does not exist. Landed via [PR #799 "#639 parts A+B — a role guard takes a witness, and the RIDER role gets an identity to mint one from"](https://github.com/TheCaptainCompany/captain-food/pull/799), squashed as `4ddad98`. **Step 1 of part C has since landed and [PRINCIPALS-MEMBER](decisions/PRINCIPALS-MEMBER.yaml) is CLOSED** ([ADR-20260831-220559](adr/ADR-20260831-220559-the-person-is-a-principalkind-not-an-eighth-usertype.md), PR [#835](https://github.com/TheCaptainCompany/captain-food/pull/835)): the person is a `PrincipalKind` (`CUSTOMER|RESTAURANT|RESTAURANT_ACCOUNT|RIDER|MEMBER`), a NEW scalar with no stored history, and **`UserType` does not widen** — `emit/bins.rs`'s `user_type_roles` emits one gateway bin and one `/{path}/graphql` route per member of that enum, so an eighth value would mint an eighth URL surface out of naming a person; `RESTAURANT` therefore stays a legal `UserType` value forever and is never renamed (ADR-0041). `ScopeType` untouched; `requires.acting`'s membership semantics are stated but NOT built (zero `requires:` blocks exist in any `specs/*/api.yaml` — #636 owns the emitter). `AuthSubject`/`MemberId` minted in `specs/common/` and **seven** `authRef` sites moved off `ExternalReference` — the HubRise catalog-ref scalar, which had been typing customers' and riders' login credentials — three of them in `specs/services.yaml` that the proposal's four-site enumeration missed and without which the retype does not compile. Zero-byte SQL diff, purely additive GraphQL, no migration. **Step 2a has since landed on the branch** (PR [#846](https://github.com/TheCaptainCompany/captain-food/pull/846), `HOLD: human`, [#794](https://github.com/TheCaptainCompany/captain-food/issues/794)): the write-side `auth_subject_reservations` table — composite key `(principal_kind, auth_subject)`, `principal_id` typed by the kind, **no release path** — with `register_rider` reserving before its append and refusing with the typed `RiderAuthSubjectAlreadyBound`; the "designed and unbuilt" reservation named above is built, and the population invariant is now decided by Postgres before the fact is recorded. **2b — the door — has since landed on the branch too** (PR [#849](https://github.com/TheCaptainCompany/captain-food/pull/849), `HOLD: human`): the RIDER seam resolves `auth_ref -> rider_id` from the `Rider` read model on every request and WS connect through `RiderIdentityRepository` / `PgRiderRepository` (born `AuthSubject`, one column, no `LIMIT 1`), with **no `Claim` arm and no OFF state** — `Identity::Rider { sub }` has no id field and `ProductClaims` parses no `rider_id`, so binding the claim is unspellable rather than merely unread; the §10 pair was seen red first (and the *try-Postgres-else-claim* shape seen passing (a) and failing (b)), mirrored at the WS seam, and the outcomes ride their own `rider-identity` contract. Build-order row 2 is LANDED. **Still true after 2b: end-to-end, no rider can sign in**, because nothing mints a `role: RIDER` token — the sole claim stamper hardcodes CUSTOMER. **2b as first pushed REGRESSED part B for RIDER** (the independent reviewer's one blocking finding on #849): the witness was minted from `Identity::Rider` BEFORE the seam resolved, so a bare `role: RIDER` JWT with no `Rider` row read `Public` and still ACTED as RIDER on all five `ALLOW_RIDER` guards (`acceptDelivery`, `confirmPickup`, `completeDelivery`, `changeRiderStatus`, `markOrderDelivered`) and RECORDED `RIDER` in `domain_events.user_type` — `AcceptDelivery` takes its target from the payload (`deliveryJobId`, `riderId`), never from the caller, so that caller could enqueue an acceptance naming any rider, the exact shape ADR-20260830-191457 closed for RESTAURANT. The `graphql_acl.rs` assertion had been rewritten to match the runtime, and the PR called the result "the customer seam's own asymmetry, named not widened" — inaccurate: the customer asymmetry needs a stamped `customer_id` claim and sits behind `RESOLVE_CUSTOMER_IDENTITY_FROM_POSTGRES` (default OFF), while the rider arm needed no claim and had no gate — strictly wider. **The confirming re-presentation restores unbound ⇒ denied on both halves**: the verifier yields `Identity::Unbound { role: RIDER }` for every rider token (a token cannot prove a binding it never carries), `resolve_read_scope` consumes the principal and hands back one whose identity IS the seam's outcome (`Identity::Rider` only when a row answered — its sole producer — `Unbound` otherwise), and `authorize_and_resolve_scope` mints the witness from THAT principal, after the seam; `ActingRole` keeps its one private constructor and stays the tuple element both transports destructure. Pinned by `crates/server/tests/rider_without_a_row_is_forbidden_on_the_write_half.rs` (a signed ES256 bare-`RIDER` JWT through the real `POST /rider/graphql` and a scripted no-row seam → FORBIDDEN as PUBLIC, `LookupFailed` likewise, with the resolved-row control passing the guard; seen red first) and by `graphql_acl.rs` with RIDER back in the can-be-unbound class plus the seam-driven pair (no row ⇒ acts PUBLIC, records PUBLIC; a row ⇒ acts RIDER, records RIDER). **Steps 3–7 remain unstarted and the proposal's five Concerns unchecked**; the `by_auth_ref` read port still takes an `ExternalReference` (step 1b — it reaches the codegen emitter and a file fenced to a concurrent session; **ten** edit sites, since retyping a trait parameter forces every `impl` signature and every hand-written caller, including the gated `RESOLVE_CUSTOMER_IDENTITY_FROM_POSTGRES` resolver at `crates/server/src/auth.rs:2181` — tracked by [#836 "#639 part C step 1b: retype the by_auth_ref read port to AuthSubject (ten sites, incl. the gated auth.rs resolver)"](https://github.com/TheCaptainCompany/captain-food/issues/836)). **2c-i — the credential — has since landed on the branch** (PR [#852](https://github.com/TheCaptainCompany/captain-food/pull/852), `HOLD: human`; build-order row **2c** inserted in the proposal, rows 3 and 4 now depend on it): the platform ISSUES a `role: RIDER` token, to riders only. `identity.stamp_rider_claim` / `stamp_rider_put_body()` is a SECOND hardcoded stamper (`{ role: RIDER }` and nothing else — no id, ADR-20260830-234532; a distinct function and port method, never a parameter on the customer's, ADR-20260818-101500), pinned by `the_verifier_reads_what_the_rider_stamp_writes`; the identify-only `roles: [PUBLIC]` pair `requestRiderSignInCode` / `confirmRiderSignIn` rides the `Rider` lane emitting nothing — the request leg never consults the rider read model (no enumeration oracle), the confirm leg identifies through the 2b `RiderIdentityRepository` bridge, REFUSES an unknown phone (`RiderNotRegistered`, naming `SUPPORT_CONTACT` — now a declared required key with no default, baked `support@captain.food`) and creates nothing, refuses a subject already carrying another claim object (`AuthSubjectHoldsAnotherRole` — the **`one-subject-one-role` Concern**, registered on the proposal and NOT decided: fail closed, never an overwrite), then stamps → rotates → parks the post-stamp session for `POST /auth/session`. The OTP vocabulary (`DialingCode`/`NationalPhoneNumber`/`OtpCode`, five refusals) is promoted to `specs/common/` because two doors throw it. **The independent review's one blocking finding (B1) is applied on the branch**: a confirm carrying no `X-SESSION-ID` used to park an OWNERLESS session (the store's both-`None` claim, another channel's contract) that any header-less `POST /auth/session` holding the messageId could take; it is now refused with the typed `RiderSignInRequiresSession` BEFORE the OTP is spent — nothing verified, stamped or parked, seen red through the real transport (door test (f)) — so a parked rider session always has an owner. The post-verify provider-failure finding is [#853](https://github.com/TheCaptainCompany/captain-food/issues/853). **Still true after 2c-i: no rider can sign in from the rider APP** — the door is GraphQL-reachable only until 2c-ii lands R1's per-screen PUBLIC capability and the sign-in screen (a staff surface cannot reach a PUBLIC operation). **2c-ii — the screen and R1 — has since landed on the branch** (PR [#854](https://github.com/TheCaptainCompany/captain-food/pull/854), `HOLD: human`; build-order row 2c LANDED): `graphql_role: <UserType>` on a screen (validator §26 — the role must be admitted by the screen's `roles` and by every operation it binds, `PUBLIC` only with `requires_auth: false`; seen RED against a planted violation) → `Surface::role_for(screen)` selects the transport for reads, writes and the push socket; the rider `/sign-in` screen (`roles: [PUBLIC]`, phone + six-digit code, `claim_session` then `/`); `jobs`/`job_detail` declare `unauthenticated: navigate /sign-in` (server 302 on a cookie-less GET, client navigate on a 401); refusals render inline in the caller's language because `Operation.message` is now localized at read time from the row's typed context (`RequestLocale`, both legs). **A rider can now sign in end to end from the rider app.** Adjacent, filed for the architect: the general audience rule (`screen.roles ⊆ ∩(op roles)`) is a WARNING at exactly two pre-existing dead controls (`deliveries_board` × `escalateDelivery` for RESTAURANT_ACCOUNT; storefront `restaurant` × `markRestaurantAsFavorite` for PUBLIC); no story→screen `$ref` grammar exists; the customer sheet's `send_otp` still passes one `phone` variable where the command requires `dialingCode` + `nationalNumber`. **Step 3 was decided by the team on 2026-09-04** ([ADR-20260904-015903](adr/ADR-20260904-015903-the-custody-doors-are-a-new-fact-a-rider-hands-a-job-back-with-the-food-s-whereabouts-and-the-read-models-fold-it.md), thirteen lenses, option (c) — a NEW handback fact; the card's "GREEN once additive" was false at HEAD) and split into **3-i** (the issue doors) and **3-ii** (the handback), both `HOLD: human`. **3-i — the issue doors — has since landed on the branch** ([PR #864 "#639 part C step 3-i: the issue doors (report, resolve, decline) and the read model that tells the restaurant"](https://github.com/TheCaptainCompany/captain-food/pull/864), `HOLD: human`, executor tier **sonnet** — the first lower-tier PR, ADR-20260904-013450): `ReportDeliveryIssue` carries a closed `DeliveryIssueKind` (required) + an optional 300-char note (the D2 pattern, ADR-20260808-171056), `ResolveDeliveryIssue` a closed `DeliveryIssueResolution` + note; the stored `DeliveryIssueReported`/`DeliveryIssueResolved` gain the kinds NULLABLE (additive, readers first — no field renamed or removed); three additive mutations with sets that start narrow (`reportDeliveryIssue` [RIDER, ADMIN], `resolveDeliveryIssue` [RESTAURANT, RESTAURANT_ACCOUNT, ADMIN], `declineDelivery` [RIDER]); **the restaurant is told through the read model**: `View_DeliveryJob.open_issue_kind` folds the reported kind and a `DeliveryIssueResolved` CLEARS it via the `derive:` grammar's new explicit `null` (`DeriveVal::Null` → `THEN NULL`; a YAML null used to be silently skipped — closed by `view-derive-value-unknown`), shipped as the hand-written migration `20260904021500_delivery_job_open_issue.sql` (view body verbatim from the generated SQL, `DROP VIEW` first because the emitter trails `created_at`/`updated_at`) + its `include_str!` chain entry, seen RED first against the applied DDL (#861's stand-in) and RED again under the named mutant; the rider `job_detail` [Un problème] → `rider_issue_sheet` (report-only path, six one-tap kind chips, OTHER reveals the note, `inline_error`, no call button — SUPPORT-CONTACT), `jobs` [Refuser] on a PENDING offer, the board's pinned issue card + `issue_resolution_sheet` (the board's read is still `skipped_reads`, #745/#749); validator `api-operation-key` closes the operation key set per section (step 4's `whileRestricted` seam); story steps under `Deliver` and `TrackDeliveries`; the `DeliveryIssue` fold + `delivery_issue_rate` on the rider's `Deliver` activity (its first metric). **Not done, by design**: `riderId` is not bound on the rider screens (no rider-identity root exists on that surface; the 2b seam resolves the caller server-side, the handlers still take the target from the payload — an adjacent finding for the architect); **3-ii — the handback — has since landed on the branch** ([PR #870 "#639 part C step 3-ii: the handback — a rider hands a job back with the food's whereabouts, the read models fold it"](https://github.com/TheCaptainCompany/captain-food/pull/870), `HOLD: human`, executor tier **sonnet**, ADR-20260904-015903): `HandBackDelivery { deliveryJobId, riderId, foodLocation }` → `DeliveryHandedBackByRider` (no `reason` — the narrative the restriction ADR made unspellable stays unspellable here too), `foodLocation` a new dedicated scalar `FoodCustody` (NOT_COLLECTED | RETURNED_TO_RESTAURANT | WITH_RIDER); custody-keyed transitions from ASSIGNED (NOT_COLLECTED → PENDING) and PICKED_UP/OUT_FOR_DELIVERY (RETURNED_TO_RESTAURANT → PENDING, WITH_RIDER → FAILED, never re-offered while a restricted rider's bag still holds the food). **The lifecycle `via:` grammar (ADR-20260721-093027) is extended**, needed and used for the first time on a field that is NOT the aggregate's own status scalar: an optional `when:` sibling lets several transition rows on one event collectively form an explicit value→target map (`crates/domain/src/generated/lifecycles.rs`'s `transition`/`target`/`initial`), mirrored on the view-projection side by `derive: { from, map }` (`DeriveVal::Mapped`, four new validator checks incl. exhaustiveness) — `View_DeliveryJob.status`/`provider`/`rider_id` fold the handback (`rider_id`/`provider` RESET to null, the fold-reset proof `TestDeliveryReofferedAfterHandBack` needs), new `food_location`/`handed_back_at` columns; `OrderTracking.delivery_status`/`courier` gain the same arm through the EXISTING hand-written `Compute` hook (Complex-classified, no `derive:` needed there). Shipped as the hand-written migration `20260904060000_delivery_job_handback.sql` (view body verbatim, `DROP VIEW` first) + its `include_str!` chain entry, seen RED first against the applied DDL. The ONE fenced arm lands in `crates/infrastructure/src/inbox.rs` (ADR-20260904-015903 §10, the isolation programme's #780/#783 antecedent) — nothing else in that file; a NON-FENCED dead-man's-switch worker (`delivery_handback_watch.rs`, beside `delivery_offer_timeout_worker.rs`) emits `delivery_handed_back_unreassigned_age_seconds` (the `custody-handback` observability contract, mirroring #608's shape) — a handed-back job with no later re-offer, since the re-offer PM step is itself FENCED and deferred ([#860](https://github.com/TheCaptainCompany/captain-food/issues/860)). The rider `rider_issue_sheet` gains a second exit ("Je ne peux pas continuer") asking the food question only at PICKED_UP/OUT_FOR_DELIVERY (derived NOT_COLLECTED from ASSIGNED) — **corrected in review round 2 on #870**: as first landed, the sheet's content sat behind a FORM-FIELD `visible_when` (`issue_exit.value`), which the renderer can never satisfy (it reads resolver data only) and the driver never re-evaluates after a chip pick, so BOTH exits rendered nothing at all (regressing 3-i's report door too); split into a two-button router (a real SDUI `open_bottom_sheet` edge per exit) into two content sheets gated only by resolver data (`delivery.status`); the board's pinned handback card (`deliveries_board`) headlines where the food is; the customer `order_tracking` page (`sdui: false`, bespoke Leptos) replaces the ETA bar with a facts-only notice while `order.deliveryHandedBack` is true — **corrected in review round 2 on #870**: the original predicate compared `order.status` to `OUT_FOR_DELIVERY`, a token no OrderStatus producer ever emits, and read a SEPARATE `delivery.byOrder` query the push path never refreshed, so the banner never rendered in production at all; the flag now lives on the SAME `OrderTracking` row (`delivery_handed_back`, new column) the pushed `Order` frame already carries. **Not done, by design, and flagged for the HOLD reviewer**: the ADR's "report + hand back" two-Tells sequencing (one control firing BOTH `reportDeliveryIssue` and `handBackDelivery`) is not wired — the SDUI `on_success` grammar has no mutation-chaining primitive, and shipping a control that silently fails half its job is worse than a narrower one that fully works (CLAUDE.md); the confirm button dispatches `handBackDelivery` alone (the safety-critical custody fact), and the board's `[Vu]`/`resolveDeliveryIssue` is gated on `delivery.openIssue != null` so it never renders against a job with nothing to acknowledge. The `DeliveryHandback` business-metric fold + `handback_rate`/`time_to_reassign_ms` land on the rider's `Deliver` activity (its second metric, after 3-i's `delivery_issue_rate`); the re-offer is [#860](https://github.com/TheCaptainCompany/captain-food/issues/860), the drift gate [#861](https://github.com/TheCaptainCompany/captain-food/issues/861). **That adjacent finding is CLOSED** ([#865 "The rider surface has no rider-identity root…"](https://github.com/TheCaptainCompany/captain-food/issues/865), `HOLD: human`, executor tier **sonnet**, PROP-171500 D2): every rider-facing mutation (`acceptDelivery`, `confirmPickup`, `completeDelivery`, `declineDelivery`, `reportDeliveryIssue`, `changeRiderStatus`) declares `derived: { riderId: rider }` — the generated Input types no longer carry `riderId` on the wire at all, and the resolver derives it from `ReadScope::Rider` at the seam instead, failing closed (`errors.yaml#/Forbidden`) when a required derived property's scope did not resolve. `changeRiderStatus` narrows to `[RIDER]` (a rider's own toggle; lifecycle restriction is ADMIN's through `RestrictRider`, ADR-20260904-014136 §Decision 6(i)). New validator section §28 (`api-derived-field-unknown` / `api-derived-type-mismatch` / `api-derived-role-mismatch`), and `action-missing-required-input`/`action-unknown-input` both narrow (the rider screens' `accept_delivery`/`confirm_pickup`/`complete_delivery` controls are now actually submittable, rebound from the unknown `orderId` to `deliveryJobId`). D1 keeps the aggregate-side compare as the authority where a rider is already assigned (`TestConfirmPickupByAnotherRiderIsRejected` / `TestCompleteDeliveryByAnotherRiderIsRejected` added); the seam is the only guard on a PENDING job with no rider yet. **Step 4 (rider restriction) was decided by the team on 2026-09-04** ([ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md), thirteen lenses, `HOLD: human`) into three slices, one train: **4-i** (the fact, the standing, the human-only doors), **4-ii** (the restricted rider is told — the notice screen, gated before ANY production `RestrictRider`), **4-iii** (the admin's hands — the roster surface). **4-i is drafted on [PR #875](https://github.com/TheCaptainCompany/captain-food/pull/875), still DRAFT pending the team's independent-reviewer pass (`HOLD: human` — never the founder)**: `Rider.standing` is a NEW column, a GRANT on the identity row orthogonal to availability (`RiderStatus`) — an OFFLINE rider is ACTIVE, a RESTRICTED rider may legitimately be ON_DELIVERY holding food. `RestrictRider`/`ReinstateRider` are `roles: [ADMIN]`, `requires: acting: { ADMIN: any }`, no EXTERNAL key, and a new validator rule `pm-sends-human-only-command` makes it an ERROR for any process manager to `send` either — a human decides, in three enforced layers. The four grounds (`RIDER_REQUESTED`, `ELIGIBILITY_DOCUMENT_LAPSED`, `IDENTITY_MISMATCH`, `ACCOUNT_COMPROMISE`, additive-only per [ADR-20260904-014136](adr/ADR-20260904-014136-rider-restriction-ships-now-with-the-smallest-closed-set-of-grounds-and-counsel-can-only-add.md)) decode through a NEW `readOnlyCatchAll: UNRECOGNISED` scalar attribute — an unknown stored ground tolerates decode (never blocking `ReinstateRider`, never leaving the projector's fold-fault-and-skip stale-grant hole) but is unspellable at every write door and off OTLP. A restricted rider is refused everywhere except a NEW `whileRestricted:` carve-out grammar names on exactly `{ myStanding, delivery, reportDeliveryIssue, handBackDelivery }` — never `myDeliveries` (would hand a compromised session the unassigned-jobs address book). `ReadScope::Rider` becomes the struct variant `{ id, standing }` (compiler-first — a guard that ignored standing would not compile), resolved from ONE `SELECT rider_id, standing`. `SUSPENDED` is retired from every write door (`ChangeRiderStatus` now takes the narrower `RiderAvailabilityTarget`); the retired lifecycle entry edges use a NEW `legacyStates:` key that exempts a legacy exit from the reachability gate without deleting the historical value. `standing`'s creating arm (`RiderRegistered`) never writes it — the Compute hook returns the PRIOR row's value when one exists, so a checkpoint-reset replay-in-place re-grants nobody (verified live against 8 planted mutants, all reverted; see [journal-2026-W36.md](status/journal-2026-W36.md) for the verbatim reds). A DB-gated walk test (`rider_standing_walk.rs`, section D "farley") drives the full restrict/deny/carve-out/reinstate cycle through the REAL router, mailbox and projector (never a scripted seam), and two new observability tests pin `rider_restricted_denied_total` (fires once, no `rider_id` label) and `rider_standing_lag_positions` (reads 0 once caught up — the ">0 while behind" half is a genuine, PRE-EXISTING gap in the shared `drain_group` loop, flagged for the architect rather than fixed here). Gates: `cargo build --workspace`/`--tests`, `make validate` (0 errors), `make rust` (0 errors), 409 codegen unit tests, `make test-crates` isolated-DB (1649 passed, 0 failed), CI-narrowed clippy (exit 0), all DB-gated projection + server ACL/guard/walk/metric suites green. **Not this slice, by design**: the SMS notice (4-ii, gates any production restriction), the admin roster screen (4-iii); `TestRiderStatusChangeTargetCannotSpellSuspended` named on the dispatch card could not be added to `tests.yaml` as named (`SUSPENDED` is not a legal `RiderAvailabilityTarget` value, so no fixture can spell it) — the equivalent coverage is the server-side static-validation test instead (card defect, attributed in the journal). **4-ii is drafted on [PR #882](https://github.com/TheCaptainCompany/captain-food/pull/882), still DRAFT pending the team's independent-reviewer pass (`HOLD: human` — never the founder)**: a restricted rider's next refused read or Tell now bounces to a dedicated `/restricted` screen ([ADR-20260904-124600](adr/ADR-20260904-124600-the-restricted-rider-is-told-on-the-client-leg-first-keyed-on-the-server-s-own-reason-and-the-page-get-leg-rides-with-the-socket.md), amending ADR-081527 §11 in part — the document-GET bounce rides with step 5's socket instead). `StandingGuard` adds the ADDITIVE `extensions.reason` (the shared `shared_types::RIDER_RESTRICTED` constant) beside the UNCHANGED `code: FORBIDDEN`; `RoleGuard` adds nothing (both asserted, both directions). The web transport parses `extensions` into a typed shape ONCE at the boundary (`crates/web/src/graphql.rs::ErrorExtensions`) instead of stringifying the errors array for classification, as it did before this slice. ONE pure function, `crate::bounce::bounce_after` (new `crates/web/src/bounce.rs`), decides the bounce for a refused hydrate-loop read AND a refused `interact.rs` Tell alike — the pre-existing 2c-ii 401 "no session" leg, previously inline in `hydrate`'s `spawn_local` and never seen red, moved in and is now covered by this function's own tests for the first time. Two new screen DSL keys, the `unauthenticated:` twin: `restricted: { type: navigate, route }` (on `jobs`/`job_detail`) and `while_restricted: true` (on the `/restricted` screen itself, which carries NO `rider_topbar` — its online toggle dispatches `changeRiderStatus`, never `whileRestricted:`), proved by two new validator rules (`screen-restricted-route-unknown`, `screen-restricted-binds-uncarved-op` — the latter's "never mounts `rider_topbar`" clause is a COROLLARY of the SAME uncarved-op check, not a second mechanism, since `bound_operations`'s component walk already expands `{ component: rider_topbar }` and discovers the uncarved `changeRiderStatus`). The notice: title + "no more jobs" UNCONDITIONAL; the ground (five leaves, `RIDER_REQUESTED`/`ELIGIBILITY_DOCUMENT_LAPSED`/`IDENTITY_MISMATCH`/`ACCOUNT_COMPROMISE`/`UNRECOGNISED`, `fr` VERBATIM from [ADR-20260904-081527](adr/ADR-20260904-081527-rider-standing-is-a-grant-on-the-identity-row-the-doors-are-human-only-and-step-4-lands-in-three-slices.md) §7) and BOTH dates (`decidedAt`/`effectiveAt`, equal in V0 but both shown) gated on `standing.restriction != null`; the transient sentence is the OTHER branch of the SAME `conditional_section` (no `&&` in the corpus grammar, so nesting is how "unrecognised renders only when the restriction exists" holds without inventing one); a NEW `format_datetime` render filter (Europe/Paris, `fr` — "4 sept. 2026, 16:02" for a 14:02Z instant in September's CEST — beside `format_currency`, `chrono`+`chrono-tz` newly in `web`'s `Cargo.toml`); the footer and all five ground sentences are each split at the support address into `.lead` / a bound `{{ standing.contestContact }}` / `.trail` — `contestContact` is a NEW additive nullable field on `RiderStandingInfo`, resolved once from `SUPPORT_CONTACT` configuration through the SAME composition-root precedent the rider sign-in door already used (`ReadDeps.support_contact`, threaded through `build_graphql_di`/`build_schema`), so no translation string hard-codes the address. The held-job card (restaurant name via the pre-existing FK nav edge, pickup address, ADR-20260904-015903 §7) opens a SECOND bottom sheet, `rider_restricted_handback_sheet` — a copy of `rider_handback_sheet` re-bound to `standing.heldDelivery.*` (no screen-level alias grammar exists to reuse the original; ADR §5 refused inventing one) — dispatching `hand_back_delivery` and, on success, closing the sheet + `navigate: "$reload"` to re-execute the screen's own reads. `myStanding.heldDelivery` is now served by a NEW port, `DeliveryReadRepository::held_by_rider` (a per-paint SQL narrow on `rider_id` + the three held statuses), replacing `for_rider(..).find(..)`'s whole-history-plus-PENDING-pool scan (#879's item, closed here). Native render tests cover every ground + the catch-all (both dates formatted, never a raw ISO instant, the contact rendering TWICE — ground sentence AND footer, so a contact dropped from either branch is caught), the transient (never blank), and the held-job card + its sheet's dispatch (present/absent/after-state); the DB-gated walk (`rider_standing_walk.rs`) was extended to assert both dates non-null after `restrictRider` and to drive a SECOND restrict/handback cycle proving `handBackDelivery` reaches the business layer for a RESTRICTED rider and `myStanding.heldDelivery` narrows to null once handed back. Seven mutants planted against the native tests and the two new validator rules, red captured verbatim, reverted (see [journal-2026-W36.md](status/journal-2026-W36.md)). Gates: `cargo build --workspace`, `make validate` (0 errors), `make rust`, 416 codegen unit tests, 161 `web` unit tests, DB-gated `rider_standing_walk.rs` and `make test-crates`, CI-narrowed clippy — all green. **Not this slice, by design**: the document-GET bounce (rides with step 5); the SMS notice (still 4-ii's own gate before production, unbuilt); the admin roster screen (4-iii). **Round 2** (same PR, still DRAFT): the held-job card's restaurant name and pickup address used to render blank (the emitter only ever sub-selected a nav edge nested one level below the query's root; `heldDelivery` is a declared PROPERTY, so `restaurant { displayName }` nested inside it never got selected) -- fixed at the emitter, plus a new `format_address` render filter replacing the unfiltered `Address`-object bindings that fell through the currency formatter. A reinstated (ACTIVE) rider landing on `/restricted` via back-navigation now reads "Votre accès est rétabli." instead of the stale restricted notice -- the whole notice body, including the door's own header, is one `conditional_section` keyed on the rider's OWN current standing. Plus six one-liners and three addendum items (both date labels asserted, the second sheet's ASSIGNED arm proven, the split legal sentence flows as one line via CSS, `make wasm` green, a router twin for the `unauthenticated:` door, `contestContact` proven end-to-end with `Some(support_contact)`). |
| Authentication / identity (Supabase JWT) | ✅ | **First cut shipped (ADR-0047)**: verify Supabase JWT via JWKS at `/{role}/graphql` (public keys, no shared secret; ~1h cache, serve-stale-on-refresh-failure — no per-request Supabase call); `app_metadata.captain_role` gates the path (`/public` open, else 401/403), fail-closed on cold cache, asymmetric-only. Verified role + `Principal` injected. **EXTERNAL service tokens** via `X-External-Api-Key` (constant-time, `EXTERNAL_API_TOKENS`) shipped. Per-field `@auth` on FK-nav edges = DSL/plan-mode follow-up |
| **IDENT-1 Phase A — CUSTOMER identity resolved from Postgres instead of the JWT claim** | 🅑 gated OFF | [#641](https://github.com/TheCaptainCompany/captain-food/issues/641), [ADR-20260818-004646](adr/ADR-20260818-004646-no-business-identifier-lives-in-the-identity-provider.md). `resolve_read_scope`'s CUSTOMER arm can now resolve the caller's domain id from Postgres via the existing `CustomerReadRepository::by_auth_ref` bridge instead of trusting `captain_food.customer_id` — behind `RESOLVE_CUSTOMER_IDENTITY_FROM_POSTGRES` (`specs/customer/configuration.yaml`, **default `false`**), selected ONCE at startup, never a per-request fallback. A `NoMapping` row and a `LookupFailed` lookup both fail closed to `Public` identically, distinguishably in telemetry (`customer-identity` observability contract: span `customer.identity.resolve`, histogram `customer_identity_resolve_ms`, counters `customer_identity_not_found_total` / `customer_identity_lookup_failed_total{reason}` / `customer_identity_lookup_source_total{source}`). Scope: **CUSTOMER only** — RESTAURANT/RESTAURANT_ACCOUNT/RIDER have no sign-in operation in the DSL at all (STAFF-AUTH, DECISIONS §46). **GDPR erasure — the Customer VOCABULARY exists; the `deletion:` block does NOT, deliberately** ([#708](https://github.com/TheCaptainCompany/captain-food/issues/708) part 1, PR [#763](https://github.com/TheCaptainCompany/captain-food/pull/763), `HOLD: human`): commands, the request/confirm/cancel/due/unlink facts, `CustomerErased`, `View_CustomerErasure`, `erasureStatus`, the grace-window reminder and the observability contract all landed, together with `legalRetention:` and its two validator rules, which close the `BRIEF-20260811 §3` BLOCKER-on-arrival. **The `deletion:` block itself was deferred once the deletion ENGINE refused it**: `DeletionEngine::new` validates every declared policy at construction, so a Customer block with no receipt builder fails to construct and takes ORDER erasure — a working, legally-required journey — down with it; and the block needs `cancelled_on:`, whose undo pass does not exist. A block declared without `cancelled_on` would let a CANCELLED erasure destroy the account anyway. So `Order` remains the only declared deletion block, pinned exactly. **The write paths REFUSE** with a typed `ErasureEngineUnavailable` and record nothing — the journey that executes an erasure is the NEXT chunk, and accepting a request it cannot run would start the Art. 12(3) month on nothing. Deferred with reasons on the PR: `CustomerErasureProcess` (a PM cannot be declared without its runtime seam), `identity.erase_identity` (nothing calls it; it forced unverifiable ACL impls) and the erasure copy (`translation-key-unused` refuses keys no screen renders); **now LAUNCH-BLOCKING**: [ERASURE-LAUNCH-GATE](decisions/ERASURE-LAUNCH-GATE.yaml) decided 2026-08-29 (*"A — Erasure ships first"*, [ADR-20260829-145848](adr/ADR-20260829-145848-the-founders-answer-sheet-of-2026-08-29.md)) — [#708](https://github.com/TheCaptainCompany/captain-food/issues/708) gates the first real order, reframed as engine-exists-gated-OFF (the ADR-20260731-214500 deletion engine already runs; Customer lacks its `deletion:` block + unlinking + crypto-shred + SNAP-1 + an executed drill). **The design proposal is APPROVED** ([PROP-20260829-150752](proposals/PROP-20260829-150752-customer-erasure.md), founder 2026-08-29 verbatim *"A — Approve Option 1"*; counsel questions run in parallel and refine-not-block; build starts now that the birth-lane flip proved the pattern, [ADR-20260830-012200](adr/ADR-20260830-012200-the-order-birth-routes-through-the-lane.md) / ADR-20260829-230418 sequencing). The outage EXPERIENCE is also decided ([IDENT1-OUTAGE-EXPERIENCE](decisions/IDENT1-OUTAGE-EXPERIENCE.yaml): visible retry, posture unchanged, ships with the flip). **The credit-disposition hole that blocked the engine chunk is DECIDED and no longer blocks it** ([#764](https://github.com/TheCaptainCompany/captain-food/issues/764), [CREDIT-AT-ERASURE](decisions/CREDIT-AT-ERASURE.yaml) / [ADR-20260831-033621](adr/ADR-20260831-033621-customer-credit-is-disposed-of-as-a-leg-of-erasure-goodwill-credit-is-refundable.md), founder 2026-08-31, six lenses): a positive `CustomerCredit` balance is disposed of as a **LEG** of the erasure and never parks it — credit traceable to a captured payment is **refunded** to the original instrument (capped at that capture's un-refunded remainder), purely promotional credit is **forfeited**, and the balance is **disclosed at the confirmation step**. Escheat and block-until-zero both rejected. Reclamation **goodwill credit is REFUNDABLE** (D1) — 100% of the credit that can exist at V0; forfeiture is a rule of **account termination generally**, not erasure-specific (D2, Art. 12(5)); a **failed refund proceeds and is recorded** on the pseudonymous receipt as an ordinary payable (D3, Art. 12(3) clock). Still open and NOT closed by that ruling: **D4** (leg inside #708 or after), **D5** (shorten the credit expiry to ~180 days), **D6** (drain order — free only until a promotional grant exists), and the **three counsel questions stay undischarged** (0 discharged, 1 narrowed, 2 untouched; Q2 — is the credit ledger L123-22-retained or shreddable — is now *heavier*, since both limbs produce an accounting movement). The split needs **provenance on `CustomerCreditGranted`**, a stored-event-shape change the engine chunk owns (`HOLD: human`); until it lands the disclosure block is **absent, not zero**. [#764](https://github.com/TheCaptainCompany/captain-food/issues/764) stays OPEN for those residues. Flipping the default is a separate recorded decision after the gated form is smoked. |

## 🔎 SIRENE prospection (ADR-0019/0020/0027/0045)

| Piece | Status | Notes |
|---|---|---|
| SIRENE ACL (INSEE → RegisterRestaurant mapping) | ✅ | Unit + DB verified |
| Interim direct-write `sirene_sync` binary | ✅ | **Retired** (ADR-0045) — replaced by the split below |
| `external_sirene_restaurants` staging table | ✅ | Migration applied by CI |
| Thin CI ingestion crate `sirene_ingest` (fetch → UPSERT raw rows, France-wide by department, active-only) | ✅ | No domain deps; scheduled workflow builds only this crate |
| On-app `sync_sirene_worker` (ACL on deployed version) + deletion reconciliation | ✅ | Per-row checkpoint; detect-by-absence (21d debounce) + explicit `F`/`C`; NON_PARTNER auto-close, partners flagged; `POST /internal/sirene/drain` (token-gated, fail-closed) |
| `INSEE_API_TOKEN` repo secret | ✅ | Added. **⏳ STOPPED at both ends, and the reason given is stale** (corrected 2026-08-30): the scheduled ingestion → staging → worker chain was paused 2026-07-28 *"until #220"*, and [#220 "Slug lifecycle + SIRENE as inbound event"](https://github.com/TheCaptainCompany/captain-food/issues/220) **closed the same day** (PR [#229](https://github.com/TheCaptainCompany/captain-food/pull/229)), as did the other named bottleneck [#218 "[watchdog] sirene-sync France sweep still exceeds the 90-min CI budget"](https://github.com/TheCaptainCompany/captain-food/issues/218) (PR [#232](https://github.com/TheCaptainCompany/captain-food/pull/232)). **Nobody has re-taken the decision to restart it**, so the chain has been off for over a month behind a blocker that no longer exists — found while checking the premise of the 2026-08-30 STAFF-AUTH answer, whose claim-your-listing path needs listings to claim. Restarting is a decision with real costs (INSEE quota, a ~4h weekly sweep, prospect creation on live data), not a flag flip: [#800](https://github.com/TheCaptainCompany/captain-food/issues/800) |
| `INTERNAL_TRIGGER_TOKEN` (Render env + repo secret) to enable the CI→worker ping | ⏳ | Optional; unset, so `POST /internal/sirene/drain` is fail-closed (503). `RUN_SIRENE_WORKER` **defaults OFF and is bound to register row [`SIRENE-RESTART`](decisions/SIRENE-RESTART.yaml)** (PR [#918](https://github.com/TheCaptainCompany/captain-food/pull/918), 2026-09-06: `decisionRow:` + deploy values `"false"` at both ends, so an env flip alone cannot restart it while the row is open; `runKind: worker`) — see the row above: the blocker it names is closed and the restart decision is open ([#800](https://github.com/TheCaptainCompany/captain-food/issues/800)) |

## 🔌 External integrations — partner adapters & M2M (ADR-20260718-145856 / -213352)

**Partner webhook adapters are self-contained crates** under `crates/adapters/*` — each an ACL +
axum shell + standalone binary, mountable into the monolith **or** deployable as its own web service.
Two directions: partner-**push** webhooks (below) vs external-**drive** `/external/graphql` (M2M).

| Piece | Status | Notes |
|---|---|---|
| **Stripe** — `crates/adapters/stripe` (`POST /adapters/stripe/webhooks`, `stripe-webhook` bin) | ✅ | `Stripe-Signature` HMAC over raw body (constant-time, 300s replay, fail-closed); ACL → `PaymentCaptured`/`PaymentFailed`/`PaymentRefunded`; idempotent by Stripe event id. 12 tests |
| Checkout must set `metadata.restaurantId` (+`orderId`) on the PaymentIntent/charge | ✅ | `StripePaymentGateway` sends `metadata[orderId]`/`[restaurantId]`/`[cartId]` on create-intent — the webhook ACL maps `charge.refunded` from them; exercised by the green prod smoke |
| **HubRise** — `crates/adapters/hubrise` (`POST /adapters/hubrise/webhooks`, `hubrise-webhook` bin) | ✅ | **Ingress** ✅ (HMAC-SHA256 hex, fail-closed, envelope parse). **Outbound OAuth2 client** ✅ (`api.rs`: `X-Access-Token`, non-expiring token from `HUBRISE_ACCESS_TOKEN`, `exchange_code` connect helper, catalog/inventory pull). **Domain wiring** ✅ (`enrich.rs`): verified catalog/inventory callback → API pull → enrichment ACL → `ImportCatalog` / per-SKU `update_offer_stock` handlers. **Deterministic UUIDv5-of-HubRise-id** ids reconciled with the **Catalog aggregate** (offer seeded from the SKU `ref` = inventory's `sku_ref`, so a stock update hits the imported `OfferId`); `"9.80 EUR"`→`Money`, tax-rate strings→`TaxRate`, `data` envelope translated at the boundary; catalog = rejectable command (`CatalogNotFound`→skip), inventory = reported fact (`OfferNotFound`→skip, never rejected). 14 tests. Enricher wired at the server composition root + the standalone bin (needs only `DATABASE_URL`). ✅ **Connect flow landed (#20, ADR-20260721-100601)**: OAuth connect provisions account/locations/catalogs with the derived ids + stores the account-scoped token in `hubrise_connections` (env token retired) |
| **`/external/graphql`** — M2M standard | ✅ | External entities query/mutate via the `EXTERNAL` role path; API-key auth (`X-External-Api-Key`, ADR-0047); allowlist is per-op `roles: [EXTERNAL]`. **Subscribe** = future (needs `SubscriptionRoot` + WS + `api.yaml`); per-partner keys = future |

## 👤 Ops / user actions

- 🌑 **Admin rider roster + restrict/reinstate screens** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 4-iii-A, [ADR-20260904-152807](adr/ADR-20260904-152807-the-admin-s-hands-one-custody-truth-read-at-query-time-a-door-that-refuses-until-the-notice-exists-and-two-slices.md)): `riders`/`rider_detail` on `specs/screens/system.yaml`, ADMIN-only, DARK (the System host is not routed and no admin sign-in door exists — reachable only through GraphQL as ADMIN until step 6 lands the magic-link door + host routing). The restrict door key `RUN_RIDER_RESTRICTION_DOOR` is OFF everywhere; three named preconditions (the admin door, the SMS notice #874, an alert route for the dead-man gauge) gate the flip, tracked in [`RIDER-RESTRICTION-PRECONDITIONS`](decisions/RIDER-RESTRICTION-PRECONDITIONS.yaml) (open). **4-iii-B landed** (same ADR §8): the gauge `rider_restricted_holding_job_age_seconds` (`rider-restriction` contract) — age of the oldest RESTRICTED rider still holding a job, anchored on `effective_at`, emitted every sweep from `delivery_handback_watch_tick` beside the `custody-handback` gauge, certified by the SAME `delivery_handback_sweep_heartbeat_total` heartbeat — plus its debounce key `RIDER_RESTRICTED_CUSTODY_MAX_AGE_SECONDS` (default 1800, `UNVERIFIED input`). The alert-route precondition is unchanged (still the recorded known gap); the bam measure `heldJobAtDecision` did NOT land — inexpressible in the fold grammar (cross-stream: `RiderRestricted` carries no `deliveryJobId`), reported to the architect.
- 🌑 **The restriction fact terminates the rider's socket** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 5, [ADR-20260905-065415](adr/ADR-20260905-065415-the-restriction-fact-terminates-the-rider-s-socket-a-connection-local-standing-read-inside-the-guard-and-one-writer-to-the-transport.md)): a RIDER WS connection carries a connection-local standing cell (`StandingGuard` reads it first) and a per-connection watcher that closes the socket (4403) the moment this rider's OWN `RiderRestricted` fact lands, behind `RUN_RIDER_RESTRICTION_SOCKET_CLOSE` (OFF everywhere — the restrict door itself is still pinned off and the rider population is zero, so this closes nothing in production today). The client leg (reading the close code instead of reconnecting) is [#894](https://github.com/TheCaptainCompany/captain-food/issues/894), not this slice.
- 🌑 **The bridge and the grant — a restaurant member can be provisioned by hand** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 6-i, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md)): the `RestaurantMembership` aggregate (`grantRestaurantAccess`/`revokeRestaurantAccess`, ADMIN-only, `basis: CAPTAIN_ONBOARDING` only), the `(MEMBER, authSubject)` reservation in 2a's table (no release), the `Member` bridge table and `ScopeMembership`'s targeted MEMBER grant/revoke pair, behind `RUN_MEMBER_ACCESS_GRANT` (OFF everywhere, [`MEMBER-ACCESS-GRANT-PRECONDITIONS`](decisions/MEMBER-ACCESS-GRANT-PRECONDITIONS.yaml) open). DARK: no query, no sign-in door (6-ii) — inventory until 6-ii lands.
- 🌑 **The door — a member can request a magic link, sign in, and land somewhere** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 6-ii, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md), PR [#899](https://github.com/TheCaptainCompany/captain-food/pull/899) round 2, DRAFT — not yet reviewed/merged): PUBLIC `requestMemberSignInLink`/`confirmMemberSignIn` (identify-only, byte-identical accept response for a member and a stranger, the `Member` bridge never consulted on the request leg), a third role-only stamper `identity.stamp_member_claim` (`{role: MEMBER}`, no id in the claim), the MEMBER arm of `crates/server/src/auth.rs`'s identity seam (auth_subject → Member → ScopeMembership → one restaurant scope, fails closed on 0 or >1), `sign_in`/`sign_in_return`/`not_linked` screens on `restaurant_backoffice.yaml` — round 2 landed the hand-written `sign_in_return` return leg (`crates/web/src/sign_in_return.rs`), so the mail-client link now actually goes somewhere — behind `RUN_MEMBER_SIGN_IN_DOOR` (OFF everywhere, [`MEMBER-SIGN-IN-DOOR-PRECONDITIONS`](decisions/MEMBER-SIGN-IN-DOOR-PRECONDITIONS.yaml) open, now also naming the return route/`?next=`/Art. 13/Art. 30 items as preconditions of the FLIP). **Item E (per-role GraphQL depth/complexity limits) LANDED in round 2** — the PROP's `public-graph-limits` Concern is discharged: `crates/server/src/graphql/query_limits.rs` (a `parse_query` extension, registered unconditionally on the one shared schema, keyed on `ActingRole`) refuses a document past its role's ceiling before any resolver runs; ceilings are codegen-derived per role (`crates/server/src/graphql/generated/limits.rs`) over the GENERATED client documents real screens bind, times `GRAPHQL_LIMIT_HEADROOM_PERCENT`; proven by `crates/server/tests/graphql_role_limits.rs` plus the module's own unit tests. The per-address email send-abuse wall now HMAC-hashes its quota key (never the raw address) and is wired into the standalone actor-runtime worker too (previously unguarded there).
- 🌑 **The roster and the invitation — a manager can invite a colleague by email** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 6-iv, [ADR-20260905-101349](adr/ADR-20260905-101349-step-6-lands-in-four-slices-the-bridge-and-the-grant-first-the-door-second-and-the-accept-is-two-commands-in-two-lanes.md), PR [#901](https://github.com/TheCaptainCompany/captain-food/pull/901), MERGED 2026-09-05 (squash `95542ada`) at the three-round ceiling): the `RestaurantInvitation` aggregate (own stream/lane, `inviteRestaurantMember`/`revokeRestaurantInvitation` RESTAURANT-role with a genuine server-side MANAGER-authority `AuthorityGuard`, `acceptRestaurantInvitation` PUBLIC, PENDING→{ACCEPTED,REVOKED,EXPIRED} lifecycle, TTL reminder declared behind `RESTAURANT_INVITATION_TTL_SECONDS` = `UNVERIFIED input` 604800s), behind `RUN_RESTAURANT_INVITATION` (OFF everywhere, [`RESTAURANT-INVITATION-PRECONDITIONS`](decisions/RESTAURANT-INVITATION-PRECONDITIONS.yaml) open), the `restaurant-invitation` observability contract, and DB-gated walks. **Round 2** (a presentation pass, ADR-20260905-101349 §2 amendment): the accept's second command is now its own PUBLIC door, `grantRestaurantAccessByInvitation` — `grantRestaurantAccess` reverts ADMIN-only with every field required, closing the round-1 gap where an anonymous caller could mint a membership for any subject on any restaurant; the new door proves the caller IS the accepting subject, derives `membershipId` as `UUIDv5(invitationId)`, and reuses an already-held `memberId` for a re-hire. The `RestaurantRoster`/`RestaurantInvitationList` read models now exist (own projector groups, own migration, `restaurantRoster`/`restaurantInvitations` GraphQL queries) with executable rebuild-recipe tests (checkpoint-reset-never-truncate for the roster, truncate-and-reset-together for the invitation list). **Round 2, phase D**: the `/team` screen now renders the live roster + invitation list with per-row revoke (MANAGER-gated) and an invite sheet; a new hand-written `/invitation` landing page (`crates/web/src/invitation_accept.rs`) sequences the two-lane accept in the browser (`acceptRestaurantInvitation` then `grantRestaurantAccessByInvitation`, bounded retry on the second leg). The TTL reminder's DELIVERY is now WIRED too (`RecordLeg::RestaurantInvitation` in `mailbox/handler.rs` — round 1 had parked it citing a fence on that file that named no concurrent claim; [#902](https://github.com/TheCaptainCompany/captain-food/issues/902)'s poison-row-on-PITR concern is closed by this). **Round 3 (the CEILING, ADR-20260826-084500)**: the roster's `RestaurantAccessRevoked` arm now LANDS (an additive `tombstone:`-declared DELETE, grant→revoke→row-gone, deterministic under checkpoint-reset replay); the invitation list's revoke button no longer renders — and does nothing — on a terminal row (nested `conditional_section`, `item.status == 'PENDING'`); `restaurant_roster_lag_positions`/`restaurant_invitation_list_lag_positions` gauges and `restaurant_invitation::expired()`'s emit call now exist (previously declared/silent); `MemberId`'s kernel description corrected (the invitation path derives `MembershipId`, never `MemberId`); the French "Gérant" label corrected to "Responsable". **Still NOT landed**: a revoke confirmation step (the SDUI `open_bottom_sheet` primitive carries no per-row context — documented gap, not built ad hoc); `invitationPreview` (needs a resolver shape mixing live token verification with a read-model lookup, no precedent in the mechanical query-emission pipeline — reported rather than invented, now a named flip precondition); the invitation SEND leg and query spans for the two new read-model queries (also named flip preconditions).
- 🌑 **The platform grant and the ADMIN seam binding — a role claim alone no longer makes an admin** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 6-v, [ADR-20260905-223957](adr/ADR-20260905-223957-the-admin-door-in-two-slices-a-platform-membership-binds-the-admin-seam-then-the-system-host-is-routed.md), PR [#907](https://github.com/TheCaptainCompany/captain-food/pull/907), **MERGED** 2026-09-06 (squash `2c3bf3f1`) at the three-round ceiling after the team reviewer pass — follow-ups [#908](https://github.com/TheCaptainCompany/captain-food/issues/908)): a new `PlatformMembership` aggregate (`grantPlatformAccess`, ADMIN-only, `basis: CAPTAIN_ONBOARDING` only) records platform standing as its OWN relationship (never a `ScopeType` widening, never `RestaurantAccessGranted` reused, PRINCIPALS-MEMBER/RLS-SEQ untouched); the `PlatformMember` bridge (own `ProjectorGroup` from 0, `auth_subject UNIQUE`, standalone — no `ScopeMembership` prefix); the ADMIN seam (`resolve_platform_scope`/`PgPlatformIdentity`, the RIDER/MEMBER shape transposed) makes `Identity::Admin` unspellable from a token's role claim alone — an ADMIN token with no live grant is `Identity::Unbound`, refused as PUBLIC on both read and write; the one-shot `bootstrap-platform-admin` subcommand dispatches the FIRST grant through the ordinary mailbox (deterministic `platformMembershipId`, never a row/SQL/migration); the `admin-sign-in` observability contract (`admin.identity.resolve`, `admin_identity_resolve_total`, `platform_access_granted_total`, `platform_access_grant_enforcing` dead-man gauge at both roots); a new codegen test asserting every `RUN_*` door key is `declare_flag`'d at both composition roots with the same default (found and fixed a pre-existing gap, `RUN_RIDER_RESTRICTION_SOCKET_CLOSE` missing from the standalone root). Behind `RUN_PLATFORM_ACCESS_GRANT` (OFF everywhere, [`ADMIN-DOOR-PRECONDITIONS`](decisions/ADMIN-DOOR-PRECONDITIONS.yaml) open). DARK: no revoke command yet (deferred until a second admin exists); 6-iii (System host routing) is next and depends on this landing.
- 🌑 **System host routing behind auth + the ADMIN sign-in door — step 6 is complete, every door dark behind its gate** ([#639](https://github.com/TheCaptainCompany/captain-food/issues/639) part C step 6-iii, [ADR-20260906-023825](adr/ADR-20260906-023825-the-admin-sign-in-door-is-the-member-doors-shape-identify-only-against-the-platform-grant.md), PR [#909](https://github.com/TheCaptainCompany/captain-food/pull/909), **MERGED** 2026-09-06 (squash `7fc8e60a`) after two review rounds): `system.captain.food` serves the SDUI shell; every System screen `requires_auth` with `unauthenticated: → /sign-in`; the admin signs in through a PUBLIC `requestAdminSignInLink`/`confirmAdminSignIn` pair on its own width-5 actor, identify-only against the `PlatformMember` bridge, a fourth hardcoded `{role: ADMIN}` stamper, the seam re-deriving the grant on every request; an anonymous browser never sees the board, a bound-but-not-ADMIN principal lands on `no_access`; `admin-sign-in-door` contract, `admin_sign_in_door_enforcing` at both roots; behind `RUN_ADMIN_SIGN_IN_DOOR` (default false, `ADMIN-DOOR-PRECONDITIONS` open — the flip waits on the founder's external items: DNS, `EMAIL_QUOTA_KEY_HMAC_SECRET`, `PLATFORM_BOOTSTRAP_ADMIN_SUBJECT` + the bootstrap run). RIDER-RESTRICTION-PRECONDITIONS (1) discharged on the code side. Follow-ups [#912](https://github.com/TheCaptainCompany/captain-food/issues/912); revocation of platform access [#911](https://github.com/TheCaptainCompany/captain-food/issues/911).
- ✅ Keep the web service **warm via uptimerobot `/ping` every 5 min** (prevents free-tier spin-down so the in-process projector + SIRENE worker keep running).
- 🗑️ `INTERNAL_TRIGGER_TOKEN` / `POST /internal/sirene/drain` — agreed to **remove** (superseded by the `/ping` warmth approach); code removal deferred to avoid colliding with concurrent `routes.rs` edits — harmless meanwhile (fail-closed 503 when the secret is unset).

> **Claim protocol (2026-07-20, ADR-20260720-233000, #39; amended 2026-07-21 by
> ADR-20260721-042018):** before working an issue, add the `status/in-progress` label + a claim
> comment naming the `NN-slug` branch, **create the branch and open a draft PR (`Closes #NN`)
> immediately**; NEVER work a claimed issue; on completion the **COORDINATOR** marks ready + enables
> auto-merge and supervises checks until MERGED
> ([ADR-20260831-183847](adr/ADR-20260831-183847-the-ready-flip-is-the-coordinators-step-and-always-was.md),
> restoring ADR-20260810-011500 §2) — **never the executor**, which physically cannot and hands back
> at green with the PR still in DRAFT; the hourly stale-claim reaper releases claims silent for >24h.
> Method: `BACKLOG.md`.

## ✅ `claude-review` is no longer a required check — REV-1 executed (2026-08-26)

> **If you are here because a PR is stuck on `claude-review`: it is no longer required, so that is
> not what is blocking you.** [REV-1](decisions/REV-1.yaml) decided on 2026-08-17 to remove it and
> was finally **executed by the founder on 2026-08-26**, nine days later, in the GitHub UI. Ruleset
> `19179892` now requires `codegen`, `build-test` and `db-test` only.
>
> **The exposure this section used to carry is CLOSED**: a 429, a model outage, a permission denial
> or the action's self-skip on a PR editing its own workflow is no longer a repo-wide merge stop.
>
> `claude-review` still RUNS on every PR and still posts findings — it just does not gate merges.
> The compensating control is unchanged and is a process obligation, not a mechanism: the
> independent reviewer pass stays MANDATORY before ready-for-review (founder directive 2026-08-01),
> now at one pass per PRESENTATION rather than per push
> (`ADR-20260826-084500`, [`review-triage`](../.claude/skills/review-triage/SKILL.md)).
>
> **A red on `claude-review` still means NO VERDICT WAS PRODUCED**, not that the reviewer found a
> problem — #680 hardened it to fail rather than self-clear, which is why that distinction matters.

## 🗂️ Decision register & ask gate (2026-08-21)

| Piece | Status | Notes |
|---|---|---|
| Machine-readable decision rows — **`docs/decisions/<KEY>.yaml` is the authority** | ✅ | One file per globally unique key, closed status vocabulary (`open\|decided\|deferred\|superseded\|withdrawn`), resolvable `decided_by`/`superseded_by`, `reconsiders` challenge chains ([ADR-20260821-095957](adr/ADR-20260821-095957-the-register-row-gets-machine-identity-reg2-reg4-and-the-ask-gate-reads-it.md)). Generated index injected into `DECISIONS.md` (§22b keeps it in sync); `_legacy.yaml` = 102 prose-only keys, a **migration boundary, never authority**; `_exempt.yaml` = self-pruning held-record citation exemptions. **v2 key grammar (2026-08-28)**: `--` is no longer bare-illegal, it is the ONE namespace separator for the per-proposal `D1`-`D7` family — `<PROPOSAL-ID>--<LOCAL>`, namespace must resolve to a committed proposal (`decision-key-namespace-dangling` otherwise) — closing `KEY-NAMESPACE` ([ADR-20260828-153000](adr/ADR-20260828-153000-register-keys-namespace-by-proposal-with-a-double-dash.md)). All 29 of the ambiguous `D1`-`D7` occurrences are now migrated: the first 13 (`PROP-20260809-003000`/`PROP-20260809-021351`, [ADR-20260828-153000](adr/ADR-20260828-153000-register-keys-namespace-by-proposal-with-a-double-dash.md)), plus the deferred 14 (`PROP-20260810-234225` §27 / `PROP-20260811-000946` §28), reconciled against the source proposals and the §27bis reversal rather than copied from the stale summary tables — D4/D6 of §27 were REVERSED 2026-08-11 and the DECISIONS.md table cells now flag that inline instead of reading as current |
| Ask gate — founder decision questions carry `Decision row: <KEY>` on an OPEN row | ✅ | Fail-closed PreToolUse hook on `AskUserQuestion` (`.claude/hooks/register-check.sh`; envelope/trail/passive lanes, exit 0 allow / 2 block only) + selftest run by the stop-gate every turn **and by CI's always-run `gate-scripts` job on every push, docs-only included** ([ADR-20260821-010543](adr/ADR-20260821-010543-agents-never-ask-an-answered-question-the-register-check-binds-every-agent.md), [ADR-20260821-103403](adr/ADR-20260821-103403-decision-ask-unregistered-and-the-citation-ratchet.md)). Boundary stated honestly: only the structured envelope is mechanically gated; free text is not. **A SECOND always-run gate step joined 2026-08-26** — the decision-lookup hermetic stub suite (row `RETRIEVAL-QMD-CI`, [ADR-20260824-205911](adr/ADR-20260824-205911-the-decision-lookup-stub-suite-runs-in-ci.md)) — and both steps compare all four gate scripts against their committed blobs before reporting. That comparison is **mostly pre-merge**: `make hooks-test`/`make stub-tests` opt out unconditionally, and the stop-gate opts out only when a gate script is dirty. So an ordinary overwrite is still caught at push (it makes the tree dirty, which is what opts the turn out); what the in-session armed path catches is the tamper that HIDES from `git status` (`--assume-unchanged`, `--skip-worktree`), i.e. the stealthier class. CI is the only caller that cannot be talked out of it. Every job the `codegen` aggregator consumes now carries a `timeout-minutes`, because `always()` still waits. **The locus is DECIDED** — `GATE-STEP-LOCUS` option (a), founder 2026-08-27 ([ADR-20260827-081500](adr/ADR-20260827-081500-the-call-sheet-answers-gate-steps-move-and-the-citation-rule-hardens.md)): both steps live in the sibling always-run `gate-scripts` job, so a gate red still reds the required check but no longer skips every other job. **The TRAIL lane reads its own `<status>` clause too (2026-08-29, [ADR-20260828-120500](adr/ADR-20260828-120500-an-answered-question-is-never-asked-again.md), closing [#709 "The register-check hook refuses a question whose controlling row is already decided"](https://github.com/TheCaptainCompany/captain-food/issues/709))**: a trail self-citing a CLOSED status (`decided`/`superseded`/`deferred`/`withdrawn`) in the canonical `(<date>, <status>)` form is refused (`trail-answered`) — the escape is a `premise-changed: <what changed>` line, allowed through and logged distinctly (`trail-premise-changed`) |
| **Dispatch gate — the COORDINATOR carries the same trail on its committing surface (2026-08-31)** | ✅ | Fail-closed PreToolUse hook on the **`Agent`** tool — Lane D of the same `.claude/hooks/register-check.sh` (extended, not forked: the gate-script self-verification set stays at four files). Founder directive 2026-08-31 (*"You really need to use the same approach as the agents have"*), [ADR-20260831-141500](adr/ADR-20260831-141500-the-coordinator-gets-the-register-check-gate-on-its-committing-surface.md), closing [#814 "The coordinator has no register-check gate: dispatch cards and direct answers are composed from the conversation, not from the register"](https://github.com/TheCaptainCompany/captain-food/issues/814) — nine failures in one session, four caught only by the founder or a lens. **The discriminator is DERIVED, not listed**: the gate fires iff the target agent's own frontmatter `tools:` line grants a write tool from a **closed token set** (`Write`/`Edit`/`MultiEdit`/`NotebookEdit`; today `architect`/`executor`/`generator`), so lens consults and the `reviewer` pass are never gated and no exemption list of agent NAMES exists to drift — the token set itself is a maintained list, the stated residual. It **fails closed on every unreadable shape it can DETECT** — unknown or undeclared agent (`general-purpose`), no `tools:` key, an empty value across its whole indented block, an unbalanced flow list, or a wildcard — a claim deliberately scoped to what it detects, because its universal form was asserted and falsified in **two** review rounds (round 1: `awk /^tools:/` returns the literal `tools:` for a list form; round 2: reading only the first physical line cannot decide continuation, missing a flow list broken before the comma, one after it, a plain list broken before the comma, and a folded scalar). The parse now reads the **whole value**, which deleted the trailing-comma special case and removed a false positive it had introduced. Named residual: it is a line-based reader, not a YAML parser. **The resolver covers all three `docs/adr/` filename eras** (`ADR-<stamp>`, legacy `NNNN-`, prefixless `<stamp>-`), mirroring `record_resolves` in `tools/codegen-rs/src/validate/decisions.rs`; covering only the first refused 101 of 266 real ADRs. **And the eras were not the corpus**: with a fixture per era in place the gate still mis-handled all 80 `docs/decisions/*.yaml` rows (53 refused, 27 silently resolving to the parent proposal rather than the cited row), `REG-2` and `QUOTE-TOKEN` included — so `resolve_record` now carries a universal row candidate and the register's own v2 key grammar. Pinned by `tools/codegen-rs/src/tests.rs :: every_record_in_the_corpus_is_citable_through_lane_d`, which drives the real hook over **417** records and reds the day a fourth filename era or fifth record kind lands. The rule it earned — **a gate that classifies members of a corpus is tested against the CORPUS, not against fixtures** — is in `docs/claude/sessions/workflow.md`; a gate that refuses its own corpus rewards the fabricated citation it exists to stop. **The escape hatch is shut**: a positive trail's record id must RESOLVE to a file under `docs/`, and a negative must name its `terms:` — a literal `Register check: none` is refused. Lane D deliberately does NOT run the envelope or passive-key lanes: on a CARD, citing a decided record is the behaviour being enforced. **Honest limit, recorded rather than hidden**: a hook gates a TOOL CALL, so the coordinator's PROSE answers to the founder remain ungateable — `.claude/skills/coordinator-register-check/` carries that half, and it is weaker (the pre-existing `decision-lookup` skill was invoked ZERO times in the session that produced the nine), which argues for routing more coordinator→founder questions through `AskUserQuestion` where the gate already bites. Proven by selftest cases D1–D27 / LD1–LD3 / W4–W7 in the always-run `gate-scripts` job |
| **Link gate — every relative link in tracked markdown must resolve (2026-09-01)** | ✅ | Founder directive 2026-09-01, verbatim: *"excellent point put in place this url checker that must be executed locally and enforced in the CI too"* — **both halves**: `make link-check` locally, and two pinned steps in CI's always-run `gate-scripts` job ([#837](https://github.com/TheCaptainCompany/captain-food/issues/837)). `docs/**` IS the operating model — CLAUDE.md is an index whose authority is the topic file it links to, every ADR cites its neighbours, and the register-check discipline turns on a reader being able to FOLLOW a citation — so **a broken link is a citation that silently resolves to nothing**, rendered dead by GitHub with no error. **Method, because a link count is meaningless without one**: relative link TARGETS (inline `[t](p)`, images, and link reference definitions) in the markdown `git ls-files --cached --others --exclude-standard -- '*.md' '*.markdown'` reports, resolved against the tree; fragments checked against github-slugger's algorithm plus explicit `<a id>` anchors. External URLs, footnote definitions and links inside fenced or indented code are NOT links for this purpose. **The corpus includes UNTRACKED files** (`--others`), so scratch markdown present at measurement time moves the figure -- which is why the number is quoted against a NAMED COMMIT measured in a CLEAN checkout. Measured against the merge base **`43317168`** in a clean checkout: **8,060 relative links across 451 markdown files, of which 124 were broken** — **28** dangling paths and **96** dead fragments (95 in `specs/generated/documentation.generated.md`, 1 in `specs/integrations/hubrise.md`). The generated ones were an EMITTER defect, not content: a test's `when:` is not always a command (59 tests are driven by an inbound integration event, ADR-0004) and a saga is documented as an `actor`, not an `entity` — both were hardcoded kinds in `emit/docs.rs`. Fixed at the emitter, never in the output — in **both** artifacts: the HTML twin `h_any_link` had the same missing arm and shipped 27 dead `href`s that no markdown scan can reach, so `neither_generated_documentation_artifact_has_a_dead_in_page_link` now asserts the invariant over both files. **Gated at ZERO with no baseline file** — a baseline is a second thing to keep honest. External URLs are deliberately out of scope: a blocking gate must not be redable by a third party's rate limiter. **Three vacuity guards, because a scanner that matches nothing passes**: the corpus is derived from `git ls-files` (never a literal list) and an empty corpus, a corpus without `CLAUDE.md`, and a corpus yielding zero links each exit 2 with their own message — all three proven RED by `tools/link-check-selftest.sh` (19 cases, completeness-asserted), which CI runs as its own step BEFORE the scan. Pinned by `the_link_check_runs_in_the_always_run_gate_job`, seen red against a `|| true` mutant. It lives in `tools/`, not `.claude/hooks/`, because the gate job's needle scan forbids a non-gate step from mentioning `.claude` |
| Citation ratchet + docs-only CI enforcement | ✅ | Validator §22/§23 on `make validate` (every full-form ADR/PROP citation across `docs/**` + `CLAUDE.md` resolves); the docs-only CI path runs the canonical validator (`docs-validate` job + by-name `codegen` aggregator assertion — the pre-2026-08-21 bypass is closed and pinned by shape tests) |

## 📋 Remaining work — todo & session split

> **⚠️ TRACKING MOVED (2026-07-20, user-directed): remaining work now lives in
> [GitHub issues](https://github.com/TheCaptainCompany/captain-food/issues) (#12–#28, typed
> Task/Bug/Feature) managed on the **org-level GitHub Project**
> ([github.com/orgs/TheCaptainCompany/projects](https://github.com/orgs/TheCaptainCompany/projects),
> created 2026-07-20) — not in this file.** Issues carry `size/*` labels + org issue fields
> Priority/Effort (mapping recorded in ADR-20260720-143000); the project's views read those
> directly, so triage state lives on the issue, never in a board-only field. New work items get an
> issue, not a table row; this file stays the narrative deployment/architecture snapshot. The table
> below is the last pre-migration snapshot, kept for history.
>
> **Issue workflow (2026-07-20, ADR-20260720-143000):** every issue is sized once with a
> `size/XXXS`…`size/XXXL` label (AI-native scale: agent sessions + cost + review, see the ADR
> table) and carries standard pre-task sections — *Why now? / What & why? / Impact / Sequence
> diagram / Estimation* (with its rank in the simplest→largest queue). The issue is the pre-task
> contract; the PR is the post-task record — overlap is intentional, divergence is signal. No
> Scrum: flow-based queue, cheapest-impactful first; re-size only on scope change; XXXL must be
> split before starting.

Two sessions run in parallel — 🅐 = this (desktop) session, 🅑 = the iPhone/other session. Pull-rebase before every push.

| # | Item | Owner | Status |
|---|---|---|---|
| 1 | **Checkout saga** — `placeOrder` + `PlaceOrderProcess` + PM runtime | 🅐 | ✅ wired (real Stripe gateway; smoke-proven in prod) |
| 1a | **Checkout snapshot** on `PaymentIntentCreated` (ADR-20260719-014434) — DSL + `place_order` freeze + tests done | 🅐 | ✅ DSL · runtime population + port retirement ride pricing |
| 1b | Stripe **outbound** `PaymentGateway` (create PaymentIntent) in the Stripe adapter crate | 🅐 (landed here, not 🅑) | ✅ `stripe::outbound::StripePaymentGateway` (create-intent + refunds, env-gated by `STRIPE_SECRET_KEY`, fail-closed stand-in otherwise) — exercised by the green prod smoke |
| 2 | **HubRise** domain ACL — webhook → `ImportCatalog`/`OfferStockUpdated` (OAuth2 pull + deterministic ref-mapping) | 🅐 | ✅ landed (`enrich.rs`, 14 tests) |
| 2a | **Connect flow** — provision `RegisterRestaurantAccount` + `Restaurant`(s) + `CreateCatalog` with the enricher's derived UUIDv5 ids, and persist the HubRise **account-scoped** token in `hubrise_connections` keyed by `RestaurantAccount`. See `docs/integrations/hubrise-process.md` §0 | #20 | ✅ (ADR-20260721-100601) |
| 3 | **Process managers** — Refund/CartBinding/DeliveryDispatch + PM runtime (event-driven, `/saga`) | 🅐 | ✅ (outbound refund via the real gateway; bounded partner re-offer landed — offer timeouts deferred, ADR-20260720-004556) |
| 4 | **Cart line invariants** + catalog `tree` projector + offer read port | 🅐 | ✅ |
| 5 | **Frontend** — Leptos/WASM SDUI renderer (customer/restaurant/rider apps) | unassigned | 📋 |
| 6 | GraphQL **subscriptions** (`SubscriptionRoot` + bus + WS + ACL) | 🅐 | ✅ |
| 7 | **Structured typed errors** (ADR-20260719-120000) | 🅐 | ✅ |
| 8 | **Per-field nav-edge ACL** — optional `roles:` on nav fields (default public), same guard/visible as ops; design agreed | 🅐 | 📋 plan mode (after ACL emitter free) |
| 8b | Delivery/account read queries + catalog `tree` + `me`/favorites | 🅐 | ✅ (read surface complete except `operation`; `phoneCountries` deleted with #305) |
| 9 | Remove `INTERNAL_TRIGGER_TOKEN`/drain endpoint (use `/ping` warmth) | 🅐 | 🗑️ deferred |
| 10 | Projection worker robustness (poison-skip) + spin-down mitigation (uptimerobot `/ping`) | 🅐 | ✅ |
| 10a | **Push-driven drain loops** ([#300](https://github.com/TheCaptainCompany/captain-food/issues/300), ADR-20260802-200416) — `pg_notify` in the append transaction + one `LISTEN` connection wakes the projector AND the saga runner; safety-net drain kept (NOTIFY has no replay) and the fallback reverts to the 1.5 s poll whenever the listener is down; idle head-gate skips per-group queries when the log has not moved | 🅐 | ✅ idle DB round trips ~70,900/h → ~120/h, and sagas react on commit instead of up to 1.5 s later. **Requires a session-mode pooler** (Supabase 5432); `RUN_EVENT_PUSH=false` forces polling |
| 10b | **Mailbox keyspace width 100 → 5** (ADR-20260802-220402) — post-#301 audit found the mailbox out-polls what #301 removed: 16 actor types × 100 lanes × one per-lane SELECT per 10 s pass ≈ 580k idle queries/h, un-gated. Width 5 in `specs/actors.yaml` + migration `20260802220000` (exact remap: 5 divides 100, so `partition % 5` = the width-5 stamp; rows remapped BEFORE registry shrink) | 🅐 | ✅ idle mailbox queries ~580k/h → ~29k/h. Real fix is 10c |
| 10c | **Push-driven mailbox** ([#313](https://github.com/TheCaptainCompany/captain-food/issues/313), [PROP-20260802-223522](proposals/PROP-20260802-223522-push-driven-mailbox.md) approved D1–D5, ADR-20260802-224532) — `pg_notify` at the `PgMailbox` door (one channel, actor-type payload) wakes workers cross-process; lanes-with-work idle gate; attempts-cap poison policy (`FAILED` + error at the cap); gated `RUN_MAILBOX_PUSH` + `MAILBOX_MAX_DELIVERY_ATTEMPTS` | 🅐 | ✅ door notifies in the enqueue tx (`PgMailbox` + PM chain); listener per process feeds the nudge map cross-process; full pass 60 s under confirmed push (beat stays on heartbeat, degradation = pre-push cadence); poison cap default 5 (`0` = old behaviour); retries back off EXPONENTIALLY since #316 (base x 2^(N-1), ~5 min to terminal at cap 5); heartbeat/lease/cap wired from Config (MAILBOX_* keys were previously unread) |
| 10d | **Typed actor inbox** ([#771](https://github.com/TheCaptainCompany/captain-food/issues/771), [ADR-20260830-183000](adr/ADR-20260830-183000-the-actor-inbox-is-a-generated-enum-and-the-match-is-human-owned.md), founder directive *"Go for the generated per-actor enum"*) — one GENERATED `<Actor>Inbox` enum per mailbox actor from `actors.yaml` `receives:` (COMMAND / inbound FACT / REMINDER, typed payloads), matched in the HUMAN-OWNED `infrastructure::inbox`. `ActorInbox::parse(actor_type, message_type, payload)` is the single fallible edge and takes the LANE, so a lane-A row carrying B's message cannot be constructed | 🅐 | ✅ an unconsumed declared message is `rustc` **E0004**, not a `FAILED "unroutable command type"` row — proven red from a mutated model in CI. Found **10 commands declared received with no dispatch arm**; **all 10 had their handler already written and all 10 are wired** (2 were live PM `sends:`, incl. `GrantCustomerCredit`). The `deferred: { reason, issue }` DSL grammar shipped with **zero users** and gained its first **seven** with #780 (allow-list, never a count; arms and declarations held equal both ways, closing [#781](https://github.com/TheCaptainCompany/captain-food/issues/781) for the fact half); [#777](https://github.com/TheCaptainCompany/captain-food/issues/777) narrows to which door should reach `UpdateDeliveryStatus`. 4 gates deleted as compiler-subsumed, 2 added where types cannot reach (the no-catch-all one asserts the parsed PROPERTY — a named binding is a wildcard with a name). **The guarantee now covers FACT delivery too** ([#780](https://github.com/TheCaptainCompany/captain-food/issues/780), [ADR-20260830-224500](adr/ADR-20260830-224500-an-unrecorded-declared-fact-parks-it-is-never-terminally-failed.md)): a GENERATED per-actor `<Actor>FactInbox` (the `receives:` entries of kind Fact/Reminder only) plus a total `ActorInbox -> Option<ActorFactInbox>` projection, matched by the human-owned pure `fact_route`. A COMMAND variant is UNSPELLABLE there, so no arm needs a lane wildcard — which matters because `ActorInbox::Payment(_)` passes BOTH #776 predicates while absorbing the whole lane; the scan now recurses under the composite and the fact half has its own reach pin (the global floor was met by the command half alone). Of **27** declared facts: 12 already routed, **5 more Payment facts wired** (`already_records` covers all ten; the record leg carries the lane's TYPED `PaymentFactInbox`, so the stream lookup is `intent_of_fact` — total over the enum. The first cut widened it back to a `DomainEvent` and the five did **not** record: review caught it by enqueueing a real `RefundOpened` through the real worker, `RecordLeg` makes the recorder/payload mismatch unspellable, and `fact_delivery.rs` now drives all five through the lane — seen red on the pre-review path), 3 PM legs typed, **7 declared `deferred:`** — the grammar's first users, each because the receiving aggregate has no fold rule answering *"already reflected?"* ([#784](https://github.com/TheCaptainCompany/captain-food/issues/784)/[#785](https://github.com/TheCaptainCompany/captain-food/issues/785)/[#786](https://github.com/TheCaptainCompany/captain-food/issues/786)/[#787](https://github.com/TheCaptainCompany/captain-food/issues/787)). An unrouted fact **PARKS**, never terminal: a fact cannot be refused, and `code: "Internal"` was invisible to `poisonedMailboxMessages` and refused by `RequeueMailboxMessage`. Two more catch-alls died (`handle_pm_fact`'s money-path one; the unparsable-`DomainEvent` arm, dead once the door's parsed value is reused). New `mailbox-delivery` observability contract — the route had **zero spans** and the status bus ignores non-COMMAND rows, so a lost fact was zero-signal. Unknown type is TRANSIENT → the existing poison park (**terminal `FAILED`** at the cap, operator requeue, lane blocked meanwhile), **no new status, no migration**. Generated `command_router.rs` 761 → 42 lines |
| 11 | **CoopCycle** delivery partner (#58) — third `PARTNER` adapter; **federated** per-instance registry + OAuth2 (ADR-20260721-122910) | 🅐 | 🚧 PR #59: DSL surface (staging + services + obs + c4 + integration doc) landed; `crates/adapters/coopcycle` + server wiring in progress |

## Production is DELIBERATELY SUSPENDED — a decided state, not an open incident (decided 2026-08-17)

**Founder answer, 2026-08-17** ([DECISIONS §45 PROD-1](proposals/DECISIONS.md),
[ADR-20260817-105844](adr/ADR-20260817-105844-the-walk-goes-first-on-one-database-and-production-stays-suspended.md)):
**production stays down and the team walks locally.** The team had recommended restoring with signup
closed at the auth provider; he declined. **The walk does not need production** — `stripe listen
--forward-to` is outbound-only, so the money path walks against a local stack with the CLI's own
signing secret and no cluster ingress (ADR-20260813-004634). Nothing here waits on an account action
any more: restoring is a decision to re-take, not a task to finish.

**The real defect was never the 503.** Verified against the Actions API on 2026-08-17: the nightly
`prod-smoke` has been **RED for 19 consecutive scheduled runs** — last green **2026-07-29**,
2026-07-30 through 2026-08-17 with no gaps — of which the billing suspension explains only **13**.
Six earlier red nights (2026-07-28, 2026-07-30 through 2026-08-04) have an **unrecorded cause**. A
scheduled gate whose red is expected trains everyone to skip the one signal that would say something
new. **Owed** (not filed by the records run that landed the answer): re-point the nightly at the
local walk target, or disable its schedule with the reason recorded in the workflow.

*History*: the Render web service `srv-d9ctcpgk1i2s73cj6820` went `suspenders: ["billing"]` at
~2026-08-04 12:26 UTC and `captain-food.onrender.com` has returned 404 since. `render-status` was
fixed in that run to report **red** on suspension (ADR-20260805-070138) — it previously read only the
last deploy's status and showed a false green while prod was down.

## 🧭 Architecture decisions
See [`docs/adr/`](adr/) for the full chronological log (247 records as of 2026-08-28) — **latest:
ADR-20260828-120500** (an answered question is never asked again — the register-check rule closing
the ask-gate). The picks below are the **load-bearing** ones a new session needs first, not the
newest filenames:

- **[ADR-20260807-002705](adr/ADR-20260807-002705-hosting-ovh-mks-cnpg-gitops.md)** — hosting
  target is OVH Managed Kubernetes + in-cluster CloudNativePG, GitOps-only ops. Decided, **not yet
  built** (see the Deployment table above).
- **[ADR-20260817-105844](adr/ADR-20260817-105844-the-walk-goes-first-on-one-database-and-production-stays-suspended.md)**
  — production stays deliberately suspended; the team develops against a local walk, not a live
  cluster.
- **[ADR-20260802-224532](adr/20260802-224532-push-driven-mailbox-approved.md)** — the CQRS/actor
  posture: the mailbox door notifies via Postgres `pg_notify`, workers wake cross-process instead of
  polling.
- **[ADR-20260719-031136](adr/20260719-031136-write-side-repository-event-sourced-actor.md)** —
  write-side `Repository` / event-sourced actors: handlers and the saga runner route through it,
  never the raw `EventStore`.
- **[ADR-20260821-095957](adr/ADR-20260821-095957-the-register-row-gets-machine-identity-reg2-reg4-and-the-ask-gate-reads-it.md)**
  — decision register rows are machine-readable files; this is the governance spine the ask-gate
  (register-check) reads before any founder question.

**ADR ids are date-time** to avoid concurrent-session collisions (ADR-20260718-135417).

> Convention: keep this file current with every substantive change, and record cross-cutting decisions as an ADR in the same change.

## Journal

**This page is durable state and a journal index. Dated status-journal entries do not go here.**

Write a new dated entry at the **TOP** of the applicable weekly file under `docs/status/` — the
journal is newest-first, so never append at the end of a week file. If the entry falls in an ISO
week that has no file yet, create `docs/status/journal-YYYY-Www.md` with the established header:

    # Status journal — YYYY-Www

    Journal entries for ISO week YYYY-Www, newest first, in the order they were written.
    Current state: [`../STATUS.md`](../STATUS.md).

then add it to the list below and place the entry at the top of the new file.

- [`journal-2026-W30.md`](status/journal-2026-W30.md)
- [`journal-2026-W31.md`](status/journal-2026-W31.md)
- [`journal-2026-W32.md`](status/journal-2026-W32.md)
- [`journal-2026-W33.md`](status/journal-2026-W33.md)
- [`journal-2026-W34.md`](status/journal-2026-W34.md)
- [`journal-2026-W35.md`](status/journal-2026-W35.md)
- [`journal-2026-W36.md`](status/journal-2026-W36.md)
