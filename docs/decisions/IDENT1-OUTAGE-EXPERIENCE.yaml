key: "IDENT1-OUTAGE-EXPERIENCE"
status: "open"
question: "When the identity lookup FAILS (outage class, lookup_failed), does the customer keep the ADR-20260818-004646 silent fail-closed-to-Public experience, or get a visible 'couldn't load your account — retry' state?"
owner: "founder"
opened: "2026-08-28"
capacity: "founder"
register: "docs/decisions/IDENT1-OUTAGE-EXPERIENCE.yaml"
evidence: "OPEN, deliberately — call-sheet round 5, verbatim: 'Decide later'. The ux + business lenses recommend the visible state (a returning customer mid-order at Friday peak reads silent-empty as 'my account vanished' and abandons); the recorded ADR posture is silent fail-closed. REVISIT TRIGGER: the IDENT1-RESOLUTION-ACTIVATION flip — the question only matters once the Postgres path serves traffic, so it returns to the founder's decision queue WITH that flip, never before, and never as a re-ask in between."
