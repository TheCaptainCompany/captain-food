key: "PROP-20260811-000946--D3"
status: "decided"
question: "What does a behaviour-event declaration carry, and which `kind:` values exist?"
owner: "team"
opened: "2026-08-11"
decided: "2026-08-11"
decided_by: "PROP-20260811-000946"
register: "docs/proposals/DECISIONS.md §28"
evidence: "Decided as a closed `kind:` enum of `VIEW | INTERACTION` only -- `IMPRESSION` and session replay are absent from the grammar entirely, not merely discouraged -- with `purpose`/`lawfulBasis`/`retention`/`identifierClass`/`specialCategoryRisk`/`dpia` required with no defaults, and `attributes` restricted to bounded sets (a `$ref` to a domain scalar where one exists, never a hand-written list). Team-owned per the same delegation. The DECISIONS.md §28 table's D3 cell matches this current text -- no divergence found. VERIFY-FIRST: the proposal's own Concerns checklist still has 'Counsel review' unchecked -- the legal-field DESIGN is decided, its underlying legal reasoning has had no licensed-counsel review."
capacity: "team"
