# PLATFORM SOURCE, hand-written (#360) -- see deploy/platform/README.md.
# Requires the CRDs from ../cnpg-operator/ to be established first. The ha/ overlay is
# DELIBERATELY not referenced here (nor anywhere): it is the ladder shape of
# ADR-20260807-114122, flipped only by its own one-line ADR.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - storageclass.yaml
  - cluster.yaml
  - scheduled-backup.yaml
  - rbac-drill-access.yaml
