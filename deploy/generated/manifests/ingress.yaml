# GENERATED by the Captain.Food codegen -- do not edit by hand (ADR-20260807-183024 step 4, #349).
# Host routing derived from the screens specs' base_url + per-screen roles (role = path,
# ADR-0006): each host serves its surface bin at / and its audience roles' /{role}/graphql at
# that role's gateway. TLS: the cert-manager DNS-01 wildcard certificate (#358 bootstrap issues
# it; ingress-nginx terminates). The BARE captain.food host is deliberately unrouted -- the
# screens specs disagree on its owner (see the emitter doc comment).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: captain-food
  namespace: captain-prod
  labels:
    app.kubernetes.io/part-of: captain-food
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "live.captain.food"
        - "*.captain.food"
        - "restos.captain.food"
        - "riders.captain.food"
        - "system.captain.food"
      secretName: wildcard-captain-food-tls
  rules:
    - host: "live.captain.food"
      http:
        paths:
          - path: /customer/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-customer
                port:
                  name: http
          - path: /public/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-public
                port:
                  name: http
          - path: /webhooks
            pathType: Prefix
            backend:
              service:
                name: adapters
                port:
                  name: http
          - path: /adapters
            pathType: Prefix
            backend:
              service:
                name: adapters
                port:
                  name: http
          - path: /services
            pathType: Prefix
            backend:
              service:
                name: adapters
                port:
                  name: http
          - path: /external/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-external
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fo-marketplace
                port:
                  name: http
    - host: "*.captain.food"
      http:
        paths:
          - path: /customer/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-customer
                port:
                  name: http
          - path: /public/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-public
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fo-storefront
                port:
                  name: http
    - host: "restos.captain.food"
      http:
        paths:
          - path: /restaurant/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-restaurant
                port:
                  name: http
          - path: /restaurant-account/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-restaurant-account
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bo-restaurant
                port:
                  name: http
    - host: "riders.captain.food"
      http:
        paths:
          - path: /rider/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-rider
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bo-rider
                port:
                  name: http
    - host: "system.captain.food"
      http:
        paths:
          - path: /admin/graphql
            pathType: Prefix
            backend:
              service:
                name: gateway-admin
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bo-admin
                port:
                  name: http
