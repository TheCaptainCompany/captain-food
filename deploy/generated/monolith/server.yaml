# GENERATED by the Captain.Food codegen -- do not edit by hand (#358 cutover, ADR-20260807-183024).
# THE WORKLOAD WE ACTUALLY DEPLOY: the monolith `server` (c4-l2 `server`, deploy_tree: monolith).
# The single deployed process (ADR-0042/0043): serves every audience host and every /{role}/graphql path from one binary, ingests partner webhooks, and runs the projector in-process. Split into the bin topology by ADR-20260807-183024 steps (6)-(7); retired when they flip.
# Image resolved from the deploy ledger deploy/pins/server.json ({digest, source_hash}; rollback =
# git revert of the pin change).
# UNPINNED: deploy/pins/server.json has no digest yet -- CI's pin-bump writes it and
# regenerates this file; the :unpinned tag is deliberately undeployable, never a default.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: captain-prod
  labels:
    app.kubernetes.io/name: server
    app.kubernetes.io/part-of: captain-food
    captain.food/family: monolith
spec:
  # ONE replica, strategy Recreate -- and here it is not a placeholder for #242's leases: this pod
  # ALSO hosts the in-process projector and the mailbox drain (ADR-0040/0043), so a second replica
  # would double-drain the same lanes and a RollingUpdate would overlap two of them mid-deploy.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: server
        app.kubernetes.io/part-of: captain-food
        captain.food/family: monolith
    spec:
      containers:
        - name: server
          image: ghcr.io/thecaptaincompany/captain-food:unpinned
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: APP_PROFILE
              value: production
            - name: PORT
              value: "8080"
            - name: HUBRISE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: HUBRISE_WEBHOOK_SECRET
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: DATABASE_URL
            - name: AUTH_SESSION_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: AUTH_SESSION_KEY
            - name: SUPABASE_SMS_HOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: SUPABASE_SMS_HOOK_SECRET
            - name: SUPABASE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: SUPABASE_SECRET_KEY
            - name: HONEYCOMB_API_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: HONEYCOMB_API_KEY
            - name: EXTERNAL_API_TOKENS
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: EXTERNAL_API_TOKENS
            - name: INTERNAL_TRIGGER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: INTERNAL_TRIGGER_TOKEN
            - name: PLATFORM_BOOTSTRAP_ADMIN_SUBJECT
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: PLATFORM_BOOTSTRAP_ADMIN_SUBJECT
                  optional: true
            - name: OVH_APPLICATION_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: OVH_APPLICATION_KEY
            - name: OVH_APPLICATION_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: OVH_APPLICATION_SECRET
            - name: OVH_CONSUMER_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: OVH_CONSUMER_KEY
            - name: OVH_SMS_SERVICE_NAME
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: OVH_SMS_SERVICE_NAME
            - name: EMAIL_QUOTA_KEY_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: EMAIL_QUOTA_KEY_HMAC_SECRET
            - name: UBER_DIRECT_CUSTOMER_ID
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: UBER_DIRECT_CUSTOMER_ID
            - name: UBER_DIRECT_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: UBER_DIRECT_CLIENT_ID
            - name: UBER_DIRECT_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: UBER_DIRECT_CLIENT_SECRET
            - name: UBER_DIRECT_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: UBER_DIRECT_WEBHOOK_SECRET
            - name: STRIPE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: STRIPE_SECRET_KEY
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: captain-secrets
                  key: STRIPE_WEBHOOK_SECRET
          # READINESS is /health ON PURPOSE (ADR-0043): it returns 503 while the applied schema
          # is behind the binary's REQUIRED_SCHEMA_VERSION, so a deploy that races ahead of the
          # out-of-band migration is held OUT of rotation instead of serving a half-migrated read
          # model. `failureThreshold` is generous for exactly that case -- the pod is not sick, it
          # is waiting for CI's `sqlx migrate run`.
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 30
          # LIVENESS is /ping, which never touches the database -- a database outage must not get
          # the process killed and restarted into the same outage.
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 6
          resources:
            # Sized from what this binary has actually been living inside: the Render free instance
            # it runs on today is 512Mi/0.1 CPU. Requests match that floor; the limit keeps the
            # headroom the SSR + projector path uses at peak without letting a leak take the node.
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: captain-prod
  labels:
    app.kubernetes.io/name: server
    app.kubernetes.io/part-of: captain-food
    captain.food/family: monolith
spec:
  selector:
    app.kubernetes.io/name: server
  ports:
    - name: http
      port: 80
      targetPort: http
